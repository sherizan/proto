{
  "name": "proto-cli",
  "version": "0.0.0",
  "description": "Proto CLI — start, add, edit, new-screen, reset",
  "type": "module",
  "bin": {
    "proto": "./dist/index.js"
  },
  "files": ["dist"],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  }
}
