{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "lib": ["ES2022"],
    "types": ["node"],
    "noEmit": true
  },
  "comment": "Typecheck covers only the pure runtime. The RN .tsx files (renderNode, ManifestRenderer) are validated on-device, matching proto-components (CLAUDE.md §3).",
  "include": ["src/runtime.ts", "src/runtime.test.ts"]
}
