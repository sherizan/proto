# Proto BACKLOG

> Future ideas and known follow-ups — things we intend to do but aren't acting on yet. Not commitments.
> When an item is picked up, it moves to `STATUS.md` "In progress"; when it ships, it lands in
> `CHANGELOG.md`. For current state → `STATUS.md`. For risks → `RISKS.md`.

## Native dev-client bundle

> Full candidate list, SDK-55 versions, and tradeoffs in `docs/native-bundle.md`. Each tier is one
> batched `release:all` cut (+ Info.plist usage strings for permissioned modules).

> Tier 1 + Tier 2 shipped at SDK 56 (see `CHANGELOG.md` / `STATUS.md`); only the items below remain.

- **Tier 3 stays on-demand** — sensors, sharing, secure-store, file-system, device/network/localization/orientation, datetimepicker, calendar, contacts, brightness, battery. Native-detection warning covers these; don't pre-bundle.
- **On-demand cloud dev-client rebuild** (big) — `proto add <native>` triggers an EAS build of a custom client + auto-install, for the long tail beyond the bundled set. Preserves no-tools UX; costs ~10-20 min build + EAS spend. The "real" fix for arbitrary native modules.

## CLI & reliability

- **`proto version` / `proto doctor`** — surface the installed Prototo dev-client version (and bundled-module set) to the designer. Today you read it via `xcrun simctl`; no command exposes it.

## Tooling / infra

- **Expo MCP** is now available via the `expo` session plugin (its tools load automatically). Just confirm it's authenticated (`/mcp`) so Claude can query EAS builds + simulator state directly; drop this line once verified.
- **Bump fallback version in `prototo-website`** (`/Users/sherizan/Public/prototo-website/lib/npm.ts:9`). Currently `"0.1.10"`; the live site auto-fetches the real version from npm but uses the fallback when that fetch fails. Single-line edit in a separate repo.

## Proto MCP — remaining tools

> The `prototo` MCP server shipped its first slice 2026-06-14: a stdio server (the `proto-mcp` bin inside `@sherizan/proto-cli`) exposing **`compile_check`** + **`get_simulator_screenshot`**, auto-connected via the scaffold's `.mcp.json`. The 3rd tool, **`get_metro_errors`**, was built 2026-07-05 (see `STATUS.md`) — the trio now closes the "Claude writes screens blind" loop. These two lower-value tools stay parked:

- **`list_screens`** — cheap to add to the stdio server: `fs.readdir` `screens/`, filter `.tsx`, stat for `lastModified`, return JSON. Low value, though — Claude can already glob `screens/` directly. Build only if a reliable timestamped inventory proves worth a tool call.
- **`get_design_md`** — return DESIGN.md as structured JSON via a `## Section` / `- Key: Value` parser. **Lower value than the brief implies:** DESIGN.md is hand-authored human reference today (generated by `renderDesignDoc`), and `proto.config.js` is the real designer surface. Claude can already read DESIGN.md directly. Build only if token cost of re-reading it proves to matter.

## Share — path to public launch

> Single ordered checklist to take cloud-streaming share from "shipped but hidden" to publicly usable.
> **Order matters:** don't start the Appetize per-minute bill before the CLI↔web round-trip is proven, and
> don't advertise `proto share` before recipients can actually watch a stream. Economics: `2026-06-12-share-economics-and-pricing-design.md`. Risks: `CS-01` (Appetize cost), `CS-07` (metering accuracy).

- [x] **Web accounts MVP live** — Supabase auth, authenticated `POST /api/share`, Postgres share store + Free caps, `/account`, metering. Prod, 2026-06-12.
- [x] **CLI auth shipped** — `proto-cli 0.6.1`: `proto login` + Bearer `proto share`, auto-login when logged out, friendly 401/cap/owner copy. npm `latest`, 2026-06-13.
- [x] **Prod Appetize public key set** — `NEXT_PUBLIC_APPETIZE_PUBLIC_KEY` in Vercel.
- [x] **Live end-to-end smoke (free tier — no Appetize upgrade yet).** ✅ 2026-06-13 — `proto share` in the `share` project hit the **auto-login** path (logged out → browser sign-in → continued), published an EAS update, and registered the share: `prototo.app/p/YNRE5DK8QE6J`. Verified server-side via `GET /api/share/<token>`: attributed to the account (`designerName`/`appName`), deep link pinned to the central `8c8ddf7d…` project + the new update group, 7-day Free expiry applied. CLI↔web round-trip proven on prod. (Recipient `/p/<token>` correctly shows the graceful fallback — Appetize gate, next step.) Still untried: the explicit 4th-project cap message.
- [x] **Appetize upgraded to Starter (2026-06-14).** External-domain embeds now stream. First paid-path validation done: opened `prototo.app/p/YNRE5DK8QE6J` in a plain browser (Playwright) and the real prototype streamed live on a cloud iPhone — the old "embeds not enabled" block is gone.
- [x] **Un-hide `proto share` + publish — SHIPPED (2026-06-14).** `@sherizan/proto-cli 0.6.2` + `create-proto 0.6.1` published to npm `latest` (create-proto's proto-cli dep correctly rewritten to `^0.6.0`, no `workspace:` leak). Re-applied fresh on branch **`ship-unhide-share`** off current `main` (`f6858da`, post-MCP-merge) rather than the stale `unhide-proto-share`: `share` line restored in `proto help` + examples (`cli.ts`), create-proto's stop message (`howToRestart` + test), and the scaffold `README.md`; the scaffold `template/CLAUDE.md` "two fidelity tiers / manifest viewer" section + root `CLAUDE.md §1` invariant replaced with the cloud-streaming truth (full-fidelity stream; only caveats = SDK runtime-version gate + simulated hardware). 325 tests green, both typecheck clean. Commit `f17748f`. PR #11 polish (clickable share link + login copy) rode along. **Branch push + PR pending Sheri's go.** The stale `unhide-proto-share` branch can be deleted.
- [x] **Announce — CHANGELOG entry added (2026-06-14).** `docs/CHANGELOG.md` June 14 entry: "Share a prototype with `proto share`. It gives you a link anyone can open in their browser to watch the real thing run live, no install needed." Website mirrors it via the `update-changelog` skill on its next run. (Optional follow-up: a Free-tier sign-up funnel note per the pricing spec.)

✅ **Checklist complete — cloud-streaming share is publicly live.** Follow-ups now live under Roadmap / "Cloud-streaming share follow-ups" (code-signing CS-05, cost modeling CS-01, embed polish CS-02) and the Team tier / Stripe fast-follow.

## Roadmap

- **Phase 3 web share — SHIPPED (2026-06-14), as cloud streaming.** Delivered as **cloud streaming** (Appetize / EAS Update), not the original App Clip + manifest renderer: `proto share` publishes via `eas update` and `prototo.app/p/<token>` embeds the live stream. The retired manifest/App-Clip/viewer stack (`packages/proto-manifest`, `proto-renderer`, `proto-compile`, `apps/prototo-viewer`) is documented in `docs/archive/` (the `MANIFEST.md` banner there) + the `docs/RISKS.md` SUPERSEDED rows. Ongoing polish lives under "Cloud-streaming share follow-ups" below.
- **Cloud-streaming share follow-ups** — code-signing the EAS Update channel (`CS-05`), Appetize cost modeling + session caps (`CS-01`), and web-embed polish (loading/cold-start UX, `CS-02`) are tracked against the cloud-streaming plan + `docs/STATUS.md`, not here.
- **Retire the App Clip — DONE (2026-06-17/18).** Deleted `apps/prototo-clip/` + `apps/prototo-viewer/` (PR #22, `da2b40e`) — freed ~1.1GB. Then **finished the de-clip inside the dev client** (PR #28, `21bd35c`): removed `ClipApp.tsx`, `app/p/[token].tsx`, `lib/fetch-manifest.ts`, `manifests/demo.json`; simplified `index.ts` to `import 'expo-router/entry'`; dropped deps `react-native-app-clip` + `@sherizan/proto-manifest` + `@sherizan/proto-renderer` (now unused in the dev client). `ios/` regenerates clean (no `PrototoClip`, no "multiple AppDelegate" warning) on the next prebuild/EAS build; lands in the next dev-client cut. `proto-manifest`/`proto-compile`/`proto-renderer` still exist as packages (`proto-cli` share path imports a `CompileConfig` type from proto-compile).
- **🎯 NEW long-term goal — build a Prototo iOS app for the App Store (Expo-Go-style). → BUILT + SUBMITTED 2026-07-04 (v1.0 build 23, in App Store review).** Replace the EAS-Internal-only distribution (`prototo.app/install/ios`) with a real **App Store** app: a published native Prototo client that loads/streams a designer's prototypes — conceptually like Expo Go, but Prototo-branded. **Design approved:** `docs/superpowers/specs/2026-06-19-ios-viewer-app-store-design.md` — one binary (graduate `apps/prototo-app`, not a new app) for both designers (local Metro) and recipients (native run of share bundles); smart universal link + web-stream fallback on `prototo.app/p/<token>`; sign-in required for recipients (reuses Supabase). **Key open question = Apple Guideline 2.5.2** (loads/executes JS at runtime; Expo Go precedent) — hedged by staged submission (designer-tool framing first) + the web-stream fallback; logged as `AS-07`/`AS-08` in `docs/RISKS.md` (the older `AS-01`/`AS-04` are the superseded share-path history). Native runs are unmetered → pricing shift `SH-09`. The EAS Internal dev client stays the working distribution until this lands. **SHIPPED 2026-07-04 — native launcher-free runtime built (`withPrototoNativeHost` plugin + 3 `DC-13` patches); v1.0 SUBMITTED to the App Store (build 23, in review); survived two rejection rounds, both on non-2.5.2 guidelines. Full status in `STATUS.md`.**
- **No de-clipped device dev-client build has shipped (surfaced 2026-06-14).** The install URL serves device build `3b56455a` (commit `b5918f9f`), which is clean but **predates cloud streaming** (no `expo-updates`/`runtimeVersion "prototo-56"`). Fine for designers' own prototyping (Metro/QR), but if the *device* client should ever load EAS-Update shares like the Appetize sim client (`cf91c268`), cut a fresh build from current de-clipped `main` via `cd apps/prototo-app && pnpm release:all` and update `/install/ios/app`. Not urgent — only the Appetize-hosted sim client needs the updates capability, and it already has it.

> Note: no other backlog items below are App-Clip/manifest/viewer-specific — the native dev-client bundle and CLI work still apply, since cloud streaming runs the same Prototo dev client (now on a cloud device instead of an App Clip).

## Watch

- **DC-08 `react-native-ease` maturity** — revisit after first real designer-usage feedback on the `motion` subpath. If ease misbehaves on common props (e.g. `borderRadius` on iOS), the fallback is to swap to `gestures` per the template CLAUDE.md guidance.

---

## How this file is maintained

- **Add** an item when a follow-up surfaces that we're deliberately deferring (not acting on this session).
- **Promote** an item to `STATUS.md` "In progress" when work starts; remove it from here.
- Keep entries one or two lines. This is a backlog, not a spec — link to a spec under
  `docs/superpowers/specs/` if an item needs real design.

## ensure-prototo-app: version check is semantically dead (works by accident)
`ensurePrototoAppMatchesProject` early-returns when the installed app's
CFBundleShortVersionString MAJOR equals the project's expo SDK major. In
reality the app ships 1.x (app.json) vs SDK 56 — never equal — so it refreshes
from `-latest` on EVERY proto start (this IS today's auto-update; cache makes
it fast). Two latent issues: needless uninstall+reinstall churn each start, and
updates would silently FREEZE if app.json ever reached major 56. Replace with a
real staleness check (record installed tarball sha256 per device at install,
compare against -latest manifest.sha256). Low urgency; found 2026-07-06 while
debugging the ui=bare rollout.

## Viewer app can't load self-hosted shares — new build blocked on App Store review
QR/share links open the installed Prototo Viewer (TestFlight / App Store track),
but it can't load prototypes published via the NEW self-hosted flow — the
shipped build predates the `isValidShareDeepLink` update that trusts
`prototo.app/api/manifest/<token>` (only u.expo.dev passes). The fix is already
on main (self-hosted-publish branch, +9 viewer tests). ACTION once build 23's
review completes: cut + submit the next iOS build (also carries DC-07 0.2.3+
fixes and the ui=bare parser). Until then recipients should use the web
viewer (prototo.app/p/<token> Appetize embed) — works today. Found 2026-07-06
during desktop 1.0.1 testing.

## Viewer: warm deep links mostly don't route — FIXED 2026-07-07 (`f4a900b`, ships in App Store 1.0.2)
Root cause was neither expo-router nor the DC-07 funnel: expo-dev-launcher's
app-delegate subscriber parks any external URL in its pending registry when it
thinks no app is running (always true in the launcher-free build, since our
AppDelegate mounts the shell itself) and returns true, which short-circuited
`super || RCTLinkingManager` — expo-router never received warm URL events.
Cold start worked via the initial-URL path. Fixed by calling RCTLinkingManager
unconditionally and OR-ing results; verified on a Release sim build (warm
share / bogus / connect links all navigate in sequence). Original report:
only the first foreground navigation worked; subsequent prototo:// opens left
the router stuck on the current tab.

## QR-scan bug post-mortem (2026-07-07) — 4 fixes to find the reported one
Builds 26-29 each shipped a real fix (shell +not-found redirect, warm-link
delivery, mounted-prototype URL guards) that was not the reported bug; build
30's ExpoLinkingRegistry clear was. Root process failure: verification ran
against reconstructed flows (dev-client-channel loads bypass URL delivery
entirely). Prevention now in place: docs/RELEASE-CHECKLIST.md gates store
cuts on reproducing the reported flow + apps/prototo-app/scripts/sim-e2e.sh
(PASS/FAIL recipient-flow run on a dedicated sim).

## Viewer: stakeholder-first revamp (ideas from Sheri, 2026-07-07)
Two audiences, one app: creators (done well) and stakeholders (underserved —
"Mine" is permanently empty for them). Direction: unconventional, 1-2 actions
done extremely well, no noise. Items:
- **Remove the pinned sample prototype** — it existed for App Review; app is
  approved, drop it in the next app build.
- **Bring back a scanner entry point** (route `/connect` exists;
  `SHOW_CONNECT_CARD` is false, so the scanner is currently unreachable from
  the UI — and docs.prototo.app already tells designers to "tap Scan").
- **Home rethink for stakeholders** — recipient-first landing (open a link /
  scan), creator dashboard secondary.
- **Comments on specific parts of a prototype** — the stakeholder feedback loop.
- **Better viewing gestures.**
- **Floating overlay menu: settings toggle to hide it** — persistent today,
  disrupts look and feel for screenshots/recordings.
