# Proto STATUS

> Living status of Proto work — the **present**. Snapshot, not log.
> Store cuts of `apps/prototo-app` gate on `docs/RELEASE-CHECKLIST.md` (repro the reported flow + `scripts/sim-e2e.sh`).
> Four-doc model: **past** → `CHANGELOG.md` (designer-facing release notes; source for the website landing page) · **present** → this file · **future** → `BACKLOG.md` · **risks** → `RISKS.md`.
> For raw history → `git log`. For design → `docs/superpowers/specs/`.
> Last updated: 2026-09-25 (night) — **#89 CTA anchors in progress on `feat/flow-cta-anchors` (proto-cli 0.8.14 unreleased); earlier today: SDK 57 FLIP: Viewer 1.1.0 build 41 Ready for Distribution; website runtime switch → `prototo-57` (`0d92533`), changelog live; create-proto 0.8.0 published; PR #60 merged to `main`.** (One sentence only; older "Last updated" text is in STATUS-ARCHIVE.md.)

## Currently live

| Layer | Version | Source / install path |
|---|---|---|
| **Prototo App — App Store viewer** | **1.1.0 build 41 LIVE — Ready for Distribution 2026-09-25** (SDK 57, runtime `prototo-57`, `expo-sensors`, Plus via In-App Purchase, #67). Earlier builds 37–40 rejected/withdrawn — see Done recently. | App Store (`https://apps.apple.com/app/id6772877748`; `prototo.app/install/ios` redirects there) |
| **`prototo.app` app backend** | `GET /api/shares` + `DELETE /api/account` live; **2026-09-18: runtime-aware shares live** — `GET /api/cli/version` carries `runtime` (`lib/runtime.ts` `CURRENT_RUNTIME`, **the migration switch, **flipped to `prototo-57` 2026-09-25**, website `0d92533`**), `POST /api/share` stores `runtimeVersion`, share reads + `/account` expose it, `POST /api/share/[token]/opened` takes `{reason:'stale_runtime'}`, hourly lifecycle cron emails owners of stale shares (once/share/day). Migration `20260918100000` applied. | `prototo-website` (Vercel) |
| Prototo App — device (EAS internal) | **RETIRED 2026-09-18** — App Store only. `prototo.app/install/ios` + `/install/ios/app` redirect to the listing. Last EAS internal builds: `20993e06` (SDK 56, 0.2.3) and `e7668d72` (SDK 57, 1.0.5, never pointed at). | — |
| Prototo App — simulator (SDK 57) | **1.1.0 / 0.2.5 / SDK 57 / runtime `prototo-57`, + `expo-sensors`** — EAS sim build `69ea05fe`, GitHub Release `prototo-sim-sdk57-2` + `prototo-sim-sdk57-latest` (2026-09-19; manifest `nativeModules` carries `expo-sensors`, sha `6c928a6e…`). Verified: installs + launches on a dedicated `Prototo-Test` sim, `NSMotionUsageDescription` present. Before it: `prototo-sim-sdk57-1` (1.0.5, 2026-09-18). **LIVE but dormant**: only expo-57 scaffolds fetch it (create-proto 0.8.0 held for the flip). E2E-verified: `proto start` on a 57 scaffold auto-installs it. | `prototo-sim-sdk57-latest` |
| Prototo App — simulator (SDK 56, shipped scaffolds) | **0.2.4 / SDK 56** (`prototo-sim-sdk56-5`, `prototo-sim-sdk56-latest`) — still what today's 56 scaffolds install; keep for 30 days after the flip (rollback rule). | `prototo-sim-sdk56-latest` |
| Prototo App — cloud/share (Appetize) | **RETIRED 2026-07-07** (Appetize dropped; shares open in the App Store Viewer). Old row kept for the API-key note: `~/.prototo/appetize-token`. | — |
| `create-proto` on npm | **0.8.0** (SDK 57 template, proto-cli pin `^0.8.0`) — PUBLISHED `latest` 2026-09-25 (flip hour). Before: 0.7.11 (2026-07-14, SDK 56). | `npm create proto@latest myapp` |
| `@sherizan/proto-cli` on npm | **0.8.7** (`latest`, published 2026-09-20; `proto add` allowlists pnpm build scripts + `proto start`/`upgrade` heal affected projects, #52). Before it: 0.8.6 remix lineage · 0.8.5 share preview · 0.8.4 `--team|--private` · 0.8.3/0.8.2 remix · 0.8.1 (2026-09-19; point-and-edit `/inspect` routes + dev-overlay backfill on `proto start`). 0.8.0 (2026-09-18) — runtime-aware: bundles labelled `prototo-<project expo major>` (fixes a mislabel bug), `proto upgrade` also moves the project SDK when `/api/cli/version.runtime` says it's behind (retries `expo install --fix` once for pnpm 11), `proto share` refuses stale projects, `proto start` nudges. Dormant while the switch is `prototo-56`. Existing scaffolds pin `^0.7.0` → they get it via `proto upgrade`/the desktop, not auto. | dep of every scaffolded project |
| **Prototo Desktop** | **1.0.18 RELEASED 2026-09-20** (Restart Preview in the View menu + sim dock, dock buttons hover/tooltip like serve-sim's own, README point-and-edit; #7 #31) · 1.0.17 (remix in-app + lineage, team Home) · 1.0.16 (Team/Just me publish) · 1.0.15 (Check for Updates answers the click + "Restart now" dialog; desktop BACKLOG #61) · 1.0.14 (point-and-edit phase 2) · 1.0.13 · 1.0.12 (serve-sim 0.1.46: taps on Xcode 27) · 1.0.11 (`sherizan/prototo-desktop-releases` v1.0.11; updater feed + `/api/download/macos` serve it). On project open, a project whose runtime is behind the website switch is moved automatically (`proto upgrade`, full-screen "Updating your prototype…", Try-again modal on failure); status-bar pill does the same on click. Released BEFORE the flip, as required. | `prototo.app/download` |
| `proto-components` (workspace) | 0.0.0, `private: true` | never published; source of truth for `components/proto/` synced into template via `scripts/sync-template.ts` |

## Done recently

- **2026-09-25 — SDK 57 flip.** Viewer 1.1.0 (build 41) went Ready for Distribution; same hour: website `lib/runtime.ts` `CURRENT_RUNTIME` → `{ version: "prototo-57", expoMajor: 57 }` + SDK 57 items in `lib/releases.ts` (website `0d92533`, pushed; lint/tsc/330 tests green), create-proto 0.8.0 published, proto CHANGELOG Unreleased → September 25, 2026; tag `sdk-57`; PR #60 (`chore/sdk-57-upgrade`, 81 commits) merged to `main` (`847e91a`). Backlog: #13 (docs pass) unblocked + gained motion sensors; #8 rewritten to its two leftovers (real-iPhone acceptance, docs-site mention). Now live: `proto start` nudges, `proto upgrade` migrates (desktop on open), `proto share` refuses 56 projects, stale opens email the owner. **Still to verify by hand:** open a real `prototo-56` share in the 1.1.0 Viewer → "older version" screen + owner email.

- **2026-09-24/25 — Plus sold through In-App Purchase in the Viewer (`sherizan/prototo-shared#67`).** Build 40 was rejected 2026-09-24 (submission `d2413d5e`): 3.1.1, the app accesses Plus bought outside it without offering IAP; plus 2.1(a) wants a demo QR image. Decision: IAP for Plus (monthly + annual, web prices, no intro offer); Team stays web-only. Spec `docs/superpowers/specs/2026-09-24-viewer-plus-iap-design.md`, plan `docs/superpowers/plans/2026-09-24-viewer-plus-iap.md` (both local), risk `AS-09`. **Website** (`feat/plus-iap`, unpushed): migration `20260924100000_apple_iap.sql` (`profiles.apple_original_transaction_id` unique + `apple_plus_until`), `effectiveTier` counts Apple Plus (every tier select updated), `lib/apple/verifier.ts` (Apple Root CA G3 embedded; Production→Sandbox only on INVALID_ENVIRONMENT; OCSP outage → 503), `GET/POST /api/billing/apple`, `POST /api/webhooks/apple` (ASSN v2), `/account` shows 'billed through the App Store'; 321 tests. **Viewer** (`chore/sdk-57-upgrade` `79b77c2..15dc831`): `expo-iap ~5.6.3` + config plugin, `lib/plus.ts` (finish on 200/4xx, leave open on 5xx/network), `components/PlusCard.tsx` on Account (Monthly/Yearly, Restore, Terms + Privacy in the card, Manage subscription for Apple subs, plain 'You're on Plus' for web subs), delete-account line for Apple subs; 92 tests + tsc. **Contract** prototo-shared `122a00c` + `6db6cd4` (Viewer billing). Demo QR for the review sample: `~/Desktop/prototo-review-qr.png`. Not verified on device yet (StoreKit sandbox + native-host registration).
- **2026-09-23 — Viewer 1.1.0 build 39 rejected again; app made plan-blind, review sample published (`sherizan/prototo-shared#66`).** Two notes: (1) 3.1.1 "accesses digital content purchased outside the app, such as Plans" — build 39 showed no plan copy on a free account (only the Plus status pill for paying users), so the app is now fully plan-blind: deleted the pill, `lib/use-tier.ts`, `lib/tier.ts` + tests (`profile.tsx` no longer reads the profiles row at all). 85 tests + `tsc` green. (2) 2.1 "demo account + a Prototo link to test" — root cause: every share is `prototo-56` (switch un-flipped) and build 39 runs `prototo-57`, so any link showed "older version". `proto upgrade` is a no-op until the flip, so `~/Prototo/review-sample` = instagram copied without node_modules/.git, `expo install expo@~57.0.0` + `--fix` + `babel-preset-expo@~57` by hand; published as **`https://prototo.app/p/EPNR0XA78FRN`** (`prototo-57`, v1). Gotcha hit on the way: the copy carried `.proto/share.json`, so the first publish overwrote instagram's `WYDTPPJJSSTY` as 57 — restored by re-sharing instagram (now v12, `prototo-56`); delete `.proto/share.json` when cloning a project. Demo-account answer: none needed, any Apple ID, all accounts identical (sign-in is Apple/Google/email code; there is no password account to hand over). Build 40 = EAS `a8a3917d-efc2-4e2d-9c83-bdeaf730fa92` (finished 18:53, build number 40). Sheri: upload it with `eas submit --id a8a3917d-efc2-4e2d-9c83-bdeaf730fa92` from `apps/prototo-app`, pick build 40 on the version in ASC, paste the reply, resubmit; put the link + "any Apple ID" line in App Review Information. The sample link shows "newer version" on the live 1.0.5 Viewer until the flip — review-only.
- **2026-09-22 — Viewer 1.1.0 rejected by App Review, fixed, build 39 uploaded (`sherizan/prototo-shared#65`).** Two notes on build 38: (1) the Scan tab's pre-permission button said "Allow camera" → now "Continue" (`components/Scanner.tsx`); (2) the Account screen's "Upgrade to Plus" link to `prototo.app/pricing` is a pointer to a subscription not sold through IAP (3.1.1) → removed outright; the "Plus" pill stays (status only, sells nothing). Decision: subscriptions stay on the web, the app never mentions or links to one. 90 tests + `tsc` green. Build 39 = same 1.1.0 version, remote auto-increment, EAS `ca0a1637`, submission `e2658b6b`, uploaded to ASC 17:18; Sheri selected it on the 1.1.0 version in ASC, replied in the Resolution Center and resubmitted the same day (reply: "Build 39 addresses both points: the camera permission button now reads Continue; the Upgrade to Plus link has been removed — the app no longer references or links to any subscription or purchase"), resubmits. What's New unchanged.

- **2026-09-20 (late night) — Desktop 1.0.18 RELEASED (`v1.0.18` on `sherizan/prototo-desktop-releases`, updater feed serves it; source tagged): Restart Preview while streaming (`sherizan/prototo-shared#7`, cheap half).** The View menu gains "Restart Preview" and serve-sim's dock gets a restart button beside full-screen (same clone-Rotate injection as #41); both run the `sim:restart` path (restart + relaunch the prototype, #54). Cure for taps dying while the video streams. Smoke-tested by Sheri on the dev build; the first cut's dock buttons cloned Rotate and froze its disabled look (serve-sim styles hover/tooltip via inline React state, not CSS) — rebuilt from scratch with the same inline values (`3699417`). Also in 1.0.18: README point-and-edit paragraph (#31 desktop half). Heartbeat half stays open on #7.

## In progress

- **2026-09-25 — Flow arrows start at the CTA (`sherizan/prototo-shared#89`), proto-cli 0.8.14 on `feat/flow-cta-anchors`.** Overlay (`proto-components/src/touch-dots.tsx`, synced into `touch-dots-source.ts` + template) answers `POST :3001/links`: walks the fiber tree from the DevTools hook roots for `href`/`onPress` elements, `measureInWindow`, posts frames (screen fractions) + debug stacks. Prompt server relays like `/navigate`, symbolicates each stack to a project file:line (one Metro call). `proto flow` asks `/links` after each `/navigate`: a `Link href` names its target; a `router.push` is read from the source within 8 lines of the pressable (`share-flow.ts linkTarget`); `flow.json` edges gain `at` (one edge per button, `anchorEdges`). Website slice PR #103 (reads `at`, safe on old flows) ships first. CONTRACTS `abab308`. **Unverified on a real project until the CLI is linked into `~/Prototo/nasma` and Export flow runs** (the fiber walk + Fabric public-instance lookup are the risk; both fail open).

- **expo-sensors — SHIPPED 2026-09-25 in Viewer 1.1.0 (`sherizan/prototo-shared#8`).** Dev client 0.2.5 + sim `prototo-sim-sdk57-2` + create-proto 0.8.0 template (AGENTS.md paragraph). **Remaining (in #8):** real-iPhone acceptance (fresh 0.8.0 scaffold → tilt screen renders in the sim without red-screening → `proto share` → moves in the Viewer on iPhone) and the docs-site mention (#13).

## Known issues / quirks

- **App Review rule (updated 2026-09-24, build 40 rejection): Plus must be purchasable in the Viewer through IAP (#67); the Viewer still never links to or mentions web pricing, Stripe or Team.** Superseded rule (builds 38 + 39): the Viewer must not mention, read, or link to a plan, subscription or purchase — at all. Plus is web-only (no IAP), so any "Upgrade" copy or `prototo.app/pricing` link is a 3.1.1 rejection, and build 39 was rejected for "Plans" with only the Plus status pill left; the app now reads no tier. A review build also needs a share on the SAME runtime as the build (a `prototo-57` build cannot open `prototo-56` shares) — keep `~/Prototo/review-sample` on the Viewer's SDK and re-share it before each submission. Permission-prompt buttons use neutral copy ("Continue"), not "Allow …".
- **A `prototo-56` share shows "made with an older version of Prototo" on a `prototo-57` Viewer — by design, until the flip (2026-09-21 field report).** Viewer 1.1.0 build 38 (TestFlight, confirmed by Sheri; App Store live is still 1.0.5) runs `prototo-57`; every share is still stamped `prototo-56` because the website switch (`CURRENT_RUNTIME`) has not flipped and all `~/Prototo/*` projects are SDK 56. Test 56 shares on the App Store 1.0.5 Viewer, or run the flip runbook once 1.1.0 is approved. Not a pipeline bug.
- **Re-publishing after switching accounts fails with a generic dialog (2026-09-21).** `~/Prototo/google-pay`'s share belongs to `hi@sherizan.com`; the Mac was signed in as `sherizan@prototo.app` (token rewritten 17:26), so `/api/share` answered 409 `owner_mismatch` and the CLI printed "That share link belongs to another account." Desktop 1.0.18 showed "Share didn't finish — try again." instead — fixed in desktop `eefd96d` (next cut; prototo-shared#64): the dialog now shows the CLI's last real line. Recovery: `proto login` as the owning account, or remove `.proto/share.json` to publish a fresh link.
- **`pnpm release` (desktop) can fail on Apple's timestamp service** (`codesign … The timestamp service is not available`, seen 2026-09-19 on the 1.0.14 cut). Transient — just re-run; the second run succeeded unchanged.
- **npm shows a new version before it can be fetched.** After `pnpm publish`, `npm view` reported proto-cli 0.8.1 while `npm pack @sherizan/proto-cli@0.8.1` still returned `notarget` for ~1–2 min (2026-09-19). Wait, then verify with `--prefer-online`.
- **Point-and-edit is label-only when:** the project's layout has several `return (` (backfill never wraps it), the overlay is missing/customised, `proto start` < 0.8.1, or the designer starts typing before the file:line answers (~0.5s poll as of the next proto-cli publish; 1.5s on 0.8.1). The agent still finds the element by its literal label.
- **Desktop preview taps can die while the video keeps streaming (1.0.12, Xcode 27).** serve-sim's in-process input session drops when another simulator on the Mac is shut down (repro 2026-09-18: `simctl shutdown` of an unrelated test sim → taps dead, stream fine; desktop relaunch restores). Desktop BACKLOG #59: always-available Restart preview + input heartbeat. **Hygiene for Claude sessions: never `simctl shutdown`/`boot` other simulators while Prototo Desktop is running.**
- **Desktop simulator taps dead on Xcode 27 (field report 2026-09-18).** Xcode 27 replaced Simulator.app with Device Hub; `serve-sim` 0.1.44's input path stopped delivering taps while the stream kept working. Fixed upstream in serve-sim 0.1.46 ("restore Xcode 27 Device Hub streaming and input", 2026-08-21); **RESOLVED: desktop 1.0.12 RELEASED 2026-09-18** (serve-sim 0.1.46; Sheri confirmed taps work on Xcode 27; updater feed + `/api/download` serve 1.0.12; source tagged `v1.0.12`).
- **Publish GC could delete the freshly uploaded bundle (field report 2026-09-18, FIXED on prototo.app).** The post-publish GC read `manifest.json` back from storage and deleted whatever it didn't reference; a stale read (storage CDN propagation) or a concurrent publish made publish N's GC delete publish N+1's launch bundle. Repeated publish/open cycles left a share with every image but no bundle → the Viewer's "Couldn't load this prototype". Fix: objects younger than 15 min are never GC'd (`lib/supabase/bundle-store.ts`). **RESOLVED for instagram `WYDTPPJJSSTY` 2026-09-18 12:52 (re-published → v7, new bundle present, share QR opens in the Viewer); any other share hit earlier heals the same way on its next publish.** (Note: a first re-publish attempt from the desktop left no trace on the server — no upload, no share update — cause unknown; the second went through. Watch for a desktop Publish that reports success without running.) Durable follow-up in BACKLOG: `POST /api/share` should receive the keep-set from the CLI.
- **A Mac-preview connect that fails showed nothing in the Viewer (≤ 1.1.0).** Scanning the desktop "Preview on iPhone" QR (or opening it via the Camera app) fired the native load with no screen behind it, so an unreachable Mac looked like "the scanner doesn't detect the QR" / "the app opens the home page and nothing happens". Fixed on `chore/sdk-57-upgrade` (`/open` screen + `+native-intent` + AppDelegate hands connect links to the JS shell, parking them during cold start) — **sim-verified 2026-09-18 on a Release build: warm link → "Connecting to your Mac…" → prototype; unreachable host → "Couldn't reach your Mac's preview… same Wi-Fi" + Try again after the native timeout; cold start → parked → flushed → loads.** Ships in the next Viewer build (1.1.1); the error copy now also names iOS's per-app **Local Network** permission (Settings → Prototo → Local Network) with an Open Settings button. **ROOT CAUSE FOUND 2026-09-18 night: Wi-Fi client isolation on Sheri's network.** Same SSID, phone `192.168.7.95` / Mac `192.168.5.195` (one /22), Local Network permission ON — yet the iPhone's Safari gets nothing from `http://192.168.5.195:8081/status` (the Mac's own Safari does), the Mac can't ping the phone or any other Wi-Fi peer, and no phone connection ever reached Metro during four port watches. The in-app scanner was proven fine (a public-host connect QR shows the on-screen rejection; a valid one is handed to the native connect, which then can't reach the Mac and, in 1.0.5, says nothing). **Verified workaround (Sheri, same night): iPhone Personal Hotspot with the Mac joined → Preview QR works end to end** (the desktop QR picks up the hotspot address; the Viewer accepts 172.16–31.x). Permanent fix is the router's AP/client-isolation setting. Software follow-up in BACKLOG (tunnel for Preview).
- **pnpm 11 exits 1 on EVERY install once a dependency has an unallowlisted build script** (`ERR_PNPM_IGNORED_BUILDS`, e.g. `@shopify/react-native-skia` after `proto add`). It writes a placeholder (`set this to true or false`) into `pnpm-workspace.yaml` `allowBuilds` and never flips it, so the old "retry once" in `proto upgrade` never worked. **FIXED in proto-cli 0.8.7 (`sherizan/prototo-shared#52`):** `src/pnpm-builds.ts` flips placeholders / adds the named packages as `true`; `proto add` heals + reinstalls, `proto upgrade` heals before its retry, `proto start` heals in place so an affected project's next install exits 0. Verified on `~/Prototo/instagram` (exit 1 → 0).
- **`npx expo-doctor` flags the scaffold's `"proto": "proto"` script** as colliding with the `proto` bin (20/21 checks). Pre-existing, cosmetic; the script is what makes `pnpm proto start` work.
- **Xcode 27 has no `Simulator.app`** (replaced by Device Hub per Expo's SDK 58 notes). `ensure-prototo-app.ts:201` runs `open -a Simulator` silently, which now fails ("Unable to find application named 'Simulator'"); `simctl boot` + the `expo start --ios` deep link still launch the app, so `proto start` works, but nothing brings the simulator window to the front. Seen 2026-09-18 on Sheri's Mac (Xcode 27.0). **FIXED 2026-09-19 on `chore/sdk-57-upgrade`:** falls back to `open devices://device/open?id=<udid>` (DeviceHub, bundle `com.apple.dt.Devices`, at `Xcode.app/Contents/Applications/DeviceHub.app`). Ships with the next proto-cli publish; not yet eyeballed live.
- **`pnpm create proto@latest` has 120-min stale-cache** for `@latest` resolution. Workaround: `pnpm create proto@<exact-version>` to force fresh fetch. `npm create proto@latest` doesn't have this issue.
- **~~3 pre-existing typecheck errors in `packages/proto-components`~~ RESOLVED 2026-09-18** by refreshing its devDeps to the SDK 57 pins (they were stale at RN 0.83.6 / no `@expo/ui` types). Was: (`Card.tsx` can't find `expo-glass-effect` types; `Toggle.tsx` can't find `@expo/ui/swift-ui` types + has one implicit-any). Not caused by the motion-primitives work — present on `main` before this PR. Not blocking; the modules still compile and run.
- **iOS Simulator can stash old Prototo binaries across device instances.** If a designer's `proto start` boots a different simulator device than where the new Prototo was installed, they see a stale version. `ensure-prototo-app` SHA-checks via the GitHub Release manifest and auto-reinstalls when out of date, but only on `proto start` — the first launch of an unused simulator may need `rm -rf ~/.prototo/cache` + a fresh `proto start`.
- **Verifying a native module is in the dev client: check `Prototo.debug.dylib`, NOT `Frameworks/`.** Most Expo/RN modules are statically linked into `Prototo.app/Prototo.debug.dylib` (the ~53MB debug dylib), not shipped as separate `.framework`s — only some (ExpoImage, ExpoFileSystem, ExpoModulesCore, ExpoCamera, ExpoMaps, ExpoVideo, RNReanimated, RNScreens, RNSkia, RNSVG, async-storage, safe-area) get their own framework. The main `Prototo` executable is a ~120KB launcher shell with no module code. To confirm a module: `strings -a Prototo.app/Prototo.debug.dylib | grep -c '<ModuleName>'` (e.g. `ExpoAsset` / `AssetModule`). This was the trap during the 0.1.11 hotfix — the framework list falsely implied the fix failed.
- **`npm cache clean --force` prints an ownership warning** (`sudo chown -R 501:20 /Users/sherizan/.npm`). Pre-existing on Sheri's machine from a past `sudo npm`. Cosmetic.
- **A leftover local Xcode App-Clip build can shadow the official dev client (resolved 2026-06-14).** During the Phase E / E2 on-device tests, clip-containing builds were installed via local Xcode under the **same bundle id `com.sherizan.prototo`** (Phase E embedded the clip into `apps/prototo-app`) and `com.sherizan.prototo.viewer` (Phase E2). Those locally-installed binaries override the official one by bundle id, so the device shows the "App-Clip version" even though the official channels never shipped a clip build. **The official builds are clean:** install URL `prototo.app/install/ios/app` → device build `3b56455a` (commit `b5918f9f`, plain SDK-56 dev client, no clip); the clip-era device build `3d4e1770` ERRORED and never shipped; the only de-clipped finished build is the sim `cf91c268` (on Appetize). **Fix:** delete Prototo (and any "Prototo Viewer") from the device, reinstall from `prototo.app/install/ios`. Confirmed working.

## Next up

> **Everything deferred is now an issue on the single Prototo backlog** — GitHub Issues on `sherizan/prototo-shared` + the Prototo project board (`gh issue list -R sherizan/prototo-shared -l proto` · `gh project view 5 --owner sherizan`). This section keeps only in-flight or event-gated items.

- **Build 41 with Plus IAP (#67), in order:** (1) Sheri, App Store Connect: Paid Apps agreement + banking/tax, Small Business Program, subscription group "Prototo Plus" with `plus.monthly` ($8) + `plus.annual` (web annual price), App Store Server Notifications V2 (Production + Sandbox) → `https://prototo.app/api/webhooks/apple`; (2) ✅ DONE 2026-09-25: migration `20260924100000_apple_iap.sql` applied, website PR #95 merged (`c7937fe`) and live (`/api/billing/apple` → 401, publish preflight still `plus`); (3) dev build on device: products load, native host OK; (4) ✅ build 41 cut 2026-09-25 (EAS `e391bb5a-c18b-426e-8b96-79d6d2788303`, commit `f16508f`) and uploaded to App Store Connect (submission `c8e15dbc`); next: TestFlight sandbox run (buy / restore / cancel / Stripe-Plus account shows no buttons / `proto share` passes the gate); (5) attach both subscriptions + `~/Desktop/prototo-review-qr.png` in App Review Information, reply, resubmit. After that: the open-risks pass → follow-up issues.
- **The SDK 57 flip hour** — DONE 2026-09-25 (see Done recently). Within a week: watch the stale-share email volume (lifecycle cron `shareStale` count) and the desktop auto-move in the wild; if a designer reports a half-moved project, `proto upgrade` again heals it. Gated on the flip: docs pass for 1.1.0 (#13), expo-sensors rollout (#8, rides the Viewer cut after 1.1.0 together with the 1.1.1 fixes on PR #60).
- **Point-and-edit: SHIPPED** (proto-cli 0.8.1 + desktop 1.0.14, 2026-09-19). create-proto 0.8.0 (template carries the overlay natively) published at the flip 2026-09-25; existing projects heal via `proto start`'s backfill. Leftovers → #31.
- **From the evening field report (2026-09-18):** desktop Publish that reported success without publishing → #14 (watch); Preview-on-iPhone docs line → #12; tunnel mode + hotspot hint → #11; GC keep-set → #20.
- **Native-runtime maintenance:** re-validate the 3 `DC-13` patches on the next SDK bump (folded into the SDK 58 issue #24); keep the sample prototype (EAS group `6d42487e`) published or re-pin `apps/prototo-app/lib/sample.ts`.
- **CHANGELOG:** the designer-facing App-Store-launch entry → #36 (Sheri's call on copy/timing).

## How to maintain this file

**At the start of a Claude Code session in this repo:** read this file first to see what's current.

**At the end of a Claude Code session:**
1. If anything in "Currently live" changed (versions, install paths, new releases), update the table.
2. Move freshly-completed work from "In progress" → "Done recently" with date + commit refs.
3. **If a change is something a designer would feel, add a designer-facing line to `CHANGELOG.md`** (under "Unreleased" until it ships). That file — not this one — feeds the website's release notes.
4. If new bugs surfaced, add to "Known issues / quirks".
5. If new follow-ups came up that aren't being acted on, open an issue on `sherizan/prototo-shared` (see CONTRACTS.md Backlog).
6. If a cross-cutting risk surfaced, add it to `RISKS.md`.
7. Update the "Last updated" line in the header.
8. Commit this file alongside whatever else the session produced — the file IS the handoff note.

Keep only the newest 5 "Done recently" entries: move older ones, word for word, to the top of the
"Done recently (older)" section of `docs/STATUS-ARCHIVE.md`. Cap: 30 KB (`~/Public/prototo-shared/check-contracts.sh` checks it).
