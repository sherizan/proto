# Proto STATUS

> Living status of Proto work — the **present**. Snapshot, not log.
> Store cuts of `apps/prototo-app` gate on `docs/RELEASE-CHECKLIST.md` (repro the reported flow + `scripts/sim-e2e.sh`).
> Four-doc model: **past** → `CHANGELOG.md` (designer-facing release notes; source for the website landing page) · **present** → this file · **future** → `BACKLOG.md` · **risks** → `RISKS.md`.
> For raw history → `git log`. For design → `docs/superpowers/specs/`.
> Last updated: 2026-09-20 (later) — **Share preview shipped (CLI 0.8.5): the share page, social card and library show the prototype's screenshot.** Earlier: **Team in Prototo Desktop shipped (1.0.16, CLI 0.8.4, account Personal/Team tabs).** Earlier: **Team space + remix built (#50): `/team` Prototypes tab deployed; proto-cli 0.8.3 with `proto remix` PUBLISHED, website deployed, verified end to end on ~/Prototo/instagram.** Earlier: **Teams MVP SHIPPED on prototo.app (#46): self-serve per-seat Team plan for Ziina; a seat resolves to Plus so proto needs no change.** Earlier (sensors): **expo-sensors built into Viewer 1.1.0 build 38 (uploaded to ASC, Sheri to submit) + sim `prototo-sim-sdk57-2` released; template + AGENTS.md carry it (held for the flip).** Earlier today: one Prototo backlog → GitHub Issues on `sherizan/prototo-shared` + project board 5. **One Prototo backlog: GitHub Issues on `sherizan/prototo-shared` + the Prototo project board (P0–P3).** Open items from proto/desktop/website backlogs → issues #7–#36; local BACKLOG files are pointer stubs; "Next up" below keeps only gated items. expo-sensors verified feasible → P1 issue #8, gated on the Viewer cut after 1.1.0. **Desktop 1.0.15 RELEASED (Check for Updates feedback + Restart now; the check had been working silently — see Done recently).** Earlier: **Three waiting-on-Apple fixes landed on `chore/sdk-57-upgrade` (unpublished): Xcode 27 DeviceHub fallback in `proto start`, point-and-edit overlay poll 1.5s → 0.5s (+ the backfill now keeps a managed overlay identical to the CLI's copy), and the Viewer's "Removed" tag on Recently viewed (rides 1.1.1).** Earlier: **Point-and-edit SHIPPED end to end: desktop 1.0.13 → 1.0.14 (RELEASED, smoke-tested) + proto-cli 0.8.1 (PUBLISHED, tarball verified).** Tap an AX element in the desktop preview → `[Leave early access button · screens/ProfileCard.tsx:77] ` lands inline at the agent prompt; the file:line comes from new `proto start` `/inspect` routes + the scaffold dev overlay (React `_debugStack` → Metro symbolicate), and `proto start` backfills the overlay into older projects. UX iterated twice on Sheri's feedback (pasted prefix → floating chip → inline tag). All three repos pushed (proto `chore/sdk-57-upgrade` = PR #60, prototo-shared `main`, desktop `main` + tag v1.0.14). **Still waiting on Apple for Viewer 1.1.0 (build 37) — the SDK 57 flip hour is the next big action (In progress).** — see the first Done-recently entry. Earlier that night: **Expo SDK 57 upgrade shipped through the App Store submission, runtime-aware shares, Desktop 1.0.11 → 1.0.12, and an evening field report closed (3 bugs, 3 causes).** Evening: Sheri on desktop 1.0.11 + CLI 0.8.0 hit (1) dead simulator taps → Xcode 27 broke serve-sim 0.1.44's input, **Desktop 1.0.12 (serve-sim 0.1.46) RELEASED, taps confirmed**; (2) a published share that wouldn't load → the website's post-publish GC deleted the fresh bundle on a stale manifest read, **age guard deployed, share healed on re-publish**; (3) "Preview on iPhone" QR does nothing → **her Wi-Fi isolates clients** (phone can't reach the Mac at all; hotspot works) — Viewer 1.1.1 (on the branch) now shows "Connecting to your Mac…" + a Wi-Fi/Local-Network hint instead of nothing. Earlier today: Today: template + CLI + dev-client on SDK 57 (4 pnpm patches re-targeted, runtime `prototo-57`); sim release `prototo-sim-sdk57-latest` LIVE (dormant until create-proto 0.8.0); **Viewer 1.1.0 build 37 SUBMITTED for App Store review**; **proto-cli 0.8.0 on npm** (runtime-aware: `proto upgrade` moves a project's SDK, `proto share` refuses stale projects, `proto start` nudges — all dormant while the website switch is `prototo-56`); **website deployed** (`/api/cli/version.runtime`, `shares.runtime_version`, stale-open email cron, "Needs update" captions; `/install/ios` now redirects to the App Store — EAS device channel RETIRED); **Desktop 1.0.11 RELEASED** (moves a project to the current runtime automatically on open). **Waiting on Apple. The flip hour on approval:** website `CURRENT_RUNTIME` → 57 + changelog block → push; publish create-proto 0.8.0. Unpushed: proto `chore/sdk-57-upgrade`, prototo-shared. Detail in Done recently (2026-09-18 entries); earlier history below them.

## Currently live

| Layer | Version | Source / install path |
|---|---|---|
| **Prototo App — App Store viewer** | **1.1.0 (build 38, SDK 57, runtime `prototo-57`, + `expo-sensors`) WAITING FOR REVIEW since 2026-09-19** (EAS production `a8edc001`, submission `f585e709`; ASC app `6772877748`). Build 37 was removed from review the same day so sensors ride the 1.1.0 cycle; build 38 submitted by Sheri with the What's New text (CHANGELOG Unreleased). Live before it: 1.0.x line on SDK 56 (1.0.5 build 36 was the last 56 submission, 2026-07-12). Release gate: `docs/RELEASE-CHECKLIST.md` (+ `scripts/sim-e2e.sh`, needs a Supabase session). | App Store (`https://apps.apple.com/app/id6772877748`; `prototo.app/install/ios` redirects there) |
| **`prototo.app` app backend** | `GET /api/shares` + `DELETE /api/account` live; **2026-09-18: runtime-aware shares live** — `GET /api/cli/version` carries `runtime` (`lib/runtime.ts` `CURRENT_RUNTIME`, **the migration switch, still `prototo-56`**), `POST /api/share` stores `runtimeVersion`, share reads + `/account` expose it, `POST /api/share/[token]/opened` takes `{reason:'stale_runtime'}`, hourly lifecycle cron emails owners of stale shares (once/share/day). Migration `20260918100000` applied. | `prototo-website` (Vercel) |
| Prototo App — device (EAS internal) | **RETIRED 2026-09-18** — App Store only. `prototo.app/install/ios` + `/install/ios/app` redirect to the listing. Last EAS internal builds: `20993e06` (SDK 56, 0.2.3) and `e7668d72` (SDK 57, 1.0.5, never pointed at). | — |
| Prototo App — simulator (SDK 57) | **1.1.0 / 0.2.5 / SDK 57 / runtime `prototo-57`, + `expo-sensors`** — EAS sim build `69ea05fe`, GitHub Release `prototo-sim-sdk57-2` + `prototo-sim-sdk57-latest` (2026-09-19; manifest `nativeModules` carries `expo-sensors`, sha `6c928a6e…`). Verified: installs + launches on a dedicated `Prototo-Test` sim, `NSMotionUsageDescription` present. Before it: `prototo-sim-sdk57-1` (1.0.5, 2026-09-18). **LIVE but dormant**: only expo-57 scaffolds fetch it (create-proto 0.8.0 held for the flip). E2E-verified: `proto start` on a 57 scaffold auto-installs it. | `prototo-sim-sdk57-latest` |
| Prototo App — simulator (SDK 56, shipped scaffolds) | **0.2.4 / SDK 56** (`prototo-sim-sdk56-5`, `prototo-sim-sdk56-latest`) — still what today's 56 scaffolds install; keep for 30 days after the flip (rollback rule). | `prototo-sim-sdk56-latest` |
| Prototo App — cloud/share (Appetize) | **RETIRED 2026-07-07** (Appetize dropped; shares open in the App Store Viewer). Old row kept for the API-key note: `~/.prototo/appetize-token`. | — |
| `create-proto` on npm | **0.7.11** (`latest`, 2026-07-14, SDK 56 scaffolds). **0.8.0 pre-bumped on `chore/sdk-57-upgrade` (SDK 57 template, proto-cli pin `^0.8.0`) — HELD: publish in the flip hour**, never before (a 57 scaffold's shares can't open on the 56 Viewer). | `npm create proto@latest myapp` |
| `@sherizan/proto-cli` on npm | **0.8.1** (`latest`, published 2026-09-19; adds the point-and-edit `/inspect` routes + dev-overlay backfill on `proto start`, tarball verified). 0.8.0 (2026-09-18) — runtime-aware: bundles labelled `prototo-<project expo major>` (fixes a mislabel bug), `proto upgrade` also moves the project SDK when `/api/cli/version.runtime` says it's behind (retries `expo install --fix` once for pnpm 11), `proto share` refuses stale projects, `proto start` nudges. Dormant while the switch is `prototo-56`. Existing scaffolds pin `^0.7.0` → they get it via `proto upgrade`/the desktop, not auto. | dep of every scaffolded project |
| **Prototo Desktop** | **1.0.15 RELEASED 2026-09-19** (Check for Updates answers the click + "Restart now" dialog; desktop BACKLOG #61) · 1.0.14 (point-and-edit phase 2) · 1.0.13 · 1.0.12 (serve-sim 0.1.46: taps on Xcode 27) · 1.0.11 (`sherizan/prototo-desktop-releases` v1.0.11; updater feed + `/api/download/macos` serve it). On project open, a project whose runtime is behind the website switch is moved automatically (`proto upgrade`, full-screen "Updating your prototype…", Try-again modal on failure); status-bar pill does the same on click. Released BEFORE the flip, as required. | `prototo.app/download` |
| `proto-components` (workspace) | 0.0.0, `private: true` | never published; source of truth for `components/proto/` synced into template via `scripts/sync-template.ts` |

## Done recently

- **2026-09-20 (later) — Share preview SHIPPED (`sherizan/prototo-shared#56`): proto-cli 0.8.5 published, website deployed, migration `20260921120000` applied.** `proto share` screenshots the booted Simulator (same `simctl` capture as `proto shot`), scales it to 600 px with `sips` (~170 KB), uploads `preview.png` with the bundle; `POST /api/share` carries `hasPreview` (reset by a publish without one). The share page shows it in a phone frame, the OG card puts it on the right, account/team rows use it as the avatar. No Simulator → text-only as before. Verified the capture on the real `google-pay` Simulator.
- **2026-09-20 — Team in Prototo Desktop SHIPPED: desktop 1.0.16 released, proto-cli 0.8.4 published, website account tabs deployed (`sherizan/prototo-shared#51`).** After Sheri's smoke test the in-app Team panel was dropped for one place: the desktop's **Team** button opens `prototo.app/account?tab=team`; **Publish asks "Team / Just me"** on a Team plan (`proto share --team|--private`, CLI 0.8.4; website `shares.visibility`, migration `20260921100000`, `PATCH /api/share/[token]`). `/account` now has **Personal** (your shares + recordings, Team/Just me pill) and **Team** tabs; `/team` redirects. Plan card says Team before a comp grant. Also in 1.0.16: titlebar regrouped (one primary), Claude Code session markers stripped from spawned envs (#53), "another Prototo window has the Simulator" + Retry relaunches the prototype (#54), Home keeps the session mounted so Codex's terminal survives (#55). Website `GET /api/team` (CLI-token) feeds the desktop's plan lookup.
- **2026-09-19 (evening) — Team space + remix BUILT (`sherizan/prototo-shared#50`).** Website: `/team` Prototypes tab is the shared space (deployed), `/account` personal again. **proto-cli 0.8.2 PUBLISHED 2026-09-20 (remix prints a false "couldn't finish" on projects with an un-allowlisted native build script, e.g. skia — the copy is fine; fixed in 0.8.3, PUBLISHED 2026-09-20), then 0.8.3:** `proto share` also uploads a `source.tgz` of the project (whole project minus node_modules/caches/git/builds/share token, 45 MB cap; `src/source-archive.ts`, real `tar`), `POST /api/share` carries `hasSource`; **new `proto remix <link> [folder]`** downloads a teammate's last published source into a new folder (own link on first share) — owner or same active team only. Website side (`sources` bucket + `shares.has_source` + `GET /api/share/<token>/source`) DEPLOYED 2026-09-20 (migration `20260921000000` applied by Sheri). **E2E verified on `~/Prototo/instagram`:** `proto share` → 1.6 MB `source.tgz` in the private bucket, `has_source` true; `proto remix <link>` → copy with identical screens, no `.proto/share.json`, deps installed. Gotcha: pnpm ≥ 9 exits 1 with `ERR_PNPM_IGNORED_BUILDS` (on **stdout**) when a native lib added by `proto add` isn't in `pnpm-workspace.yaml` `allowBuilds`; remix now treats that as success (issue filed for `proto add` to maintain the allowlist). Desktop one-click Remix = #51. Not scaffold+overlay: `npx create-proto` auto-runs `proto start` and create-proto 0.8.0 is held for the flip.
- **2026-09-19 (teams, later) — Team onboarding is its own experience: `/team/new` wizard (name → seats → Stripe → invite → done) + `/team` home; the `/account` card is gone (one row remains).** Email magic-link sign-in now resumes any destination via a one-shot `proto_next` cookie (was CLI-only), so a new designer from /pricing lands back in the wizard. Walked end to end locally in Stripe test mode with Chrome (Joined / Invited / inline error / Remove / member view / cookie resume). Website-internal; no contract shapes moved. Spec section "Onboarding flow" in the Teams spec.
- **2026-09-19 (teams) — Teams MVP SHIPPED: prototo-website `main` `1f13077` deployed, migration applied, Stripe live product `prod_VHtEHoqI3tcDXc` + per-seat prices + portal seat updates set, Vercel env set (`sherizan/prototo-shared#46`).** Ziina (3 designers) asked for a Teams account — the first inbound. Self-serve per-seat plan: $16/seat/mo, $160/seat/yr; `teams` + `team_invites` + `profiles.team_id`; Stripe Checkout with adjustable seats, Customer Portal for seat changes, webhook mirrors quantity; `/account` Team card (invite by email, remove, manage billing) + shared library; `/pricing` Team section replaces the lead form. **A seat resolves to Plus in `effectiveTier`, so proto-cli / desktop / Viewer are untouched** (`tier: "plus"` in every CLI-consumed response). Spec: `prototo-website/docs/superpowers/specs/2026-09-19-teams-mvp-design.md`. Remaining: a test-card run on the live site, then email Ziina's lead (sign in → Your account → Start a team → 3 seats → invite the other two). Future (idea issues on prototo-shared): remix a teammate's prototype, comments on shares.
- **2026-09-19 (03:56) — Desktop 1.0.15 RELEASED: "Check for Updates…" feedback (desktop BACKLOG #61).** Sheri's report: on 1.0.12 the menu check "does not check" while the feed served 1.0.14. Root cause (systematic-debugging, ShipIt log + updater cache): the check worked, silently — the manual path only had dialogs for "up to date" / "couldn't check", a found update cleared the flag with no UI, the zip downloaded, and `autoInstallOnAppQuit` installed it on the next quit (it had in fact installed 1.0.14 at 03:22; `defaults read` on the bundle plist returned a stale cached 1.0.12). Fix in desktop `electron/main.ts` `setupAutoUpdate`: manual check → "Downloading Prototo x…" dialog; every `update-downloaded` → "Restart now / Later" → `quitAndInstall`; launch check drops `checkForUpdatesAndNotify` (no duplicate OS notification). Cut: `bash scripts/package-signed.sh` (signed + notarized + stapled), draft created from the artifacts with `gh release create`, published by Sheri's go-ahead; feed rolled over. **The 1.0.14 → 1.0.15 hop was necessarily silent** (1.0.14 runs the old handlers): click → nothing visible → zip in pending → ShipIt install on quit 03:56:33 → 1.0.15. The new dialogs are first observable on the NEXT cut — verify then. Quirk logged: a second same-bundle-id copy in the repo's `release/mac-arm64/` is what the updater relaunched into at 03:22; delete `release/` after smoke-testing. Desktop `main` tagged `v1.0.15` (3 commits ahead of origin, NOT pushed).
- **2026-09-19 (later) — Three small fixes while Viewer 1.1.0 waits on Apple (branch `chore/sdk-57-upgrade`, NOT published).** (1) **Xcode 27:** `ensureSimulatorBooted` now falls back from `open -a Simulator` to `open devices://device/open?id=<udid>` (DeviceHub's URL scheme, the same fallback `@expo/cli` ≥ 57.0.25 uses), so `proto start` brings the booted device forward again; test added. (2) **Point-and-edit poll** `POLL_MS` 1500 → 500 in `proto-components/src/touch-dots.tsx` (template + `touch-dots-source.ts` regenerated via `sync-template`), and `ensure-touch-dots.ts` now refreshes any `// Proto-managed.` overlay that differs from the CLI's copy (the old `/inspect/result` sentinel was a one-shot migration and would have pinned existing projects at 1.5s forever). Reaches designers with the next proto-cli publish (0.8.2). (3) **Viewer "Removed" tag:** `lib/open-history.ts` gains `removedAt` + `markRemoved(token)`; the share screen writes it when a tap resolves a literal 404; the home Recently-viewed card renders a muted "Removed" badge (wins over "Yours") on a dimmed, still-tappable card whose tap lands on the existing not-found explainer. A later successful open clears it. Rides Viewer 1.1.1. All tests green (proto-cli 386, prototo-app 90, create-proto 63).
- **2026-09-18 (late night) — Point-and-edit phase 2: inline prompt tag + precise file:line (proto-cli 0.8.1 PUBLISHED 2026-09-19; create-proto 0.8.0 template; desktop `c68bb00` = 1.0.14 draft).** Phase 1 (desktop 1.0.13, published) pasted `Edit the "<label>" <role>: ` into the terminal on an AX-overlay tap; Sheri asked for a label instead of text, chose **attach on Enter**, then rejected the resulting floating chip ("should be inline with the prompt"). **Final UX (desktop `main`, second commit):** the tap pastes an inline tag at the prompt — `[Leave early access button · screens/Home.tsx:42] ` — and the designer keeps typing; the label lands instantly and the file:line is spliced in a beat later (two backspaces + paste) only while nothing has been typed since. The prompt line belongs to the agent TUI, so "inline" has to be real text. **The file:line pipeline** (new prompt-server routes, contract in prototo-shared "Prompt server — :3001"): desktop → `POST 127.0.0.1:3001/inspect {x,y}` (screen fractions from the overlay button's geometry) → the scaffold's Proto-managed `components/proto/touch-dots.tsx` picks it up on its existing `GET /recording` poll (additive `inspect` field), runs the renderer's `getInspectorDataForViewAtPoint` through the React DevTools hook (NOT the deprecated `react-native/src/private/...` deep import — that one raised a yellow-box WARN on the device), walks `closestInstance._debugOwner` collecting React 19's dev-only `_debugStack` (the JSX call site), `POST /inspect/result {id, stacks}`; the CLI parses the Hermes frames, symbolicates via Metro `POST :8081/symbolicate`, picks the first frame under the project excluding `node_modules/` + `components/proto/`, answers `{file, line}` (204 on timeout ~4s / nothing). **Backfill:** `proto start` ≥ 0.8.1 `ensureTouchDots` adds the overlay to pre-0.7.10 projects and wraps a single-`return (` `app/_layout.tsx` in `<TouchDots>` (skips anything ambiguous; refreshes only a `// Proto-managed.` overlay lacking the hook). `TOUCH_DOTS_SOURCE` is GENERATED into proto-cli by create-proto's `sync-template.ts` (test pins it to `proto-components/src/touch-dots.tsx`). **Verified live** on `~/Prototo/instagram` with the local CLI build: the heal wrote the overlay + wrapped its customised layout (GestureHandlerRootView), and `POST /inspect` at (0.5,0.5)/(0.5,0.85) returned `screens/ProfileCard.tsx:77` / `:326` — the project is left healed (it's what its next desktop open would do). Tests: prompt-server 11, ensure-touch-dots 5, create-proto 63, CLI suite 385 green. **To ship:** Sheri publishes proto-cli 0.8.1 (`pnpm publish --no-git-checks --otp=…` in `packages/proto-cli`; safe before the flip — no runtime changes), then smoke-tests desktop 1.0.14 draft (the desktop auto-updates the project CLI on open → `/inspect` live) and publishes it. Without 0.8.1 the chip is label-only.
- **2026-09-18 (evening) — Field report on desktop 1.0.11 + CLI 0.8.0: three bugs, three unrelated causes, all closed.** Project `~/Prototo/instagram` was cleanly SDK 56 throughout (only the CLI moved). **(1) Simulator taps dead** → Xcode 27 replaced Simulator.app with Device Hub and `serve-sim` 0.1.44's HID input broke; upstream fix (2026-08-21) is in 0.1.46 → **Desktop 1.0.12 built, notarized, published** (`prototo-desktop` #58, tag v1.0.12; updater + `/api/download` serve it); Sheri confirmed taps. **(2) Published share "Couldn't load"** → manifest fine, launch bundle object absent from storage (`NoSuchKey`), 25 images present, older share fine: the website's post-publish GC re-read `manifest.json` from storage and, on a stale read (storage CDN) or overlapping publish, deleted the NEXT publish's bundle; repeated publish/open cycles left zero bundles → **GC now never deletes objects < 15 min old** (`lib/supabase/bundle-store.ts`, 243 tests, deployed); instagram healed on re-publish (v7, bundle 200). RISKS `SH-11`; BACKLOG: carry the keep-set in `POST /api/share`. Oddity noted: Sheri's FIRST re-publish left no server trace. **(3) Preview-on-iPhone QR "not detected"** → proven a network problem, not the scanner: a public-host connect QR shows the on-screen rejection (scanner + parser fine), a valid one is handed to the native Metro connect which 1.0.5 fails SILENTLY (plus a scanner latch that then ignores further scans until tab refocus); Mac side fully verified (Metro on `*:8081`, bundle served over LAN, firewall off, Local Network permission on) yet no phone connection ever reached Metro across four port watches, the Mac can't ping any Wi-Fi peer, and the iPhone's Safari gets nothing from `http://192.168.5.195:8081/status` → **Wi-Fi client isolation**; **Personal Hotspot with the Mac joined works end to end (verified)**. Viewer fix on `chore/sdk-57-upgrade` for 1.1.1: `/open` route (`+native-intent` rewrites `expo-development-client` links; scanner pushes it) shows "Connecting to your Mac…", then "Couldn't reach your Mac's preview… same Wi-Fi… Local Network in Settings" with Try again + Open Settings; AppDelegate hands connect links to the JS shell (parks them during cold start; native swap kept while a prototype is mounted) — sim-verified warm, cold, unreachable. 88 app tests. Follow-ups in BACKLOG: tunnel mode for Preview (needs a Viewer allow-list), docs note "same Wi-Fi and the network must let devices see each other".

- **2026-09-18 (late) — Runtime-aware shares (proto `chore/sdk-57-upgrade` committed; **proto-cli 0.8.0 PUBLISHED 2026-09-18** (create-proto 0.8.0 still held for the flip); **website DEPLOYED 2026-09-18** — migration `20260918100000` applied via `supabase db push --linked` (Sheri ran it), `main` `c932da3` pushed, Vercel live: `/api/cli/version` carries `runtime: {prototo-56, 56}`, `/api/share/CJCGN93R04XQ` returns the backfilled `runtimeVersion` with `version` still 2, `/account` 307s to sign-in; prototo-shared `1689408` committed).** Sheri: old shares failing on the new Viewer is a bad experience — can we open them? **No, not on the 57 binary:** `react-native-worklets` `checkCppVersion` throws on any JS↔native major.minor mismatch at import (verified in both 0.8.3 and 0.10.1 source; the 57-JS-on-56-binary test red-boxed the same way), and every scaffold imports reanimated via `components/proto`. No cloud fallback (Appetize dropped) and the backend can't rebuild bundles. So the fix makes re-publishing near-automatic. Three real bugs surfaced and fixed on the way: (1) the manifest runtime was a CLI CONSTANT — proto-cli 0.8.0 on a 56 project would have stamped `prototo-57` on a 56 bundle, passed the gate and crashed; now `shareRuntimeVersion(root)` derives `prototo-<installed expo major>`; (2) `proto upgrade` only bumped the CLI — a 56 project could never produce a 57 bundle (AGENTS.md literally said "re-scaffold"); now it also runs `npx expo install expo@~<major>.0.0` + `--fix` (retried once: pnpm 11 exits 1 the first time it records a blocked build script) when `/api/cli/version`'s new additive `runtime` says the project is behind, then re-writes the share config; (3) the website's `shares` version-bump trigger fired on EVERY update, so the first-open email mark was already counting as a re-publish ("Updated · v{n}") — email marks now go through RPCs that set a local flag the trigger honours. **Also:** `proto start` prints "Prototo has a new runtime. Run proto upgrade…" when behind; `proto share` REFUSES a stale project ("…Run proto upgrade first, then proto share", fail-open when the runtime is unknown); `POST /api/share` carries `runtimeVersion`; the Viewer gates from `/api/share/[token].runtimeVersion` (one round-trip; manifest scrape fallback), pings `/opened` with `{reason:'stale_runtime'}` on the older gate ("We've let {designer} know it needs a quick update"), shows "Update Prototo" → App Store on the NEWER case, and captions its own list "Needs update"; the website stores `shares.runtime_version` (backfilled `prototo-56`, trigger disabled), `share_opens.reason`, `shares.stale_emailed_at`, and the hourly lifecycle cron emails the owner once/share/day (`emails/share-stale.tsx`: the two commands); `/account` appends "Needs update". `lib/runtime.ts` `CURRENT_RUNTIME` = **the migration switch (still `prototo-56`)**. **Verified:** proto-cli 375 (+13), app 86 (+4), website 241 (+8), all tsc clean; **real e2e** on a fresh SDK 56 scaffold against a mock `/api/cli/version` at runtime 57: share refused → `proto upgrade` → expo 57.0.24 / worklets 0.10.1 / share config `prototo-57` → share passes the gate. **Not yet verified (Sheri):** the Viewer stale screen + email on a real `prototo-56` share (needs a signed-in session on the 57 build + the website deployed with the migration). CONTRACTS.md has a new "Runtime version" section + changelog entry (prototo-shared, uncommitted there). **Ship sequence (revised):** ✅ website deployed (switch still 56) · ✅ proto-cli 0.8.0 published · ✅ **desktop 1.0.11 RELEASED 2026-09-18** (v1.0.11 public on `sherizan/prototo-desktop-releases`, source pushed + tagged; `prototo-desktop` `#57`, Sheri's call = seamless: on project open a project whose runtime is behind is moved AUTOMATICALLY — full-screen "Updating your prototype to the latest Prototo… about a minute" (`ProtoState 'updating'`) before Metro starts; failure → session still starts (sim preview works on the old runtime) + a "Your prototype needs an update" modal with Try again; the status-bar pill also runs `proto upgrade` and appears when the project runtime is behind. released before the flip, as required)** → Viewer 1.1 approved → same hour: flip `CURRENT_RUNTIME` to 57, publish create-proto 0.8.0, add the changelog block to `lib/releases.ts`. (The `/install/ios` EAS pointer is RETIRED 2026-09-18 — the URL now redirects to the App Store listing; device build `e7668d72` is unused.)
- **2026-09-18 — Expo SDK 56 → 57 upgrade (branch `chore/sdk-57-upgrade`, Phases 0–2 of `docs/expo-sdk-upgrade-sop.md` DONE; stopped at the Phase 2 gate).** Why now: SDK 56 ships a Hermes V1 memory regression for reanimated/worklets apps (every scaffold + the Viewer); Expo says go straight to `expo@57.0.17+`. SDK 57 = RN 0.86.3, React unchanged, no breaking changes claimed; expo-router/@expo/ui 57.x additive (Toggle/Slider controlled-prop fixes ride along). Decisions (Sheri): target 57 now (SDK 58 — beta 2026-09-15, mandatory scene lifecycle + Node 22 floor — is a separate later upgrade, must land before Apple's **April 2027** iOS-27-SDK deadline, new risk `DC-15`); bump `runtimeVersion` → `prototo-57` in Phase 3 (existing shares will need a re-publish). **Done this session:** Phase 0 spec written; `packages/create-proto/template/package.json` on the `expo install --fix` pins (expo ~57.0.0, RN 0.86.3, reanimated 4.5.1, worklets 0.10.1, gesture-handler ~2.32.0, screens 4.26.2, all expo-* 57.x, babel-preset-expo ~57.0.0); `proto-components` devDeps refreshed to 57 (were stale at RN 0.83.6) — its `tsc` is now CLEAN (the 3 long-standing Card/Toggle type errors in Known issues are gone); README/create-proto README/root CLAUDE.md/proto-master/SOP header say 57; `RISKS.md` +`DC-15`, `DC-13`+`CS-03` annotated. **Verified:** create-proto 63 + proto-cli 362 green, `expo-doctor` 20/21 on a 57 scaffold (the 1 failure is the pre-existing `proto` script-name/bin collision, not the bump), Metro bundles clean (1846 modules, no peer/Node/react-navigation warnings). **Smoke test vs the sdk56 sim binary (SOP 2.7): red-box, as the plan predicted** — `[Worklets] Mismatch between C++ code version and JavaScript code version (0.8.3 vs. 0.10.1)` → reanimated init throws at `components/proto/gestures/index.ts:1`, expo-router `<Stack>` cascades. Native mismatch, not a JS bug — the JS layer can only be fully validated on the 57 binary. **Phase 3 LOCAL — DONE later the same day (same branch):** `apps/prototo-app` on `expo ~57.0.23` via `expo install --fix` (gotcha: `--fix` aligns to the *installed* expo, so `pnpm install` must run after bumping the pin or it "fixes" you back to 56.x); `@expo/cli`/`@expo/config-plugins`/`babel-preset-expo` on 57. **All 4 pnpm patches re-created** on the 57 versions — `expo-modules-core@57.0.18` (DC-07 `@synchronized`), `expo-dev-launcher@57.0.20` (`debugOnly` + `EXDevLauncherLoadProgress`), `expo-dev-menu@57.0.18` (`debugOnly`), `expo-updates@57.0.22` (Release dev-launcher path + `USE_DEV_CLIENT` + build-from-source) — every hunk applied with `patch -p1`, zero rejects; Podfile.lock resolves all four from `patch_hash` store paths. `runtimeVersion` → **`prototo-57`** (TDD: `publish-update.test.ts` + `share-lookup.test.ts` flipped first → `share-config.ts` + `app.json`); proto-cli 362, app 82, app `tsc` clean. `expo prebuild -p ios --clean`: native-host plugin applied (AppDelegate + shim + `-ObjC`), no `UIApplicationSceneManifest` (correct — no scene opt-in on 57). **Local Release sim build (`expo run:ios --configuration Release`, Prototo-Test on iOS 26.5, Xcode 27.0): PASS** — cold launch lands in OUR shell (no Expo launcher; embedded `Expo.plist` runtime `prototo-57`); deep-linking the 57 scratch scaffold from Metro renders Home with the floating Viewer menu, Metro clean (no worklets mismatch, 0 errors); **DC-07 race loop 25 cold starts × 5 rapid `simctl openurl`: 0 crash reports, 26 bundle loads, app alive.** `check-contracts.sh`: hard couplings OK. **Not yet verified (needs a signed-in session / real share):** `sim-e2e.sh` (`--session` JSON), the download-% bar on a share open (proves the dev-launcher notification patch end-to-end), Slider/Stepper/BottomSheet eyeballing on device — fold into Sheri's device pass. **Phase 3 OUTWARD — DONE (Sheri's go, same day):** `eas build` `development-simulator` → **`9a4d7994`** and `development` (internal device) → **`e7668d72`**, both 1.0.5 / runtime `prototo-57`, remote credentials resolved without an Apple login. `pnpm release:simulator` → **`prototo-sim-sdk57-1` + `prototo-sim-sdk57-latest`** (sha `4a1c13ca…`, 48 native modules in the manifest). Two transient hiccups, no code change: the first EAS artifact download got a connection reset at 28/79 MB (re-ran), and the `-latest` create got an HTTP 400 on asset upload after `-1` had succeeded (re-created `-latest` by hand with the same tarball + manifest the script left in its temp dir — script is fine, GitHub upload flake). **E2E verified:** fresh `proto start` from the 57 scratch scaffold on a sim holding the sdk56 1.0.1 app → "Setting up Prototo on the Simulator…" → cached `~/.prototo/cache/57-4a1c13cac687` → installed 1.0.5 → Metro bundled → Home rendered, 0 errors. **Website install pointer NOT flipped on purpose** (`/install/ios/app` still → `20993e06`, the 56 device build): a 57 dev client on today's 56 scaffolds red-boxes on the worklets native mismatch — flip it together with the npm 0.8.0 publish (Phase 5). CONTRACTS.md changelog entry written (prototo-shared, uncommitted there). **Phase 3 leftover for the SOP (2026-09-18 head):** the old `prototo-sim-sdk56-*` releases + EAS builds stay for 30 days (rollback rule). **Phase 4 — production build DONE 2026-09-18:** `app.json` → **1.1.0** (commit `9c38461`), `eas build --profile production` → **`83133954`, build 37**, runtime `prototo-57`, App Store Connect API key credentials resolved non-interactively. **Uploaded to TestFlight** (`eas submit --id 83133954…`, EAS submission `16c9c2c4`, ASC app `6772877748`), then **SUBMITTED FOR APP STORE REVIEW by Sheri 2026-09-18 — waiting for review.** `sim-e2e.sh` (needs a Supabase session JSON) was not run; ASC "What's new" = the CHANGELOG Unreleased entry (incl. the "re-publish older shares" line). **Phase 5 prep DONE:** create-proto **0.8.0** + proto-cli **0.8.0** pre-bumped (template pin `^0.8.0`; 63 + 362 green, NOT published), `docs/CHANGELOG.md` Unreleased entry, `docs/expo-sdk-upgrade-sop.md` header → 57 + "SDK 58 prep" + 56→57 lessons. **Phase 5 remaining (Sheri's go, AFTER 1.1.0 is live):** `pnpm publish` both packages (never `npm publish`), website: `/install/ios/app` → `e7668d72`, `/api/cli/version` highlights + `lib/releases.ts` from the CHANGELOG entry, Currently-live table + `docs/native-bundle.md` flip, `git tag sdk-57`, commit CONTRACTS.md in prototo-shared. **Phase 4 (original notes):** bump `apps/prototo-app` + `expo install --fix`; re-create the 4 pnpm patches on the 57.x versions (all verified still required upstream on `sdk-57`); `prototo-56` → `prototo-57` in `proto-cli/src/share-config.ts` + `app.json` (+5 tests); local Release sim build on the **iOS 26.5** runtime (local Xcode is 27.0) + `sim-e2e.sh` + DC-07 race loop + download-% bar check; `pnpm release:all` → `prototo-sim-sdk57-latest`. **Ship order (hard):** Viewer 1.1 on the App Store → sim release → create-proto 0.8.0 + proto-cli 0.8.0 (the CLI derives the sim tag from the scaffold's expo major, so a 57 scaffold 404s until the release exists). `docs/native-bundle.md` + the Currently-live table stay at 56 until Phase 3 ships.

- **2026-07-13 — Codex support in scaffolds (branch `codex-support`, create-proto 0.7.11 pending publish).** Companion to Prototo Desktop's agent picker (desktop BACKLOG #51 lets designers choose Claude Code or OpenAI Codex): the scaffold now serves both agents. `template/CLAUDE.md` → **`template/AGENTS.md`** (canonical, title now "Agent Instructions"; content otherwise unchanged) and CLAUDE.md becomes a one-line `@AGENTS.md` import — Claude Code expands imports, Codex reads AGENTS.md natively (git-root walk, 32 KiB budget; ours is 14.6 kB), so one source, no drift. NEW `template/.codex/config.toml` — the Codex analog of `.mcp.json` — registers `[mcp_servers.prototo]` (`npx proto-mcp`); Codex honors project-scoped config once the designer trusts the project (its standard first-open prompt), and `mcp_servers` is NOT on Codex's project-config denylist (verified against openai/codex source). Deliberately NOT global `~/.codex/config.toml` registration: that would spawn the prototo MCP server in every Codex session machine-wide. TDD: +3 template-integrity tests (AGENTS.md canonical + under Codex's 32 KiB doc budget, CLAUDE.md stays a thin pointer, .codex/config.toml shape) — create-proto **63** green; `npm pack --dry-run` confirms `.codex/config.toml` + `AGENTS.md` survive the tarball (the `.gitignore`-strip trap doesn't apply). proto-cli untouched except a comment (cli.ts `proto shot` fallback note → AGENTS.md). Ride-along fix (same branch): the template's share copy was still Appetize-era ("open in a browser… streamed from a cloud iPhone", stale since the 2026-07-07 Viewer switch) — AGENTS.md's "Sharing your prototype" now describes the real flow (permanent `/p/<token>` link → free Prototo app on iPhone → native run; limits = current-runtime republish prompt + bundled-native-modules per CS-03), and template README's share line matches. Also fixed: template README claimed `proto add "describe a screen"` generates a screen (`proto add` installs packages; screen generation isn't a designer-run command) — the line now tells designers to describe the screen to their coding agent. **Second pass (same branch, proto-cli 0.7.16):** (1) `proto start` now BACKFILLS `AGENTS.md` (copied from the project's own CLAUDE.md, retitled) + `.codex/config.toml` into pre-0.7.11 projects (`ensure-agent-files.ts`, 6 tests; never overwrites, skips pointer-only CLAUDE.md, fail-open) — without it, every EXISTING project opened under Codex after the desktop update would run with no instructions and no MCP tools; the desktop auto-updates proto-cli on project open, so existing users heal automatically. (2) AGENTS.md now tells the agent what missing prototo tools under Codex means (untrusted project → ask the designer to relaunch and trust; `proto shot` meanwhile). (3) create-proto's terminal-flow copy is agent-neutral (`cd X && claude (or codex)` in nextSteps/howToRestart/Home.tsx). proto-cli **362** green, create-proto **63** green, Biome clean. **Publish order: create-proto 0.7.11 to npm BEFORE the desktop agent-picker release.**
- **2026-07-06 — Viewer share-open: old-runtime gate + escapable loading screen.** From Sheri's device screenshots (pre-f95d0a9 build): both the loading and error states were plain top-left text with no way back. The error state was already rebranded on Friday; this adds the rest. **Loading:** centered logo + "Opening prototype…" (replaces the "Protomaxxing.." placeholder copy) + a ghost **Cancel** → home; both Loading render sites covered. **Old-version detection:** before the native handoff, the viewer GETs the share's self-hosted manifest and regex-reads its `runtimeVersion` (website serializes the manifest JSON verbatim in the multipart body — documented coupling), compares against `Updates.runtimeVersion`; mismatch → "made with an older version of Prototo. Ask <designer> to publish it again." with ONLY "My prototypes" (no Try again / Watch in browser — the runtime gate applies to Appetize streaming too). Fail-open: legacy u.expo.dev links, network errors, or a missing field skip the check and load as before. New `manifestUrlFromDeepLink` + `fetchManifestRuntimeVersion` in `lib/share-lookup.ts`, TDD (+4 tests, viewer 62 green).
- **2026-07-06 — Viewer brand pass: sign-in + Prototypes + Account UI (uncommitted → this commit).** Brand accent `#E86A9C` (from the logo lottie) now set once on the root `ProtoConfigProvider` — primary buttons, ghost text, toggles and the NativeTabs active tint (`tintColor` fed from `useAccent()`) all render brand pink instead of iOS blue. **Sign-in:** paperplane SF Symbol on the email button; Verify matches the other buttons (52pt / 19pt). **Prototypes:** "Hi {name}" → centered 36pt logo; segmented control sized up (40pt, 15pt labels); `EmptyHint` deleted in favour of a new `InfoCard` (Card + `info.circle`) used for loading/empty states; empty copy → "Hit Publish in Desktop app to view your prototypes here."; Open/Open-a-link buttons → primary; Open-a-link card moved above the opened list; Connect-to-your-Mac card paused behind `SHOW_CONNECT_CARD = false` (kept intact per the pausing convention); Profile tab relabelled **Account**. **Account page:** header row shows "Upgrade to Plus" (→ prototo.app/pricing) when free or a "Plus" pill when upgraded — nothing while the tier loads; user-info card (name + email) with Sign out / Delete account; app version footer from `Constants.expoConfig.version`. Tier comes from a new `lib/use-tier.ts` reading the user's own `profiles.tier` via the existing Supabase client — the website's "own profile read" RLS policy already allows it, **no backend change**. Also root-caused the "Apple button not rendering" report: the installed sim binary predated the `expo-apple-authentication` native module (the component silently returns `null` without it); resolved by rebuilding — no code change needed. tsc clean.
- **2026-07-06 — PERF-REPORT fixes shipped (botim session post-mortem: ~2x tool-call overhead from tooling gaps).** All 6 ranked findings addressed: (1) `compile_check` now returns the raw tsc diagnostics (file:line + TS code) under the friendly summary — the agent was re-running tsc manually every type error; (2) new **`reload_app` MCP tool** (4th tool; terminate → launch → settle → connect link, the DC-07-safe split; `ui=bare` only under PROTO_HEADLESS_SIM=1) — root-layout changes don't Fast-Refresh and were costing ~6 calls of manual restart archaeology; (3) designagent-figma `get_spec` diet (user's repo, 0.17.3): minified JSON (62% cut — fits the tool-result limit), hidden subtrees stripped, vector leaves collapsed, per-node css dropped, enrich walk id-matched; (4) template CLAUDE.md: `npx proto add`, exact NativeTabs nested sample for the pinned expo-router, cold-restart rule → reload_app, 13-primitive props cheat sheet (stops per-session source re-reads); (5) SVG/asset export → designagent TODO; (6) **Stack gains `align`** (Row parity; no default → unset keeps RN stretch so existing prototypes don't reflow) + sync-template. Live botim project patched directly (CLAUDE.md + Stack). proto-cli **349** green (+6), 4-tool MCP handshake verified; create-proto **60** green. Versions pre-bumped: proto-cli **0.7.10**, create-proto **0.7.6** — publish on Sheri's go (botim then needs `npm update @sherizan/proto-cli` for reload_app).
- **2026-07-06 — dev-client 0.2.4 CUT (sim + Appetize; device deferred): bare prototype UI live.** EAS sim build `aec3dd16` → `prototo-sim-sdk56-5` + `-latest` pointer (proto starts auto-upgrade via SHA) → Appetize upload (versionCode 4, same publicKey). Producers flipped ON the same day: desktop `protoProcess.ts` connect link + website `lib/appetize.ts` buildEmbedUrl now send `ui=bare` → the floating Viewer menu no longer shows in Prototo Desktop's streamed sim or the `/p/<token>` Appetize embeds; Viewer app flows unchanged (they never send the param). Website 189 tests green; CONTRACTS.md deep-link contract + changelog updated. **Device IPA still 0.2.3** — cut it with the next batch (needs the interactive Apple login); ui=bare is sim/Appetize-only so no user impact.
- **2026-07-06 — bare prototype UI for Desktop/Appetize (native `ui=bare`, pending dev-client cut).** The floating Viewer menu (Refresh / Exit to home) showed in Prototo Desktop's streamed sim and would in Appetize embeds — wrong chrome for pure-runtime hosts (no "Home" exists there); it belongs only to the Viewer app. Fix in `plugins/withPrototoNativeHost/native/`: `appURLFromString` rewritten to a proper `NSURLComponents` queryItems parse (inner URL from the `url` item — the old `?url=` prefix-strip would swallow any extra param into the inner URL and break loading), optional `ui=bare` sets a static `bareUI` flag re-derived on EVERY load; `AppDelegate.onPrototypeLoaded` hides the overlay when bare. Viewer flows never send the param → menu unchanged. **Verified:** Release sim build clean; on-sim parse check — old-style link and `&ui=bare` both extract `http://localhost:8081` identically (NSLog `loadApp START`). **Pending:** dev-client cut (0.2.4 line) via `release:all`; ONLY AFTER it's live may the producers send the param (desktop `protoProcess.ts` launch URL + website `lib/appetize.ts` launchUrl — NOT `bundle.ts`'s stored deepLink, which the Viewer share-open shares). Order matters: on 0.2.3 the param breaks loading.
- **2026-07-06 — share-viewer red-screen FIXED: published manifests now carry the expo config (proto-cli 0.7.8, publish pending npm login).** Prototo Desktop e2e caught every shared prototype crashing in the viewer with expo-linking's "needs access to the expo-constants manifest" — the self-hosted `buildBundle` shipped `extra: {}` so `Constants.expoConfig` was null at runtime (local dev unaffected: Metro injects the config). Fix: `publish-update.ts` reads the project's resolved expo config (`readShareExpoConfig`: managed `.proto/expo-config/app.json`, `app.json` fallback — the scaffold `app.config.js` is a passthrough require) and embeds it as `manifest.extra.expoClient` + `extra.expoConfig`; website `/api/manifest` passes `extra` through untouched, so no website change. Ride-along: cli-login loopback pages are app-neutral ("Head back to Prototo" — was "your terminal", wrong from the desktop). proto-cli **342** green (+5), tsc clean, version → **0.7.8**, commit `ac2b920`, CONTRACTS.md changelog updated. **Pending:** `npm login` expired — publish 0.7.8, then re-publish a share to verify the /p/<token> viewer renders. (Existing shares stay broken until re-published.)
- **2026-07-05 — DC-07 dev-client launch crash: root cause found + fixed (branch `fix/dev-client-runtime-race`, pending dev-client release).** Sheri's diagnosis confirmed from the two fresh `.ips` reports: two `com.facebook.react.runtime.JavaScript` threads concurrently inside `AppContext.registerNativeModules`, crashing in `-[EXPermissionsService registerRequesters:]` (shared singleton, zero synchronization — SIGSEGV via ImagePicker 20:21, SIGABRT via ExpoLocation 16:15; module-agnostic). Trigger: a `prototo://expo-development-client/?url=` deep link during cold start is intercepted **natively** by expo-dev-launcher's app-delegate subscriber → `loadApp` → runtime #2 races runtime #1's registration, bypassing `ProtoNativeLoader`'s in-flight guard. Upstream: expo `main` still unsynchronized, changelog empty on it — no version bump fixes it. **Fix:** (1) **4th pnpm patch** `expo-modules-core@56.0.16` — `@synchronized` on `registerRequesters:` + both requester lookups; (2) **native-host serialization** — AppDelegate now funnels `expo-development-client` URLs through the guarded loader, and loads arriving during a runtime transition (cold start / every mount) park in a last-wins pending slot, released by the shell's new `shellReady` ping (`app/_layout.tsx` → `lib/native-runtime.ts` → `PrototoRuntime.shellReady`) or a 3s native valve — incidentally fixes dropped rapid QR scans. **Verified:** app 58 JS tests green, no new tsc errors, Release sim build clean; scripted race loop (25 cold starts × 5 rapid `simctl openurl` each, Metro live in `playground/test6`) → **0 crashes**, deferred links drain and load. `docs/RISKS.md` DC-07 updated (root-caused). Desktop's own mitigation (`4ee94b3`, splits cold start from connect link) stays as belt-and-braces. **Pending:** Sheri's go for the dev-client cut — `apps/prototo-app` bumped to **1.0.1**, `pnpm release:all` + install-pointer updates; the fix rides into any future App Store build automatically (build 23 in review is untouched).
- **2026-07-05 — `get_metro_errors` MCP tool BUILT (branch `feat/mcp-get-metro-errors`, TDD, pending live e2e + publish).** The 3rd feedback-loop tool: Claude self-serves Metro's current error state (build failures + runtime red-box/ERROR lines, per Sheri's call) instead of the designer pasting errors. File-based handoff per the backlog design — `proto start` now tees Metro output (pipe + byte-identical passthrough + `FORCE_COLOR=1`; per-stream line splitters) into a rolling 5-entry scanner (`src/metro-errors.ts`: starts on `Bundling failed`/`Unable to resolve`/`SyntaxError`/`ERROR`, clears on a successful `Bundled`, dedups re-prints, persists `.proto/metro-errors.json`, reset at startup); the stdio MCP server gained `get_metro_errors` (`mcp/metro-errors-tool.ts` — clean-state message via new `messages.metroClean`, else JSON `{friendly, raw, at, screen?}` through `translateMetroError`). **Ride-along root-cause fix:** `translateMetroError` missed modern Expo's `Unable to resolve "x" from "y"` wording (only matched `…resolve module`) — broadened; desktop's vendored `errorTranslation.ts` re-synced + CONTRACTS.md changelog entry (ritual followed; `check-contracts.sh` OK). Template CLAUDE.md fix-session guidance + `.gitignore` (`metro-errors.json` + the previously-missing `tsconfig.mcp.json`). **Verified:** proto-cli **337** green (was 311), create-proto **59**, tsc + Biome clean, real MCP stdio handshake lists all 3 tools with correct clean/populated states. Versions pre-bumped (proto-cli **0.7.7**, create-proto **0.7.4**), NOT published. Spec: `docs/superpowers/specs/2026-07-05-get-metro-errors-design.md`. **Pending:** live `proto start` tee-fidelity eyeball (kills port 8081 + boots sim — needs a safe window), then publish both on Sheri's go.
- **2026-07-05 — self-hosted sharing: `proto share` needs only a prototo account (no EAS, no `EXPO_TOKEN`) — implemented across two repos + on-device spike, PENDING deploy/merge.** Root fix for the share publish-auth problem: stop publishing via EAS Update to a central Expo project (which needs Expo creds designers don't have) and instead **self-host the bundle**. The dev client loads whatever manifest URL the deep link points at — validated on-device (`simctl openurl` of a hand-served `localhost` manifest → dev client fetched it + downloaded the bundle + all 23 assets + executed JS; unsigned, no re-cut; the only red-box was a stale-build native-module mismatch, orthogonal). New flow: `proto share` → `expo export` → precompute a conformant expo-updates manifest (base64url-sha256 hashes + md5 keys, `runtimeVersion prototo-56`) → `POST prototo.app/api/publish` (prototo Bearer → per-file signed Storage URLs) → deep link `prototo://…/?url=https://prototo.app/api/manifest/<token>`.
  - **`prototo-website` branch `feat/self-hosted-publish`** (tsc + 179 tests): `bundles` public bucket; `POST /api/publish` (auth, ownership guard, path-pinned signed uploads); `GET /api/manifest/<token>` (conformant multipart, 404s on missing/expired share); `DEEP_LINK_RE` accepts prototo.app (keeps u.expo.dev); `cleanupExpiredBundles` + hourly `/api/bundles/cleanup` cron.
  - **`proto` branch `feat/self-hosted-publish-cli`** (build + 313 CLI tests, +9 viewer): `publish-update.ts` rewritten (export→manifest→upload, friendly error mapping, no raw leak); `share.ts` new signature; the viewer's `isValidShareDeepLink` accepts prototo.app manifests.
  - **Resolves the A-vs-B publish-auth question entirely** (no Expo credential to hand out). **Pending:** deploy `prototo-website` (run the `bundles` migration; cron uses existing `CRON_SECRET`), merge both branches, then a real `proto share` e2e on a fresh prototo account. Caveat: same runtime/native-module gating as before (`CS-03`) — a prototype using a module the shipped dev client lacks still won't run.
- **2026-07-05 — first-run bug fixes (merged to `main`, proto-cli 311 green + build clean).** From a real first-time user (fresh Node/Xcode/Claude Code).
  - **`proto start` no longer leaks a raw Expo `CommandError`** when there's no iOS 26 Simulator runtime (fresh Xcode). `ensure-prototo-app.ts` detects a missing runtime (`simctl list runtimes`), auto-runs `xcodebuild -downloadPlatform iOS` with a one-time-setup message (streamed progress), falls back to clear manual steps if that fails, never silently no-ops into the raw error. New `messages.installingIOSRuntime`/`iosRuntimeManualStep`/`noIOSSimulatorDevice`; injectable `downloadIOSPlatform` dep.
  - **`proto login` loopback** (`cli-login.ts`) only shows "You're signed in" when a valid `token`+`state` arrives — a dropped/errored OAuth redirect shows "didn't complete" and keeps waiting, instead of the premature success page.
  - **`proto share`** dropped the misleading "make sure it runs with proto start" on publish failure.
  - **IN PROGRESS (cross-repo, `prototo-website`): server-side publish** so `proto share` needs only a prototo account (no Expo creds). `publish-update.ts` runs `npx eas update` locally with only ambient Expo auth (no `EXPO_TOKEN`/bridge) → only the owner can publish today. Fix: CLI `expo export` → `POST prototo.app/api/publish` (prototo Bearer) → backend runs the EAS Update with its own creds, scoped to the caller's branch. Plus the **Google-OAuth `/cli` redirect** must thread `redirect_uri`+`state` back to the loopback (email sign-in works; Google dead-ends on the landing page). Spec: `~/.claude/plans/ios-widgets-and-live-quizzical-spark.md`.
- **2026-07-05 — @expo/ui shift (additive) + token-drift fix — code done, on-device verify pending.** Explored making `@expo/ui` the component foundation. **Finding:** re-basing the token-branded leaves (`Button`/`Input`/`Card`) is partly *regressive* — native `@expo/ui` `Button` styles via system `buttonStyle` and **ignores `theme.radius.button` + custom bg**, and can't host RN `icon`/`style`/`textStyle` (which `SignInScreen`'s `<GoogleIcon/>` uses). Consistent with the earlier deferral of the full migration. **Decision (additive, zero-regression):** keep `Button`/`Input`/`Card`/`Text` on RN + tokens; deliver the shift as **new native `@expo/ui` controls Proto lacked**, following the shipped `Toggle` pattern. Added **`Slider`** + **`Stepper`** (`packages/proto-components/src/{Slider,Stepper}.tsx`, exported from `index.ts`) — real SwiftUI on iOS via `<Host>`, tinted with the app accent, minimal RN Android fallback (no new deps; product is iOS-first). Synced into the template (`sync-template.ts`) + listed them in the template generator `CLAUDE.md`. **Also fixed a pre-existing bug:** the `design-tokens.ts` CLI mirror had drifted (`liquidGlass.surface.card` `#f3f5f8` vs canonical `#F2F2F7`) — reconciled the mirror **and** a second test (`design-template.test.ts`) that had hardcoded the wrong value; and declared the previously-undeclared `@expo/ui` + `expo-glass-effect` peers/devDeps in `proto-components` (closed a standalone-typecheck hole on `Toggle`/`Card`). **Verified:** proto-cli **307** green, create-proto **59** green, proto-components `tsc` clean. **Pending:** on-device simulator screenshot of `Slider`/`Stepper` (they follow the device-validated `Toggle` pattern + typecheck against real 56.0.17 types, but not yet eyeballed); `SegmentedControl`/`Picker` deferred (56.x uses a `selection`+`children`/`tag` API that needs a device check). All changes uncommitted. **Widgets/Live Activities plan parked** (in `~/.claude/plans/ios-widgets-and-live-quizzical-spark.md`) — pick up after.
- **2026-06-18 — screen-orientation shipped across all surfaces · App Clip fully removed · tagline refreshed (all LIVE).**
  - **`expo-screen-orientation` + rotation:** bundled in the dev client + scaffold template, `app.json` orientation `portrait`→`default`. Cut the **clean de-clipped dev client 0.2.2** — device `97ef3cd0` (install pointer `next.config.mjs` → it, PR #38, deployed) and sim `prototo-sim-sdk56-3` (`…-latest` pointer) both **LIVE**; re-uploaded the sim build to **Appetize** (`b_evjmux…`, still 0.2.1 — functionally identical) so shared prototypes can rotate too.
  - **App Clip fully removed:** deleted `apps/prototo-clip` + `apps/prototo-viewer` (PR #22), then finished the in-dev-client de-clip (PR #28) — removed `ClipApp.tsx`, `app/p/[token].tsx`, `lib/fetch-manifest.ts`, and the `react-native-app-clip` / `@sherizan/proto-manifest` / `@sherizan/proto-renderer` deps; `index.ts` → `import 'expo-router/entry'`. Clean prebuild (no `PrototoClip`, no AppDelegate warning). `apps/` is now just `apps/prototo-app`.
  - **Tagline** "Describe a screen. Watch your prototype run natively on iPhone." (was "The prompt-native design environment…") → `create-proto 0.7.2`, then **0.7.3** added the screen-orientation scaffold dep. Both published + verified in the tarball.
- **2026-06-17 — Tagline refreshed + bundled `expo-screen-orientation` (prep — completed 2026-06-18 above, shipped as dev client 0.2.2).** New tagline **"Describe a screen. Watch your prototype run natively on iPhone."** (was "The prompt-native design environment for native iOS.") in the scaffold banner + README + npm description — **`create-proto 0.7.2` published**. Then prepped `expo-screen-orientation` (~56.0.5) into the bundle (branch `feat/bundle-screen-orientation` `66bd0f4`): added to `apps/prototo-app` + the scaffold template, and flipped both `app.json` orientations `portrait`→`default` (a portrait-locked binary ignores `lockAsync`, so rotation needs this). Dev client bumped 0.2.0→**0.2.1**; `native-bundle.md` updated. **NOT live yet — needs a native rebuild:** Sheri runs `pnpm release:all` (device + sim EAS build) + updates install pointers; **decide runtimeVersion (`CS-03`):** adding a native module to the same `prototo-56` runtime means old binaries lacking it could crash a new share that uses it — consider bumping the runtime string. **Then** republish **`create-proto 0.7.3`** with the template dep (NOT before the dev client ships, or scaffolds get the JS import without the native module).
- **2026-06-17 — Update notifier + `proto upgrade` — SHIPPED.** `@sherizan/proto-cli 0.7.4` published to npm (`latest`); website PR #34 merged + `GET /api/cli/version` live on prototo.app (CLI PR #24 merged-pending). Endpoint reports npm `latest` (hourly-cached) + top changelog highlights; the nudge appears on `proto start` for projects below latest. (Original build note:) Frequent releases were stranding old scaffolds (pinned `^0.7.0`, run via `npx proto`). Now: on `proto start`, a fail-open, **daily-throttled** check (cached `~/.prototo/update-check.json`) hits a new public **`GET /api/cli/version`** on prototo.app → `{ latest (npm, server-side cached), highlights (top changelog items), changelogUrl }`; if behind, prints a one-line nudge with inline "what's new" + `Run proto upgrade`. New **`proto upgrade`** detects the project's package manager (lockfile) and runs `<pm> add/install -D @sherizan/proto-cli@latest` (crosses carets too), no npm jargon. Website: extracted `RELEASES` → `lib/releases.ts` (shared by the changelog page + endpoint; **caught it up to current** — was stuck at June 10, now incl. share/record/upgrade), fixed the `update-changelog` skill pointer. **Verified:** proto-cli **306 tests** green (+16: compareSemver/fetch/checkForUpdate/upgrade), website **148** (+3), both tsc + lint/Biome clean; real-code smoke test (nudge when behind; fail-open 0ms on dead server). CLI built + packed **0.7.4** (`sherizan-proto-cli-0.7.4.tgz`). CHANGELOG (proto + website) June 17 entry added. **Chicken-and-egg:** existing installs only gain the notifier *after* their first upgrade. **Pending:** deploy website first (CLI fail-open if endpoint absent), then publish proto-cli **0.7.4** (auto-propagates via `^0.7.0`).
- **2026-06-17 — Retired-app cleanup + new App Store direction.** Deleted `apps/prototo-clip/` (tracked) + `apps/prototo-viewer/` (untracked, build-artifacts-only — no unique source left) on branch **`chore/remove-retired-clip-viewer`** (`da2b40e`, unpushed); freed ~1.1GB, `apps/` is now just the live EAS dev client `apps/prototo-app` (untouched). Archived the `@expo/ui` migration prompt to `docs/archive/` (deferred — proto-components stay on RN + `expo-glass-effect`). **New long-term goal recorded in `BACKLOG.md`:** build a **Prototo iOS app for the App Store** (Expo-Go-style: a published native client that loads/streams prototypes), replacing EAS-Internal-only distribution — needs its own spec; the 2.5.2 review question re-opens (Expo Go precedent). **Pending CHANGELOG decision:** a designer-facing `proto record`/Studio entry is drafted but not committed (CHANGELOG publishes to the website).
- **2026-06-16 — docs/ cleanup + reconciliation.** Moved the retired **App Clip / manifest / viewer** docs (self-marked SUPERSEDED 2026-06-12) + the consumed MCP handoff into **`docs/archive/`** (with a README; nothing deleted — `docs/*` is local-only, no git recovery). Living docs stay at `docs/` root; all `superpowers/specs`+`plans` for shipped features kept in place. Refreshed **`native-bundle.md`** to SDK 56 (added `expo-updates` for cloud streaming). Reconciled **`proto-master.md`**: header now carries a "what actually shipped" reality banner (SDK 56, share live via cloud streaming, accounts Free/Plus, Studio/`proto record`, MCP) + fixed §3 stack facts (SDK 53→56, reanimated 3→4, blur→glass-effect) + §8 status → SHIPPED. **Root docs too:** `CLAUDE.md` (context7 row → SDK 56/reanimated 4/glass-effect; `proto.run` → `prototo.app` ×3; phase pointers marked shipped); `BACKLOG.md` (Phase 3 web-share marked SHIPPED, archive path fixed — App-Clip on-disk cleanup TODO kept, still valid); `RISKS.md` (6 now-moot tunnel/App-Clip rows — `SH-03/04/07`, `CL-01/02`, `AS-03` — marked superseded/resolved per the file's in-place convention; `SH-01` rate-limit kept OPEN, it's now real — we hit 10/hr/IP during debugging). Still flagged: `proto-components-migration-prompt.md` (deferred @expo/ui migration — kept, verify if still wanted); **`CHANGELOG.md` not touched** (it publishes to the website) — needs a designer-facing `proto record`/Studio entry (Sheri's call on copy/timing); its May-25 "viewer mode" line is stale-but-historical (append-only, leave).
- **2026-06-16 — `proto record` countdown + tier caps + upload progress + browser auth handoff — SHIPPED.** `@sherizan/proto-cli 0.7.2` published to npm (`latest`); proto-cli PR #20 merged to `main` (`ac2172d`); website PR #31 merged + live. Verified on-device end-to-end (full 30s → upload → Studio opens signed-in). Four asks, built on the Ctrl+C→Enter fix below.
  - **Tier-capped duration (Free 30s / Plus 180s)** — single source of truth in the website `lib/plan.ts` (`PlanLimits.maxRecordingSeconds`). `POST /api/studio` now returns `tier` + `maxRecordingSeconds` (resolved live from the profile). The CLI **creates the session up front** (before recording) to learn the cap and to surface sign-in/rate-limit errors early; falls back to 30s if an older server omits it.
  - **Live countdown** — new `packages/proto-cli/src/countdown.ts` (`formatCountdown` pure + `startCountdown` 1s-tick live line). Recording auto-stops + uploads at zero; Enter still stops early. The orchestrator races Enter vs countdown-expiry vs recorder-died.
  - **Upload progress %** — `uploadRecording` PUTs the MP4 via Node's core `https` client (buffered + explicit Content-Length, written in 256KB chunks for the progress fraction); the CLI renders `↑ Uploading… NN%` in place. (Initial `fetch`+`ReadableStream`+`duplex:'half'` impl worked on Node 24 but failed on older Node/undici — switched to the core client, which is version-stable; added a real local-server integration test.)
  - **Storage limit blocker — RESOLVED.** Website shipped to prod (PR #31 merged, `836a3f6`) — `POST /api/studio` returns the cap, the open endpoint mints the magic link (both verified live). Recordings >~50 MB were failing to upload: confirmed via direct service-role uploads of real files — **6.8 MB ✅, 40.5 MB ✅, 73.5 MB → 413 "object exceeded the maximum allowed size."** Root cause: the **project-global Storage upload limit was 50 MB** (the `recordings` bucket's 200 MB value is capped by the global; a bucket can't exceed it — confirmed by an `updateBucket`→500 MB rejection while global was 50 MB). A full 30s retina capture is 40–73 MB; Plus 180s ≈ 450 MB. **Fix:** Sheri (already on Pro) raised the **global upload limit to 500 MB** in the Storage dashboard; then the `recordings` **bucket `file_size_limit` was raised 200 MB → 500 MB** via the service role. **Re-verified:** real 73.5 MB ✅ and synthetic 450 MB ✅ both upload `200` via signed URLs. Single-PUT signed-URL upload is fine to ~450 MB; resumable/TUS is a future hardening option, not needed now. Ref: supabase.com/docs/guides/storage/uploads/file-limits. Remaining: live on-device `npx proto record` e2e (the create endpoint is rate-limited 10/hr/IP — tripped during debugging, resets within the hour; clearing it was correctly blocked as a prod-data write).
  - **Browser auth handoff (fixes the /login bounce)** — new website `POST /api/studio/[token]/open`: Bearer CLI token → owner check → Supabase **admin `generateLink` magiclink** → returns a `/auth/callback?token_hash=…&type=magiclink&next=/studio?v=<token>` URL (reuses the existing `verifyOtp` callback, no page changes). The CLI calls `studioOpenLink` and opens that so the browser lands on Studio **already signed in, in any browser**; **fail-open** to the plain `/studio` URL on any error.
  - **Verified:** proto-cli **289 tests** green (+13: countdown 4, studio-api +4, record +5), website **137** green (+5: plan +1, studio create +2, open endpoint +6 file… 137 total), both `tsc` clean, CLI Biome-clean, website lint clean (only pre-existing `<img>` warnings). CLI built + packed (`sherizan-proto-cli-0.7.2.tgz`). **Pending:** deploy the website first (CLI is backward-safe), then on-device e2e (countdown 0:30/3:00, % to 100, browser opens authed), then publish the CLI (**0.8.0** — bundles this + the Ctrl+C fix). No push/publish yet (Sheri's go).
- **2026-06-16 — Fixed `proto record` dying on Ctrl+C before it ever opened Studio (TDD, code done, publish pending).** Symptom from a real run: `npx proto record` printed `Recording started…`, then `^C` returned straight to the shell — no `Saving recording…`, no error, no Studio. Root cause: the stop was implemented by intercepting Ctrl+C (`defaultWaitForStop` → `process.once('SIGINT')`), but under `npx` the terminal delivers SIGINT to the whole foreground process group (npx wrapper + CLI + the `xcrun … recordVideo` child) at once and the Node process is killed by the default signal action **before** the `await` continuation (the create-session → upload → `openBrowser` pipeline at `record.ts:193-207`) runs. That whole pipeline was correct but never reached. The stop path was also effectively untested — `record.test.ts` injected a fake `waitForStop`/`startRecording`, so real signal/stdin behaviour was never exercised.
  - **Fix (Sheri's call): stop on an Enter keypress, not a signal.** `defaultWaitForStop` now resolves on the first stdin `data` chunk (falls back to `SIGINT` when stdin isn't a TTY, e.g. piped/CI); `messages.recordStarted` copy → "press Enter to stop." Ctrl+C stays a plain cancel. Extracted `spawnRecorder(command, args)` out of `defaultStartRecording` and exported it + `defaultWaitForStop` so the previously-untested spawn/stop + keypress primitives now have real tests (PassThrough for stdin; a `node -e` SIGINT-exiting stand-in for the recorder).
  - **Verified:** proto-cli **276 tests green** (record file 11/11), tsc clean, Biome clean on changed files. **Still pending:** the on-device e2e (booted sim → `npx proto record` → record → Enter → `Saving…`/`Uploading…`/opens Studio) is Sheri's to run — it's a real-runtime interaction the unit tests can't fully prove. **Then** publish **0.7.3** (`pnpm publish`; scaffolds pin `^0.7.0` so it auto-propagates). **Secondary finding (follow-up, not fixed):** opening `prototo.app/studio?v=<token>` **307-redirects to `/login`** when the browser isn't signed into prototo.app — the CLI token and browser session are separate auth contexts, so even a perfect run can land on a login page.
- **2026-06-15 — Shipped the share gate + fixed a scaffold-breaking dep bug + trimmed the command surface (three npm releases).**
  - **`@sherizan/proto-cli 0.7.1` published** — the free-cap upgrade gate (PR #15 + bump #16). Note: a prior session had published `0.7.0` (proto record) and `create-proto 0.7.0`; STATUS was stale (it still said 0.6.2).
  - **`create-proto 0.7.1` published — FIXED a scaffold-breaking bug (PR #17).** The `0.7.0` release left `@sherizan/proto-cli` pinned in BOTH `dependencies: ^0.7.0` and `devDependencies: ^0.6.0` (disjoint ranges) → npm resolved the lower one, so **every fresh `npm create proto` scaffold installed proto-cli 0.6.2** — no `proto record`, no share gate (symptom: "Unknown command: record"). Fix: proto-cli in devDependencies only at `^0.7.0`, plus a new `template-integrity` test that fails if it's declared twice or its minor drifts from the workspace CLI. Verified the published 0.7.1 tarball: single pin, dep rewritten to `^0.7.0` (no `workspace:` leak).
  - **`proto-cli 0.7.2` — hid vestigial commands from `proto help` (PR #18, merged, NOT yet published).** `new-screen` (Claude Code writes screens directly; referenced nowhere in the template) and `shot` (superseded by the MCP `get_simulator_screenshot` tool) removed from the help text + examples only — dispatch, implementation, and tests stay intact and functional (hide-not-delete, per the pause pattern). `login` kept visible. **Pending: publish proto-cli 0.7.2** (create-proto's `^0.7.0` pin will auto-propagate it).
  - **Publish gotcha learned:** `pnpm publish --no-git-checks` bypasses the unclean-tree guard — fine here, but it would ship uncommitted working-tree state; commit first. The npm `E404`-on-publish was just "not logged in" (npm masks scoped 404 for unauthenticated); `EOTP` then needed the 2FA code.
- **2026-06-15 — Free-tier "upgrade to keep sharing" gate wired across both repos (TDD).** Audit finding: the gate existed but the two repos had drifted — the website already enforced Free cap = 1 (Plus = 3, Stripe) and `/account` already had an "Upgrade to Plus" CTA, but the **CLI was stale**: `messages.shareProjectCap` said "Free accounts can share **3** prototypes… **remove one**", the gate only fired on the post-publish 403, and the `prototo.app/account` URL was printed but never opened. Per Sheri's call (tighten to 1, upgrade-to-continue, handoff → `prototo.app/account`):
  - **Website (`prototo-website`, branch `feat/share-preflight`, unpushed):** new **`POST /api/share/preflight`** (`app/api/share/preflight/{handler,route}.ts`) — authenticated, informational, never mutates. Returns `{ allowed, tier, activeProjects, projectCap }`; re-share of an existing token is always `allowed:true`, a new token is allowed only while under the tier cap. Reuses `verifyApiToken` / `ProfileStore.getTier` / `limitsForTier` / `ShareStore.{get,countActiveByUser}`. No cap change needed (already 1). **119 web tests green (+7), tsc + lint clean.**
  - **CLI (`proto-cli`, branch `prototo-studio`, unpushed):** `share-api.ts` gains `preflightShare(token, opts)` (**fail-open** — any error/non-200/old-server → `null`) + `accountUrl()` (respects `PROTO_SHARE_API_BASE`). `runShare` now preflights **before** `publishUpdate`: capped → `handleCapReached` (logs the new upgrade copy + `openBrowser(accountUrl())`), no publish; `null` → publish proceeds; the createShare **403 backstop** also routes through `handleCapReached`. `messages.shareProjectCap` rewritten to "You're at your free sharing limit. Opening prototo.app/account — upgrade there to keep sharing more." DRY: extracted `src/open-browser.ts` and pointed `cli-login.ts` + `commands/record.ts` at it (record's opener was macOS-only — now cross-platform). **290 proto-cli tests green (+10), tsc clean, changed files Biome-clean.**
  - **Other CLI→website handoffs audited + confirmed wired:** `proto login` opens `/cli`, `proto share` prints `/p/<token>`, `proto record` opens `/studio`. No upgrade/billing handoff existed before this; `/account` is now the one.
  - **Pending:** live e2e (capped free account → `proto share` against local website with `PROTO_SHARE_API_BASE=http://localhost:3000` → nudge + opens `/account`, no publish); push both branches + deploy the website preflight route (Sheri's go); CLI publish a patch bump if shipping.
- **2026-06-14 — 🎉 `proto share` LAUNCHED PUBLICLY (cloud streaming). `@sherizan/proto-cli 0.6.2` + `create-proto 0.6.1` published to npm `latest`.** The whole "Share — path to public launch" checklist is closed. Appetize upgraded to **Starter**, which enables external-domain embeds — verified live: `prototo.app/p/YNRE5DK8QE6J` streams the real prototype on a cloud iPhone in a plain browser (the old "embeds not enabled" block is gone). Then un-hid `proto share` (reverse of the 2026-05-29 pause) on branch **`ship-unhide-share`** off current `main` (`f6858da`, post-MCP-merge) — `share` restored in `proto help` + examples (`cli.ts`), create-proto's stop message (`howToRestart` + test), and the scaffold `README.md`. Copy rewritten for cloud streaming, **not** a literal tunnel-era revert. The bigger correctness fix in the same commit: the scaffold `template/CLAUDE.md` "two fidelity tiers / manifest viewer" section + the matching root `CLAUDE.md §1` invariant (local-only/gitignored) replaced with the cloud-streaming truth — **a shared link streams the real prototype at full fidelity**; only caveats are the SDK runtime-version gate + simulated cloud hardware (camera/GPS). `docs/CHANGELOG.md` June 14 entry added (website mirrors it). 325 tests green, both typecheck clean, create-proto's published proto-cli dep correctly `^0.6.0` (no `workspace:` leak). Commit `f17748f`; PR #11 polish (clickable share link + login copy) rode along. **Pending:** push `ship-unhide-share` + open PR (Sheri's go); delete the stale `unhide-proto-share` branch.

- **2026-06-14 — Proto MCP server (first slice) BUILT on branch `feat/prototo-mcp` (not committed/published).** Closes the "Claude Code writes screens blind" loop from the handoff (`~/Downloads/prototo-mcp-handoff.md`). Shipped the **feedback-loop pair** of the brief's five tools; the other three are parked in `BACKLOG.md` ("Proto MCP — remaining tools").
  - **`compile_check(screenName?)`** — type-checks the project and returns errors in designer-friendly copy (reuses the `error-translation.ts` pattern via a new tsc-specific `translateTscError`). Scaffolds ship `typescript` but **no tsconfig**, so it writes a Proto-managed `.proto/tsconfig.mcp.json` (extends `expo/tsconfig.base`) — off the designer surface, gitignored like `last-shot.png`. Graceful fallback when tsc can't run.
  - **`get_simulator_screenshot()`** — wraps the existing `runShot` helper (`commands/shot.ts`), base64-encodes the PNG → MCP image block; friendly text block when no sim is booted.
  - **Two divergences from the brief, by design:** (1) **stdio transport, not HTTP/SSE on port 4759** — both tools are stateless, so no port handling and **zero changes to `proto start`**. (2) **Shipped as a second bin `proto-mcp` inside `@sherizan/proto-cli`**, not a new `packages/prototo-mcp` — proto-cli is already a scaffold dep (`^0.6.0`), so a patch bump auto-propagates with no new publish target (avoids the `workspace:`-leak / stale-cache class of bugs). MCP code lives in `packages/proto-cli/src/mcp/`.
  - **Scaffold wiring:** `create-proto/template/.mcp.json` (`{prototo: {command:"npx", args:["proto-mcp"]}}`) auto-connects Claude Code on project open — verified it ships in `npm pack`. Template `CLAUDE.md` gained a "## Proto MCP tools" section (after "Check your work visually"). A `template-integrity` guard test pins the `.mcp.json` contract.
  - **Verified:** TDD red→green for all three logic units; **proto-cli 267 tests** (+19), **create-proto 58** (+1), both typecheck clean, my files biome-clean, build emits `dist/mcp/index.js` with shebang intact. **Real MCP stdio handshake**: spawned the built server, both tools list with correct schemas, `compile_check` runs end-to-end. **Not yet done (gated):** the live on-device E2E (fresh scaffold → `proto start` → Claude Code connects → screenshot/compile against a real RN screen) needs the published CLI + a running sim. **To ship:** bump proto-cli 0.6.1→0.6.2 (auto-propagates) + create-proto 0.6.0→0.6.1 (template changed), `pnpm publish` both, on Sheri's go.

- **2026-06-13 — CLI half of the Share Accounts MVP SHIPPED: `proto-cli 0.6.1` published to npm (`latest`).** `proto login` + authenticated `proto share` restore sharing against the now-auth-gated prod API. TDD red→green in `packages/proto-cli`; **244 tests green** (was 213, +31), tsc + biome clean. PR #10 merged to `main` (`eb589df`); published 0.6.1 verified — fresh `npm install` works, `dependencies` clean (no `workspace:`/`0.0.0` leak), bin carries `login`, `proto help` lists `login` (share stays hidden). Patch bump so the template's `^0.6.0` pin auto-propagates (no create-proto republish). **`proto share` deliberately stays out of `proto help`** — so a designer won't stumble onto it before the Appetize Starter launch flip lets recipients actually watch the stream. **Pre-publish fix:** dropped `proto-manifest` + moved `proto-compile` to devDeps (they were stray `workspace:*` runtime deps added post-0.6.0 — would have rewritten to unpublished `0.0.0` and broken installs). **Still pending:** a live human-driven end-to-end (real Supabase browser sign-in → `proto share` → 201 → appears on `/account`) — unit + loopback-integration verified, but the real sign-in path is unproven on-machine.
  - **`cli-token.ts`** — token store at `~/.prototo/api-token.json` (mode 0600), DI'd `fs`/`homedir`/`env`; `PROTO_API_TOKEN` env override wins (CI/scripted shares). Reuses the `designer-identity.ts` global-config-home pattern (`~/.prototo/`), not project-local `.proto/`. **Storage decision: file, not OS keychain** — consistent with how the codebase already keeps secrets; keychain is a deferred follow-up.
  - **`cli-login.ts` + `commands/login.ts`** — `proto login`: one-shot loopback server on `127.0.0.1:0`, opens the browser to `<base>/cli?redirect_uri=http://127.0.0.1:<port>&state=<rand>`, catches the redirect, **verifies `state`** (CSRF), validates the `proto_` token, friendly "close this tab" page, 2-min timeout. Pure `buildLoginUrl`/`parseCallback` unit-tested + a real-socket integration test; end-to-end loopback smoke passed. `CliLoginError` kinds: `timeout`/`state-mismatch`/`no-token`.
  - **`share-api.ts`** — `createShare` now sends `Authorization: Bearer <token>` (via `opts.token`); new `ShareApiErrorKind`s `unauthorized`(401)/`cap-reached`(403)/`owner-mismatch`(409) mapped before the generic branch.
  - **`commands/share.ts`** — resolves the account token before the (slow) publish; **auto-runs `proto login` when logged out** then continues; threads the token into `createShare`; maps the new errors to designer copy (`shareLoginExpired`/`shareProjectCap`/`shareOwnerMismatch`). `cli.ts` registers `login` + help line.
  - **Still gated:** end-to-end live verification (browser sign-in → real `/api/share` 201 → appears on `/account`) needs Sheri to run it against prod/local. And **`proto share` stays hidden from `help`/README/template** (hidden 2026-05-29) — un-hiding advertises sharing, which is still gated on the **Appetize Starter** launch flip; decide at launch, not here.
- **2026-06-12 — Web side of the Share Accounts MVP SHIPPED to production (prototo.app).** All of P1 + P2 on the **`prototo-website`** repo, built TDD, merged to `main` (PRs #3 + #4), deployed green. **63 web tests, tsc + build clean.** Supabase project `wlnztkzlcvqiuzdavagt`; the `increment_usage` RPC migration is pushed; Supabase env vars + Google OAuth are configured in Vercel prod; **Google + email magic-link sign-in works end to end** (confirmed by Sheri). What's live:
  - **Auth:** `@supabase/ssr` session middleware (scoped to `/account` + `/cli`), `/signin` (email magic-link **and** Continue-with-Google), `/auth/callback` (token_hash + code, same-origin-guarded `next`), `/auth/signout`, `/account` (projects n/3 + streaming min n/30 + share list).
  - **Tokens:** `lib/api-token.ts` — a CLI token is `proto_` + base64url(32 random bytes); the server stores **only its sha-256 hash** in the `api_tokens` table; `verifyApiToken` checks hash + not-revoked and stamps `last_used_at`.
  - **Shares are now owned:** `lib/share.ts` moved Redis→Postgres behind a `ShareStore` port; `createShare(store, userId, input)` attributes the share to the authed user, enforces **`FREE_PROJECT_CAP = 3`** (only on *new* projects; re-sharing an existing token is an update), and rejects overwriting a token owned by another account.
  - **`POST /api/share` now REQUIRES `Authorization: Bearer <proto_token>`.** ⚠️ **Breaking for the CLI:** the current `proto share` sends no auth header → **401** → nothing is created, nothing shows in `/account`. This is intended; the CLI half is the next task.
  - **CLI token handoff (web side, ready + tested):** `/cli?redirect_uri=<loopback>&state=<rand>` is a server-gated page (routes logged-out users through `/signin`); after sign-in it POSTs `/api/cli/token` (which mints a token for the session user) and redirects the browser to `redirect_uri?token=<proto_token>&state=<state>`. The redirect_uri is **pinned to loopback** (`127.0.0.1`/`localhost`/`::1`) server-side via `lib/cli-auth.ts isLoopbackRedirect()`.
  - **Metering:** `POST /api/usage` heartbeat (clamped to elapsed wall-time + a per-beat cap; atomic `increment_usage` RPC), and `/p/[token]` blocks streaming + shows a friendly "out of free minutes" state when the owner is over the 30-min/mo Free budget — without ever exposing the owner's id to the recipient.
  - **Still pending (web, not blocking the CLI):** Appetize **Starter** upgrade + `NEXT_PUBLIC_APPETIZE_PUBLIC_KEY` in prod is the **last** launch step (until then `/p` shows the graceful fallback; the stream itself is validated via the direct `appetize.io/embed` URL). Team tier / Stripe is the fast-follow.
  - **Web reference code (read these when building the CLI):** `app/cli/page.tsx` + `app/cli/AuthorizeCli.tsx` (the handoff page), `app/api/cli/token/{handler,route}.ts` (mint), `lib/api-token.ts` (token format + verify), `app/api/share/handler.ts` (the Bearer check + status codes), `lib/cli-auth.ts` (loopback rule). Handoff log: `prototo-website/SESSIONS.md`.
- **2026-06-12 — Share monetization refined to Free → Team + MVP scope decided.** **Plus dropped** — only two tiers: **Free (solo)** + **Team (per-seat ~$24)**. **MVP = Free + accounts + metering, NO payments** (ship to track real usage + member sign-ups before pricing); **Team (Stripe, per-seat) is the fast-follow**; the **Appetize paid upgrade (Starter $59, enables embeds) is the LAST step before launch** — don't pay per-minute before there's something real to stream. The website is **greenfield** for this (only Upstash Redis today — no auth/db/stripe/accounts). **Stack locked: Supabase (Auth + Postgres) + Stripe (Team, fast-follow); sign-in Google + Apple + email** (Sheri has a Supabase account). Postgres = system of record (accounts, shares, usage; later orgs/seats/subs for Team — hand-built, Supabase has no Organizations primitive); Upstash Redis kept for rate-limiting. Next: MVP implementation plan + build P1 (Supabase auth on prototo.app + `proto login` CLI + share attribution) → P2 (metering + free caps). Detailed plan: `docs/superpowers/specs/2026-06-12-share-accounts-mvp-implementation.md`.
- **2026-06-12 — (superseded same day) Share pricing & economics model first drafted (Free / Plus / Team, land-and-expand).** Spec: `docs/superpowers/specs/2026-06-12-share-economics-and-pricing-design.md`. **Strategy:** Prototo's core (local prototyping) is already valuable + stays free for solo; the *sharing layer* is the monetized, inherently-org product. **Free + Plus = solo** acquisition funnel (their Appetize stream-minutes are accepted marketing spend); **Team = per-seat (~$24 bracket)** revenue, championed into the org by the designer — per-seat pricing makes margin scale with org size and fixes the $0.06/min margin problem that made cheap solo tiers thin. Grounded in **real Appetize pricing** (rendered live 2026-06-12): Starter **$59/mo** = 500 min + **3 active devices** (concurrent streams, account-wide) + **embed enabled** (the unlock the round-trip needs), then **$0.06/min**; Premium $319 = 16 devices + whitelabel. Free-tier guarded by 4 levers (project cap + monthly minute budget + per-session caps + time-boxed links). Auth required at `proto share` (accounts on prototo.app); streaming **self-metered in our own embed** (no Appetize-reporting dependency). Build phasing 2a→2b→2c in the plan's Phase 4; **gated on upgrading our Appetize account to Starter** (Sheri's billed call — also unblocks the in-page stream, which the free trial blocks: "embeds not enabled, upgrade your plan"). New risks `CS-06` (concurrency ceiling) + `CS-07` (metering accuracy); `CS-01` updated.
- **2026-06-12 — Phase 1 share contract wired end to end (both repos) + Phase 2 embed scaffold.** Both halves of the cloud-stream share contract now agree on `{token, designerName, appName, deepLink}` and are green.
  - **proto-cli (branch `phase-1-eas-update`, committed):** `publish-update.ts` (real `eas update` flags), `share-token.ts` (12-char CSPRNG token in `.proto/share.json`), `share-config.ts` (idempotent `.proto/expo-config/app.json` patch → central `prototo-share` project), `share-api.ts` + `commands/share.ts` rewritten manifest→deep-link. **213 tests green.**
  - **prototo-website (branch `phase-1-deep-link-share`, `3f816a6`, NOT pushed):** `lib/share.ts` upserts `{token,…,deepLink}` keyed by client token, **pins `deepLink` by regex to the central `prototo-share` projectId** (security: blocks loading arbitrary code via the cloud dev client's `?url=`); `lib/appetize.ts` `buildEmbedUrl()` + `SESSION_MAX_MS` cap (TDD); `app/p/[token]/` now renders the **Appetize live-stream** (`ProtoStream.tsx`, gated on `NEXT_PUBLIC_APPETIZE_PUBLIC_KEY`, graceful "not available" fallback + max-length resume) instead of the App Clip card. **27 tests green, typecheck clean, lint clean.** Commit is isolated to the 8 share files — Sheri's other WIP on `phase-f-manifest-share` is untouched.
  - **✅ NO-LOGIN DEEP-LINK LAUNCH VALIDATED IN-BROWSER (2026-06-12):** with the Appetize public key wired (`NEXT_PUBLIC_APPETIZE_PUBLIC_KEY=b_evjmuxy2xyy5xxtozyggaueliu`, the uploaded dev-client sim build), loading `appetize.io/embed/<key>?…&launchUrl=<ombak deep link>` in a plain Chromium (Playwright) **booted the Prototo dev client on a cloud iPhone (iOS 26), loaded the published `ombak` EAS Update via `launchUrl`, and streamed it live — "Launching app…" → the full Skia/Reanimated surf prototype — with NO login prompt and no install.** Confirms (a) the launchUrl path needs no EAS login (unlike the Updates tab), (b) the default device/osVersion slugs `iphone17pro`/`26.0` are valid, (c) a real published update streams. The `/p/[token]` page wraps this identical URL via `buildEmbedUrl` → validated by construction. Real ombak update group: `ef82f80e-ee00-474b-94bb-ae2911ede134` (branch `ombak`, runtime `prototo-56`, central project `8c8ddf7d`).
  - **✅ FULL PAGE ROUND-TRIP RUN (2026-06-12) — page correct, embed gated on Appetize plan.** `next dev` → `POST /api/share` (token `XK92MABCDEFG`, real ombak deepLink → 201; `GET` reads back with no token leaked) → opened `/p/XK92MABCDEFG` in Chromium. The page chrome is perfect: prototo logo, "Sheri shared a prototype with you.", "ombak · made with Prototo", correct page-shaped iframe mounting the right embed URL. **BUT the embed shows "Embeds are not enabled for this app. The app owner must upgrade their plan to enable embeds."** → **KEY FINDING: third-party `<iframe>` embedding requires a PAID Appetize plan (Starter $59+).** The free trial only allows the *hosted* `appetize.io/embed/…` page (which streamed ombak fine in the earlier top-level-nav test) — it does NOT allow embedding into our own domain, which is exactly what `prototo.app/p/<token>` does. So: our code is validated end to end; the live stream-in-our-page is blocked purely on the Appetize plan. This was always coming (the cost model assumed Starter/Premium at launch) — it's now a hard prerequisite even to demo the embed. **(Test share `XK92MABCDEFG` is in prod Redis, 7-day TTL.)**
  - **Remaining for Phase 2:** **upgrade Appetize to a paid plan that enables embeds** (Starter $59+ — Sheri's call, it's billed) → re-run the page round-trip to see the stream in-page; tighten session caps against the real Appetize SDK events (idle = Appetize's ~2-min default; max-length = our deterministic timer); set `NEXT_PUBLIC_APPETIZE_PUBLIC_KEY` in the Vercel prod env before deploy.
- **2026-06-12 — 🔀 PIVOT + Phase 0 spike GO: cloud streaming replaces the App Clip for sharing.** A delta analysis of real user prototypes (motion/reactbits/test2/waaave — **14/14 screens**) showed the declarative manifest/App Clip expresses **~0%** of what users build (Skia/games 10, Reanimated 8, native maps/camera/audio ~7, dynamic `.map` 13, raw RN 16) — all imperative/native, outside any declarative manifest **and** Apple 2.5.2. The requirement is now **100% fidelity**, which only **cloud streaming** (run the real prototype on a cloud device, stream to a web link — the Expo Snack/Appetize model) delivers. **The App Clip / manifest / `apps/prototo-viewer` are retired once cloud streaming ships** (user: *"if expo snack equivalent works then we can retire app clip"*). Plan: `~/.claude/plans/read-prototo-app-clip-joyful-popcorn.md` (PIVOT section).
  - **✅ Spike validated on Appetize (GO):** (0a) the SDK-56 dev-client sim build uploaded to Appetize **boots + streams on iPhone 17 Pro / iOS 26.0** (Liquid Glass works on Appetize — the make-or-break risk, cleared). (0b) `playground/ombak` — a real **Skia + Reanimated + gesture + fetch** surf prototype (SDK 56) — loaded via a public Expo tunnel **renders, animates, and is interactive in the browser stream.** 100% fidelity for a prototype that's impossible as an App Clip.
  - **Architecture (Expo-native):** `proto share` → `eas update` the real bundle to a shared prototypes project → deep link `prototo://expo-development-client/?url=https://u.expo.dev/<project>/group/<group>`; the recipient's `prototo.app/p/<token>` web page embeds **Appetize**, boots the dev client (uploaded once), loads the update → live stream, no install. Bundle hosting = **EAS Update** (`u.expo.dev`, no custom CDN); the dev client loading a published update via the `expo-development-client/?url=` deep link is documented + supported.
  - **Graceful degradation = the runtime-version gate:** the prototype must match the dev client's SDK (56). SDK-55 prototypes won't load → "can't preview, built on an older Prototo." Coverage 100%, render best-effort.
  - **Appetize plan:** Free $0 (trial — used for the spike) · Starter $59 · Premium $319 · Enterprise (custom); billed by concurrent sessions and/or metered minutes. Free for build/validate → Starter/Premium at launch sized by concurrent viewers × monthly minutes (per-minute streaming is the main ongoing cost).
  - **Next:** Phase 1 (`proto share` → EAS Update + `expo-updates` in the dev client — the tunnel proved fidelity but needs a live Metro, doesn't scale), Phase 2 (reskinned Appetize web embed at `/p/<token>`), Phase 3 (retire the App Clip/viewer/manifest-share). **Open:** cold-start latency + Appetize cost at concurrency, still to measure.
- **2026-06-12 — Phase E2: `apps/prototo-viewer` — native App Store viewer app that hosts the App Clip (branch `phase-e2-viewer-app`, `89b8421`).** A new, App-Store-bound app **`com.sherizan.prototo.viewer`** (clip **`com.sherizan.prototo.viewer.Clip`**) that opens shared prototypes and **renders manifests natively — never downloads JS**, so Guideline 2.5.2 is a non-issue. The dev client (`com.sherizan.prototo`) stays EAS-Internal and untouched. **v1 = local-only** (no accounts, no backend changes — decided 2026-06-12). Design spec: `docs/superpowers/specs/2026-06-12-prototo-viewer-app-design.md`.
  - **App:** Expo SDK 56 + expo-router + `react-native-app-clip`; `index.ts` branches on `isClip()` (clip → `ClipApp`; full app → expo-router). **Home** (`app/index.tsx`): open a prototype by pasting a link or typing a 5-char code + an on-device **"Recently viewed"** list. **Render route** (`app/p/[token].tsx`): fetch by token → `ManifestRenderer`, recording the view in recents. Reuses the workspace `proto-renderer`/`proto-manifest`/`proto-components` (the native-fidelity renderer from Phase E).
  - **Shared-code cleanup:** promoted `fetch-manifest` into **`@sherizan/proto-renderer`** (`fetchManifest` + `tokenFromUrl` + `tokenFromInput`; the ok-result now carries `appName`/`designerName` for recents) so the clip, viewer, and dev-client `/p` route share one tested copy. Dev client migrated to import it; local copy deleted. `lib/recents.ts` (capped/deduped AsyncStorage history) is TDD'd.
  - **✅ VALIDATED ON-DEVICE (2026-06-12):** built the `Prototo` (full app) scheme via local Xcode (Release) on a physical iPhone — home renders, pasting token **`N62YV`** fetches from **production** and renders the prototype natively (the Phase E native nav + sheet), and it persists into **"Recently viewed"** + re-opens. The whole local-only viewer works end to end.
  - **Verified (in our control):** renderer suite **28/28** (11 runtime + 17 fetch/token), recents **7/7**, typecheck clean (own files); the viewer's clip target wires correctly — `com.sherizan.prototo.viewer.Clip`, parent `com.sherizan.prototo.viewer`, applinks+appclips (via `expo config`).
  - **Two build gotchas hit + fixed (carry forward for any new App Clip app):**
    1. **Stale RN-0.83 `react-native-svg`** linked transitively (proto-components peer) broke the Xcode compile → add it (+ other unused proto-components viz peers: skia/lottie/blur/ease/gesture-handler) to the app's **`expo.autolinking.exclude`**. Applied at `pod install` time — no prebuild needed. (`0989475`)
    2. **"Multiple commands produce Prototo.app"** when building the *main app* scheme: `react-native-app-clip` sets the clip's `PRODUCT_NAME` from its `name` option, so `name: "Prototo"` made the clip build `Prototo.app`, colliding with the app. Fix: **`name: "PrototoClip"`** (distinct, matches the target name). Phase E never hit this because it only built the *clip* scheme. (`0fbac6d`) — **the dev client (`apps/prototo-app`) has the same latent issue in its main scheme, but it's moot (never App-Store-built; only its clip scheme is used).**
    3. Viewer is **not** a dev client → in Debug without Metro it shows raw "No script URL provided" (not a launcher). Build **Release** for standalone, or `expo start` + Debug for live reload.
  - **On-device validation (Sheri, same loop as Phase E):** `expo prebuild -p ios` → `xed ios` → automatic signing on both targets (`Prototo` + `PrototoClip`) → **Release** scheme → ⌘R. Test the **home** (paste a link/code → renders, appears in Recently viewed) AND the **clip** (Local Experience for `com.sherizan.prototo.viewer.Clip` + `_XCAppClipURL`). Local Xcode, not EAS (EAS can't auto-provision App Clip App IDs — Phase E learning).
  - **Apple/infra track (deferred, needs Sheri at submission time):** new App Store Connect record for `com.sherizan.prototo.viewer` (app-id TBD); re-point the website AASA `appclips` + the `/p/<token>` card meta from `com.sherizan.prototo.Clip`/app `6772877748` to the viewer's clip id + new app-id; TestFlight/App Store distribution; register the **Default** App Clip Experience; submit (Phase G). The live website is untouched until then; two clips coexist during dev (dev-client's + viewer's — only the viewer's is the production clip).
- **2026-06-11 — Phase E (minimal): App Clip embedded into the Prototo App; shares render natively (branch `phase-e-clip-embed`, `fc76013`).** One Expo app now ships two iOS targets sharing a JS bundle. `apps/prototo-app/index.ts` branches on `react-native-app-clip`'s **`isClip()`**: the clip target mounts a minimal `ManifestRenderer` root (`ClipApp.tsx`), the full app boots `expo-router` unchanged. The plugin wires the clip as **`com.sherizan.prototo.Clip`** (exactly matches the deployed website AASA + card meta), parent `com.sherizan.prototo`, `on-demand-install-capable`, with `appclips:prototo.app` on the clip's associated-domains — **verified via `expo config`** (the clip target + entitlements resolve correctly). **Retired the 2.5.2-offending JS-download share viewer:** `app/p/[token].tsx` now fetches the manifest by token and renders it **natively** via `ManifestRenderer` (same renderer as the clip → 2.5.16 parity), replacing the `lookupShare → redirectToDevClient` tunnel handoff; deleted `lib/share-lookup.ts` + `lib/viewer-redirect.ts` (+ tests). New `lib/fetch-manifest.ts` (TDD, **6 tests green**) is the shared never-throws fetch helper (ok / expired-404 / unreachable + designer copy) used by both the clip and the route. Heavy native modules excluded from the clip target via the plugin's `excludedPackages`; Metro gained monorepo wiring for the workspace renderer packages. Typecheck: 0 errors in app files (the 8 remaining are the pre-existing workspace-source-import condition, identical to the standalone clip — packages typecheck under their own configs; Metro/Babel build doesn't typecheck).
  - **✅ ON-DEVICE ROUND-TRIP PROVEN (2026-06-11):** the App Clip renders the `N62YV` manifest **natively on a physical iPhone** — CLI → manifest → prod Redis → App Clip fetch → native `proto-components` render, no code over the wire. The whole 2.5.2-safe thesis works on hardware. Build path that worked: **local Xcode** (`expo prebuild -p ios` → `xed ios` → automatic signing on both targets) — *not* EAS (EAS managed credentials can't auto-create the App-Clip-type App ID nor enable the parent's App Clips capability; see learnings). Invocation tested via the canonical **`_XCAppClipURL`** scheme env var (Edit Scheme → Run → Arguments → `_XCAppClipURL=https://prototo.app/p/N62YV`).
  - **Device-test learnings (carry to Phase G + docs):**
    1. **EAS build fails at signing for App Clips** — `com.apple.developer.associated-appclip-app-identifiers` (parent→clip link) is NOT in EAS's capability-sync list, and EAS doesn't sync the clip App ID's Associated Domains. EAS managed credentials also won't create the **App-Clip-type** App ID (must be made manually in the portal, type "App Clip", parent = main, product name "Clip"). → For dev/test, **local Xcode automatic signing** handles all of this; EAS path needs manual portal setup first.
    2. **Must run Release, not Debug** — ⌘R defaults to Debug, which expects a Metro dev server ("Finding dev servers" screen). The clip ships JS bundled, so set the scheme's Run Build Configuration to **Release** (README: "Release mode tends to be more reliable").
    3. **Installed full app suppresses the clip** — on the dev's own device the Prototo dev client claims `applinks:prototo.app` and intercepts the link (lands on "no dev servers found"), so the App Clip card path can't be tested without deleting the full app. `_XCAppClipURL` sidesteps this entirely and is the right dev loop. Real stakeholders (no app installed) get the card normally.
  - **`N62YV`** is live in prod Redis with a 7-day TTL from 2026-06-11 — re-share if it lapses.
  - **✅ NATIVE-FIDELITY PASS validated on-device (`afa6503`):** the renderer now uses real iOS chrome via **react-native-screens** (`ManifestRenderer` → `ScreenStack`/`ScreenStackItem`): native nav bars showing the manifest's screen titles, native push/pop + swipe-back, a **large title that collapses to centered inline on scroll** (entry screen), and the **Modal** as a native `presentationStyle="pageSheet"` (spring + backdrop dim + drag-to-dismiss). `react-native-screens` un-excluded from the clip target (+ pod install). Runtime reducer unchanged (11/11). Confirmed on a physical iPhone: large-title collapse, push/back, native sheet, coral manifest accent — all native.
    - **KEY SDK-56 LEARNING:** **SDK 56's expo-router dropped react-navigation — it's standalone, built directly on `react-native-screens`** (verified: `expo-router@56.2.10` has zero `@react-navigation` deps). So **do NOT use `@react-navigation/*`** in this project — it's incompatible with expo-router 56 and would conflict in the full-app `/p` route. Use `react-native-screens` directly (what expo-router itself uses). Large-title-collapse gotcha: needs a **`translucent: true` header** + the scroll view's `contentInsetAdjustmentBehavior="automatic"` (already on `Screen`) — without translucent the large title scrolls away instead of collapsing.
    - **Deferred:** state-driven dark mode (the demo "Dark mode" toggle flips its bound state, but v1 manifests don't map a state key to `colorScheme` — by-design; logged as a manifest enhancement for a reserved key or a `setColorScheme` action).
  - **Deferred to E2 / Phase G (open risks in the plan):** the dev-client's own runtime-JS-download path (the real App-Store 2.5.2 blocker), the native your-own-prototypes gallery + Sign in with Apple, the App-Clip JS-bundle-size number (shares the full app's bundle — fine for 100 MB link, not 15 MB QR), and the TestFlight/App-Store distribution shift for real public invocation.
- **2026-06-11 — Phase F: real `prototo.app/p/<token>` sharing — software complete + validated end-to-end against real Upstash Redis. PRs merged; website deployed to prod.** (Update: both PRs merged — proto `0145b27`, website `ce420e5` — and the website auto-deployed to production; verified live on `prototo.app`: AASA serves the `appclips` entry, `/p/<token>` serves the App Clip card meta, `/api/share/<token>` returns the manifest. App Store Connect registration uses the **Default** App Clip Experience, NOT Advanced — dynamic `/p/*` tokens + AASA; it's gated on Phase E producing a clip-containing build.) Sharing now crosses the wire as **data, not code**: `proto share` compiles the project's screens to a manifest, uploads it, and the App Clip fetches by token and renders natively. Three commits across two repos (none pushed; F4 is Apple/infra-bound):
  - **F1 — `proto-cli` (branch `phase-f-share`, `d710cae` + clip `76c85ed`):** rewrote `share.ts` from the tunnel/`cloudflared` flow to a manifest flow. New `share-project.ts` (`enumerateScreens` reads `screens/*.tsx`; `gatherProject` loads `proto.config.js` → `CompileConfig`); `share-api.ts` contract changed `tunnelUrl` → `manifest` (`ShareCreateInput {designerName, appName, manifest}`); orchestrator: findConfig → designerName → gather → `compileManifest` (rejects unshareable screens loudly with designer copy, surfaces warnings) → `createShare` → QR + URL. **198 tests green** (vitest aliases proto-compile/proto-manifest to source). The clip's `App.tsx` (`76c85ed`) reads its invocation URL, extracts the 5-char token, fetches `GET /api/share/<token>`, renders `body.manifest` via `ManifestRenderer`; bundled-demo fallback when launched tokenless; friendly copy for expired-link (404) / offline.
  - **F2 — `prototo-website` (separate repo, branch `phase-f-manifest-share`, `ba9711d`):** `lib/share.ts` stores the manifest (256 KB cap) keyed by a 5-char Crockford token in Redis (7-day TTL); `app/p/[token]/page.tsx` emits the **App Clip card** meta (`apple-itunes-app: app-clip-bundle-id=com.sherizan.prototo.Clip, app-id=6772877748`) + renders share metadata; `public/.well-known/apple-app-site-association` gains the **`appclips`** entry (`BEJ53HUAE7.com.sherizan.prototo.Clip`) alongside the existing applinks. **25 tests green, 0 TS errors.**
  - **End-to-end validation (real Redis, no prod deploy):** ran the website locally (`next dev` + `.env.local` real Upstash creds), ran `proto share` on a two-screen project (`PROTO_SHARE_API_BASE=localhost`) → real token `N62YV`; `GET /api/share/N62YV` returned a renderable manifest (materialYou, initial=Home, Screen/Detail nodes); the clip's token-parse (7 URL cases incl. lowercase/query/trailing-slash + invalid-token reject) and the exact fetch it performs both pass against the live endpoint; 404 path → friendly error. CLI → real Redis → clip-fetch proven; only the on-device **clip-invocation** (`getInitialURL` returning a real `/p/<token>`) is left, which needs F4.
  - **F4 — remaining Apple/infra handoff (NOT software; needs Sheri + Apple):** register the **Advanced App Clip Experience** URL (`prototo.app/p/*`) in App Store Connect; **deploy the website** (so the prod AASA serves the new `appclips` entry + the `/p/<token>` card meta goes live); ship the clip target in a real signed build via **Phase E** (the 2.5.16 parent app must contain the clip). Until then, real-device invocation can't be exercised. **Push of both branches awaits Sheri's go** (outward-facing); prod deploy is gated on the same.
- **2026-06-11 — Phase D2: `apps/prototo-clip` builds + renders a manifest natively (branch `phase-d-clip-renderer`, `99dc61e`). Phase D core done.** The real workspace App Clip app (Expo SDK 56 + `react-native-app-clip`, root = `<ManifestRenderer manifest={bundled demo}>`, wired to the **workspace** `proto-renderer`/`proto-components` — not vendored) **builds in Release and renders the demo manifest natively on the simulator** (Settings screen: title, native toggle, divider, tappable card, coral manifest-accent button) — no code over the wire. Monorepo gotchas resolved + documented (`2026-06-11-clip-size-spike.md` D2 addendum): **`expo.autolinking.exclude`** trims the heavy native modules the monorepo would autolink into the clip (skia/lottie/blur/svg/gesture-handler — incl. a stale RN-0.83 `react-native-svg` that *broke the build*; pods 100→93, keeps the clip at the spike's floor); **`@expo/cli`** as a direct devDep (pnpm hides it under `.pnpm`); `react-dom` pinned to `react`. **Deferred:** manifest fetch-by-token + clip-target invocation flow → Phase F; parent surface (2.5.16) → Phase E. Renderer pure-runtime tests still 11/11.
- **2026-06-11 — Phase D1 (manifest renderer core) built + validated on-device (branch `phase-d-clip-renderer`, `4c427ae`).** New `packages/proto-renderer`: pure `runtime.ts` state machine (`initialRuntime`/`applyAction`/`currentScreen` — nav stack + the 6 actions) with **11 vitest tests** (TDD red→green, typecheck clean); RN `renderNode` + `ManifestRenderer` mapping the 9 nodes + 6 actions onto `proto-components` (renderer owns onTap→onPress, bind→value/onChange, modal bind→visible/onClose, Card.onTap→Pressable wrap). Enabler: `proto-components` gains `ProtoConfigProvider`/`useProtoConfig`; `useTheme`/`useAccent` now read context with the static `proto.config.js` as fallback — **backward compatible** (create-proto template-integrity + sync tests green). **On-device validation** (vendored into a throwaway SDK-56 app, hosted by the Prototo 0.2.0 dev client, all 7 fixtures + 2 state-variant manifests): renderer mounts; all 9 node types render; **manifest theme injected** (coral `#FF6B6B` accent, materialYou); state→view binding works (bound Modal hidden vs natively presented; nav state renders the Detail screen). Live tap-dispatch not exercisable (macOS blocks AppleScript taps; no idb/cliclick) — covered by the unit-tested reducer + trivial onPress wiring; confirm by tapping in D2. **→ D1→D2 checkpoint: renderer validated; next is D2 (real `apps/prototo-clip`).**
- **2026-06-11 — D0 App Clip bundle-size spike: GO. Renderer = React Native (RN-reuse); no SwiftUI rewrite.** The architecture's go/no-go gate is answered. Built a throwaway SDK-56 / RN-0.85 / New-Arch App Clip in `/tmp/clip-spike` via `react-native-app-clip` v0.9.0, reusing the current `proto-components`. **It builds** (clip target compiles fully — Hermes + reanimated + glass + @expo/ui + haptics + safe-area; no native incompatibility; issue #80 avoided with a plain icon). **Size ≈ 13.5MB device** (arm64 binary ~10.2MB statically-linked + 2.6MB Hermes JS bundle + ~0.7MB assets) → fits the **100MB link budget** with ~87MB headroom; even *plausibly* under 15MB QR (~2MB margin, unconfirmed — QR is a stretch). Findings + Phase-D gotchas: `docs/superpowers/specs/2026-06-11-clip-size-spike.md`. `AS-06` → mitigated. **Phase D (real `apps/prototo-clip` manifest renderer) is unblocked.** Carry-forward gotchas: the clip's bundle phase must resolve `@expo/cli` (pnpm hoisting matters); pin `react-dom` to match `react`; `react-native-app-clip` is maintenance-mode (pin it).
- **2026-06-11 — SDK 56 SHIPPED end to end (tag `sdk-56`, main `12ca32f`).** The three SDK-pinned artifacts moved together per the SOP: **npm** `create-proto` 0.6.0 + `@sherizan/proto-cli` 0.6.0 (`latest`, workspace-rewrite verified `^0.6.0`); **simulator** `prototo-sim-sdk56-1`/`-latest` (sdk55 tags retained — old scaffolds keep working); **device** 0.2.0 EAS build `3b56455a`, and the website `/install/ios/app` redirect updated + deployed (was stale — it had pointed at a pre-0.1.10 build). Phases: 0 research → 2 template (gate: bundle+doctor clean; old-binary render check died on the expected RN 0.83↔0.85 `MessageQueue` mismatch — SOP amended) → 3 sim binary (render smoke on the real sdk56 binary: glass/Toggle/Modal/hot-reload all ✓) → 4+5 shipped coupled (device URL flip + template publish must move together; the install URL is unversioned). New SDK-56 facts: `expo-router` 56 is standalone → `expo-constants` + `expo-linking` are now direct deps of template + dev client; Node floor 20; TS 6; gesture-handler 2.31.1. Existing SDK-55 projects keep working untouched **unless** the designer re-installs the device app — changelog tells them to re-scaffold for the new foundation. Remaining: real-device smoke (In progress). `@expo/ui` migration intentionally NOT included (decoupled; not 1:1 — see Phase 0).
- **2026-06-11 — shareable-interaction authoring surface built; App Clip branch merged to `main` + pushed (`73e8606`).** All Phase A–C work + this surface is now on origin/main; the feature branch is deleted. **Release gate:** none of it is published — `create-proto`/`proto-cli` on npm and the 0.1.11 binaries are untouched, and `template/CLAUDE.md` deliberately does NOT teach `onTap`/`bind` yet (gated until Phase D's local runtime makes the props real — see the interaction spec). Do not publish a create-proto that advertises them before then. The shared tier is no longer static-only. Per spec `2026-06-10-shareable-interaction-authoring-surface-design.md`, TDD red→green: **proto-manifest** gains `Card.onTap` (types + JSON Schema + validator `collectRefs`; `interactive.json` rewritten to the compiler-emitted bind form) — 21 tests; **proto-compile** gains the `onTap` grammar (`navigate:`/`dismiss`/`toggle:`/`showModal:`/`hideModal:`/`set:key:value`) on Button/Card, `bind` on Toggle/Modal, `<Screen state={{...}}>` lifting + inferred-false state, `compileScreen` → `{screen, state}`, `compileManifest` → `{manifest, warnings}` (cross-screen conflicts last-writer-wins + warning), and the `interactive-home/detail.tsx → interactive.json` golden — 25 tests. Compiler tests now alias proto-manifest to **source** (vitest alias) so a stale `dist/` can't skew results. Docs: MANIFEST.md authoring section, both CLAUDE.mds teach `onTap`/`bind`, architecture-spec risk #7 resolved. **Still deferred to Phase D:** the local runtime (components honoring `onTap`/`bind` on-device) + the `proto graduate` codemod. Branch also merged main's 0.1.11 hotfix and committed the lockfile importers (the hotfix session's branch switch had emptied the working tree of A–C source; all safe in `0bc20bf`).
- **2026-06-11 — dark-mode palettes validated on-Simulator: 0.1.11 is now fully validated.** Fresh 0.5.2 scaffold against the 0.1.11 binary on iPhone 17 Pro (iOS 26.5); flipped `simctl ui appearance` light↔dark and inspected screenshots. Surfaces flip correctly (dark background, near-black cards, translucent glass card), white primary text, secondary text readable in both schemes, accent blue holds, no overlap/clipping. (Side discovery: the dev-menu first-run sheet blocks screenshots — `xcrun simctl spawn booted defaults write com.sherizan.prototo EXDevMenuIsOnboardingFinished -bool true` suppresses it for automated `proto shot` checks.)
- **2026-06-10 — on-device validation of 0.1.11 PASSED.** Sheri tested a fresh 0.5.2 scaffold against the 0.1.11 dev client with prompts exercising every Tier 1+2 module — all work: `expo-image` (the ExpoAsset regression — confirmed gone), linear-gradient, font, image-picker, location, maps, video, audio, async-storage, flash-list, webview, camera. Permission-gated modules (photos/location/camera/mic) surfaced their iOS sheets, confirming the Info.plist usage strings shipped. Benchmark while validating: cold `proto start` ~23s to running (sim already booted; ~18s of that is the 75MB binary download), warm start ~5.7s; Tier 1+2 added ~1.7s one-time to the cold download (67.7→75.1MB). Only dark-mode palettes remain to eyeball.
- **2026-06-10 — P0 hotfix: 0.1.10 shipped without `ExpoAsset` → every fresh scaffold red-screened. Fixed in 0.1.11 + create-proto 0.5.2.**
  - **Symptom:** a fresh `npm create proto` scaffold crashed on first launch with `Cannot find native module 'ExpoAsset'` / `"main" has not been registered` (caught by the 0.1.10 scaffold smoke test's auto-launch).
  - **Root cause:** the Tier 1+2 bundle added `expo-image`, which calls `requireNativeModule('ExpoAsset')` on load. `expo-asset` was only a **transitive** dep (not direct in `apps/prototo-app`), and pnpm's symlinked `node_modules` hid its podspec from Expo autolinking — so `ExpoAsset` was never compiled into the 0.1.10 dev client.
  - **Fix (`96c8843`):** added `expo-asset ~55.0.17` as an explicit dep of `apps/prototo-app` + the scaffold template (so it autolinks), bumped dev client → **0.1.11**, rebuilt via `release:all` (device `97636931`, sim `be3b066a`, sim release `prototo-sim-sdk55-6`). create-proto → **0.5.2** (`ab62738`) so the template pins the matching version. proto-cli unchanged (0.5.1).
  - **Binary-verified, not assumed:** 0.1.10's `Prototo.debug.dylib` had **0** `ExpoAsset`/`AssetModule` references; 0.1.11 has **12**/**6**. Live manifest now lists `expo-asset` (35 modules). All install paths fixed — sim auto-updates via sha on next `proto start`; device build 0.1.11 at the install URL (re-install needed); 0.5.1+ scaffolds get the native module from the rebuilt dev client.
  - **Process note:** a concurrent `pnpm install` race (this session + the paused App-Clip task on the shared working tree) briefly corrupted the lockfile mid-fix; resolved by killing all installs and re-installing single-threaded on `main`.
- **2026-06-10 — App Clip sharing architecture: decided + Phase A/B landed.** Sharing pivots from the tunnel + JS-bundle viewer (2.5.2-blocked at App Store review) to a **manifest renderer**: TSX → declarative JSON manifest → fixed native App Clip + parent app, no code over the wire. Decision brief: `docs/prototo-app-clip-handoff.md`. Spec: `docs/superpowers/specs/2026-06-10-app-clip-sharing-architecture-design.md`.
  - **Phase A — manifest contract (landed):** new `packages/proto-manifest/` package — TS types (`src/types.ts`), JSON Schema (`schema/manifest.schema.json`), `validateManifest` (structural via zod + referential: initialScreen/navigate-target/state-key integrity), and compile-target fixtures for all six `new-screen` templates + an interactive one. Human spec: `docs/MANIFEST.md`. **19 tests green** (TDD red→green), typecheck + Biome clean, builds. v1 = nine node types (1:1 with the component library) + six enumerated actions (`navigate`/`dismiss`/`setState`/`toggleState`/`showModal`/`hideModal`). Reconciliation: the shipped `Screen` ignores `title`; the manifest treats it as the nav title.
  - **Phase B — generation constraints (landed):** root `CLAUDE.md` §1 gains "manifest-expressible by default" + "graduate constraint" invariants; `packages/create-proto/template/CLAUDE.md` gains a designer-facing "two fidelity tiers (local vs shared)" section. Both point at `docs/MANIFEST.md`.
  - **Phase C — compile-from-TSX (landed):** new `packages/proto-compile/` package — a `ts-morph` walker turning a screen `.tsx` into a validated manifest. `compileScreen(source)` → ScreenNode; `compileManifest(screens, config)` → full validated `Manifest`. **15 tests green** (TDD red→green): the six template `.tsx` fixtures compile to their `proto-manifest` JSON fixtures, `compileManifest` round-trips to `empty.json`, and out-of-schema input fails loudly. Gate rule: a *shareable* screen imports **only** from the proto barrel — so all current interaction/state patterns (which need `expo-router`/`react`) are correctly rejected as local-only. **Decision surfaced, not yet made:** there's no TSX *authoring surface* that compiles to Phase A's action set (`navigate`/`toggleState`/etc.) without rejected imports — needs a shareable-interaction affordance in the component library (e.g. an enumerated `onTap="navigate:Detail"` prop or a `<Link>` component). Until then the shared tier is static-only.
  - **Doc sweep:** `proto-master.md` §8 banner-superseded; `RISKS.md` — `AS-01`/`AS-04`/`SH-05`/`SH-06` updated to the manifest path, added `AS-05` (UGC-edge rejection), `AS-06` (clip bundle-size budget / SwiftUI-vs-RN), `XX-06` (permanent schema ceiling). `AS-04`'s deferred SDUI path is now **re-engaged**.
- **2026-06-10 — shipped the Tier 1+2 native bundle: Prototo App 0.1.10 built + sim-released, proto-cli/create-proto 0.5.1 published.**
  - `release:all` built the device (`0ec612bc`, internal dist) and simulator (`cb1535c6`) dev-clients at 0.1.10 and shipped GitHub sim release `prototo-sim-sdk55-5` (+ moved the `prototo-sim-sdk55-latest` pointer). The live manifest now carries `nativeModules` (34 entries) — this **activates the 3a native-detection** for designers. Published `@sherizan/proto-cli` + `create-proto` 0.5.1 to npm via `pnpm publish` (verified: no `workspace:` leak, dep rewritten to `^0.5.0`, both `latest`).
  - **Dropped `expo-notifications` from the bundle (commit `a425cbe`).** The first `release:all` device build *failed* at the fastlane/Xcode step — `expo-notifications` injects the `aps-environment` entitlement, which needs the Push Notifications capability on the App ID + a regenerated provisioning profile that EAS can't create in a non-interactive build. Real push also needs APNs + a server (out of prototype scope). Removed from the dev-client + the scaffold template (12 of the 13 modules remain — image, image-picker, video, audio, camera, maps, location, font, linear-gradient, async-storage, flash-list, webview); lockfile updated, tests stayed green (proto-cli 194, create-proto 57). Rationale recorded in `docs/native-bundle.md`. Version stayed 0.1.10 (the failed build never shipped). Re-add path documented if local/scheduled notifications are ever needed (budget a one-time interactive `eas credentials` capability-sync).
  - **Device designers must re-install** from `prototo.app/install/ios` to get 0.1.10 (auto-update only triggers on a version bump — this is one, so it's distinct). Simulator auto-updates via sha256 on next `proto start`.
- **2026-06-09 — batteries-included native bundle (Tier 1 + Tier 2) + proto-cli 0.5.1, committed to `main` — *shipped 2026-06-10, see above.***
  - **Bundle (dev-client 0.1.9 → 0.1.10):** added 13 native modules to `apps/prototo-app` + the template at SDK-55 pins — `expo-image`, `expo-linear-gradient`, `expo-font`, `expo-image-picker`, `expo-location`, `expo-maps` (Tier 1); `expo-video`, `expo-audio`, `@react-native-async-storage/async-storage`, `@shopify/flash-list`, `react-native-webview`, `expo-camera`, `expo-notifications` (Tier 2). iOS permission strings (location/photos/camera/mic) added to `app.json` infoPlist; `expo-maps` needs no plugin for Apple Maps. `expo-font` also removes the ombak Google-Fonts cascade. Candidate list/tradeoffs: `docs/native-bundle.md`. Verified all 13 resolve at SDK-55 versions with `expo@55.0.26` (lockfile committed) — but the EAS build is the real validation. Commit `456cc22`.
  - **proto-cli 0.5.1 (`3727ff4`):** `proto add` now surfaces the unsupported-native warning at add time (was only on next `proto start` — the default warnFn didn't forward a logger). create-proto → 0.5.1 (template deps changed).
  - **Release sequence (not yet done):** `cd apps/prototo-app && pnpm release:all` (builds device+sim 0.1.10 with the new modules + emits the updated `nativeModules` manifest) → `pnpm publish` proto-cli 0.5.1 + create-proto 0.5.1 (pnpm, not npm). Then scaffold-smoke + on-device check (image/maps/location/font).
- **2026-06-09 — docs converged to a four-doc model + Phase 4 (polish) landed** (branch `prototo-eval-fixes`).
  - **Docs convergence:** added `CHANGELOG.md` (designer-facing release notes — the single source the website's `update-changelog` skill now reads, instead of reconstructing from raw commits) and `BACKLOG.md` (future/idea backlog, moved out of STATUS "Next up"). STATUS header now states the four-doc model: past=CHANGELOG / present=STATUS / future=BACKLOG / risks=RISKS. Rewired `/Users/sherizan/Public/prototo-website/.claude/skills/update-changelog/SKILL.md` to mirror dated entries from proto's CHANGELOG (commit-reconstruction kept only as a fallback appendix).
  - **Phase 4a (mock→real pattern):** added a blessed fetch + loading + error shape + a curated keyless-API list to `template/CLAUDE.md` (builds on the `mock()` helper). Documentation, no new primitive — RN data fetching is bespoke per screen.
  - **Phase 4b (dark mode / dynamic type / a11y):** added dark token variants (`liquidGlassDark`/`materialYouDark`/`baseDark`) to `proto-components/src/tokens/*`; `useTheme()` is now a reactive hook (`useColorScheme`) that resolves light/dark and honours a new `proto.config.js` `colorScheme` override. Light token objects unchanged, so the Phase 2 mirror + sync test stay green. Added `a11y` constants (`minTapTarget` 44, contrast floors) and a "Light, dark, and accessibility" section to `template/CLAUDE.md`. Dynamic Type already works (RN `Text` default); documented. **Dark palettes need an on-device visual pass — can't validate colors here.**
  - **Local verification pass (test agent) found + fixed two real issues:**
    - **`proto add` ERESOLVE (the eval's existential #1, now fixed).** A fresh scaffold let `react-dom` float to 19.2.7 (pulled as expo-router's optional peer) while `react` is pinned 19.2.0 — react-dom@19.2.7 demands `react@^19.2.7`, so `expo install` / `proto add` died with ERESOLVE. `expo install` does NOT resolve this on its own (my earlier 3c assumption was wrong). Fix: pin `react-dom: 19.2.0` (SDK-matched) in `template/package.json`. Verified end-to-end: `proto add date-fns` now succeeds in a real scaffold (`date-fns ^4.4.0` added, exit 0).
    - **Test files leaked into scaffolds.** `proto-components/src/mock.test.ts` (added in Phase 0c) was synced into `template/components/proto`, importing `vitest` which the scaffold doesn't have → a stray `tsc` error in designers' projects. Fix: `sync-template.ts` now excludes `*.test.ts(x)`. Added `template-integrity.test.ts` guarding both fixes (no test files vendored; react-dom pinned == react).
    - **Dark mode confirmed on-device:** test agent captured light/dark Simulator screenshots (`/tmp/shot-light.png`, `/tmp/shot-dark.png`) — colors visibly flip. SVG-on-device + native-detection warning remain correctly deferred to the EAS rebuild.
  - Verified: proto-cli 193 tests / 0 tsc errors; create-proto 57 / 0; proto-components 3 known pre-existing errors only.
- **2026-06-08 — ombak-evaluation fix plan, Phases 0–2 + 3b landed on branch `prototo-eval-fixes` (not committed/published/built yet).** Driven by `playground/waaave/PROTOTO-NOTES.md`. Plan file: `~/.claude/plans/users-sherizan-public-playground-waaave-frolicking-wilkinson.md`.
  - **Phase 0 (scaffold consistency):** expanded the `proto/gestures` barrel (`Animated` default, `SharedValue` type, `interpolateColor`) and rewired starter `template/screens/Home.tsx` to route reanimated through `proto/gestures` — fixes the self-contradiction at its source. Switched the edit rule to *targeted edits* in `template/CLAUDE.md` and repo invariant `CLAUDE.md §1`. Added a `mock()` identity helper (`proto-components/src/mock.ts` + test) and a "Mock vs real data" convention (function, not comment — generated screens stay comment-free).
  - **Phase 1 (screenshot loop):** new `proto shot` command (`proto-cli/src/commands/shot.ts` + 3 tests) → `xcrun simctl io booted screenshot` to `.proto/last-shot.png` (gitignored); wired into `cli.ts` dispatch + help; "Check your work visually" loop instruction added to `template/CLAUDE.md`.
  - **Phase 2 (single token source):** `renderDesignDoc()` now reads `proto-cli/src/design-tokens.ts` (a CLI-local mirror of `proto-components/src/tokens/*`) instead of hardcoded PRESETS; `design-tokens.test.ts` asserts the mirror == the canonical tokens so drift fails CI. Made `base` a real token (`proto-components/src/tokens/base.ts` + `useTheme`/`ThemeName`) — previously `base` silently fell back to liquidGlass. DESIGN.md now emits real token values (e.g. card `#f3f5f8`), a `## Data` mock note, and `Nav` radius instead of the phantom `input` radius. "One palette, one place" guidance added to `template/CLAUDE.md`.
  - **Phase 3a (native-rebuild detection):** new `proto-cli/src/native-modules.ts` — `computeUnsupported` (pure) + `isNativeModule` (FS heuristic: `.podspec` / `expo-module.config.json` / `app.plugin.js`) + best-effort `warnUnsupportedNativeModules` that reads `manifest.nativeModules` and prints an honest "ask the Proto team to add it" message (designers can't self-rebuild). `Manifest` type gained optional `nativeModules?: string[]`; `release-simulator.ts` now emits it from `apps/prototo-app` deps. Wired into `proto start`. Silent no-op until a build ships the field (risk `XX-05`). 7 tests.
  - **Phase 3c (`proto add`):** new `proto add <pkg…>` (`proto-cli/src/commands/add.ts`) wraps `expo install` (SDK-correct versions + clean peers — the cure for ombak's `--legacy-peer-deps` pruning), translates failures to designer copy, then runs 3a detection on just-added packages. Wired into `cli.ts` + help; `template/CLAUDE.md` now tells Claude to use `proto add`, never raw npm/pnpm. 5 tests.
  - Spec: `docs/superpowers/specs/2026-06-09-native-detection-and-proto-add-design.md`. proto-cli now 193 tests / 0 tsc errors.
  - **Phase 3b (basic SVG):** added `react-native-svg@15.15.3` (SDK-55-pinned from `bundledNativeModules.json`) to the dev-client (`apps/prototo-app`), the template, and a new `proto/svg` subpath (`proto-components/src/svg/`, wired into exports + sync + the proto library descriptor + `template/CLAUDE.md`). Template `metro.config.js` now uses `react-native-svg-transformer` so `import Logo from './x.svg'` works. **Deferred (builds later):** EAS dev-client rebuild (`release:all`) to bundle the svg native module, and npm republish of create-proto/proto-cli.
  - Verified: proto-cli 181 tests + 0 tsc errors; create-proto 55 tests + 0 tsc errors; proto-components 2 mock tests + the same 3 pre-existing Card/Toggle tsc errors (no new ones).

- **2026-05-29 — hid all user-facing mentions of the paused `proto share` feature.** Share/live-link is paused, so removed its advertisements: the `share` line from `proto`'s `help` text (`proto-cli/src/cli.ts`), the "Run `proto share`…" line from the scaffold `template/README.md`, the **Share** bullet from `template/CLAUDE.md`, and the "To share a live link…" line from create-proto's stop message (`howToRestart`). The `proto share` command, its implementation (`commands/share.ts`, tunnel, share-api, cloudflared), and all share tests remain intact and runnable — only the copy is hidden, so it's a one-line re-add when share resumes. Bumped proto-cli 0.4.2 → 0.4.3 and create-proto 0.4.4 → 0.4.5.
- **2026-05-29 — fixed `workspace:` protocol leak in published create-proto (0.4.4).** 0.4.3 was published with plain `npm publish`, which does NOT rewrite pnpm's `workspace:^0.4.2` dependency protocol, so `npm create proto@latest` died with `EUNSUPPORTEDPROTOCOL Unsupported URL Type "workspace:"`. 0.4.2 had worked because it was published via pnpm. Fix: republished as 0.4.4 via `pnpm publish` (rewrites `workspace:^0.4.2` → `^0.4.2`), deprecated the broken 0.4.3, and added a `guard:pnpm` prepublishOnly check that fails fast if create-proto is ever published via npm instead of pnpm. **Always publish workspace packages with `pnpm publish`.**
- **2026-05-28 — fixed `expo prebuild` Jimp CRC crash on fresh scaffolds (issue #5).** The template's placeholder `assets/icon.png` and `splash.png` were 70-byte 1×1 PNGs with a corrupt `IDAT` chunk CRC (stored `0xbbcb3b6e`, actual `0x694f56d9`). `file`/Metro don't validate CRCs so they looked fine, but Jimp inside Expo's `withIosDangerousBaseMod` does — every fresh `npm create proto` died at prebuild. Replaced both with valid 1024×1024 solids (icon `#000000`, splash `#FFFFFF`). Added `src/template-assets.test.ts` guarding chunk CRCs on every template PNG. Verified end-to-end: scaffolded a temp app from the new assets, `expo prebuild --clean --platform ios` finishes and generates the 1024² iOS AppIcon. `create-proto` bumped 0.4.2 → 0.4.3.
- **2026-05-27 — proto-cli `exports` map regression fixed.** The `exports` map added in 0.4.1 (subpaths for `./design` + `./design-libraries`) inadvertently gated all deep paths, so `create-proto`'s `resolveProtoCli` (which does `req.resolve('@sherizan/proto-cli/dist/index.js')`) returned null and designers saw `"Couldn't find proto-cli. Run manually: cd myapp && npx proto start"` on every fresh scaffold. Fix: added `"."` main entry, `"./dist/*"` wildcard, and `"./package.json"` to the exports map. Both packages bumped to 0.4.2 and republished. Verified via `node -e` resolution test + end-to-end scaffold spawn.
- **2026-05-27 — motion + gestures + lottie + canvas primitives shipped end-to-end.** Four subpath modules in `packages/proto-components/src/{motion,gestures,lottie,canvas}/`, mirrored into template via auto-sync. Prototo App 0.1.8 bundles `react-native-ease`, `lottie-react-native`, `@shopify/react-native-skia`, plus the existing reanimated 4.2.1 + gesture-handler. Commits: `bd2c388` (feature), `80d30dd` (versions), `f1c3b8c` (IPA-size doc), `d4b76b8` (DESIGN.md auto-population), `b83f2f8` (release:all + DC-09 risk), `6a4d285` (README), `326d7dc` (demo video).
- **2026-05-27 — DESIGN.md auto-populates on scaffold.** `create-proto` now calls `@sherizan/proto-cli/design`'s `renderDesignDoc()` after copying the template; fresh `npm create proto@latest` produces a fully-populated DESIGN.md including the four subpath lines. Static `template/DESIGN.md` removed in favour of the generator. TDD'd with 4 tests.
- **2026-05-27 — `release:all` script added** to `apps/prototo-app/package.json`. Chains `build:ios` + `build:ios:sim` + `release:simulator`. Use this for every Prototo App release so device + simulator channels stay in lockstep.
- **2026-05-27 — README.md refreshed** with a "Motion & graphics" section explaining the four subpath modules + four example prompts, plus the new YouTube demo URL (`https://youtu.be/_srHicLflms`).

## In progress

- **expo-sensors — BUILT + RELEASED into the 1.1.0 cycle (2026-09-19, `sherizan/prototo-shared#8`).** Dev client bundles `expo-sensors ~57.0.3` + its config plugin (designer-facing `motionPermission`); app.json stays **1.1.0** (Sheri removed build 37 from review; build 38 uploaded, see Currently live), package.json 0.2.5. Sim released as `prototo-sim-sdk57-2` (`-latest` moved; install+launch verified on a dedicated sim; `sim-e2e.sh` NOT run — needs a Supabase test session, same as the build 37 cut; link routing is untouched by a module add). create-proto 0.8.0's template now carries `expo-sensors` + an AGENTS.md paragraph (guard with `isAvailableAsync()`, the sim never fires motion, feel it via `proto share` → Prototo app on iPhone); still HELD for the flip. **Remaining:** Apple approves build 38 (waiting for review since 2026-09-19) → flip hour (unchanged choreography) → docs pass #13 (now includes motion sensors) → real-iPhone acceptance: scaffold + `npx proto add expo-sensors` (warning already clears on 57 scaffolds) + tilt screen + `proto share` + open in the Viewer.
- **NEXT — the SDK 57 flip hour (waiting on Apple's review of Viewer 1.1.0 build 37).** On approval, same hour, in this order: (1) `prototo-website`: `lib/runtime.ts` `CURRENT_RUNTIME` → `{ version: "prototo-57", expoMajor: 57 }` + add the CHANGELOG "Unreleased" block (`docs/CHANGELOG.md`) to `lib/releases.ts` as the top dated entry → `pnpm lint` + `tsc` → push `main`; (2) `packages/create-proto`: `pnpm publish --no-git-checks --otp=<code>` (0.8.0 is already bumped; Sheri's OTP); (3) proto: move CHANGELOG Unreleased under a dated heading, flip the Currently-live table, `git tag sdk-57`, merge/push `chore/sdk-57-upgrade`; prototo-shared push. From then on: `proto start` nudges, `proto upgrade` migrates (desktop does it automatically on open), old shares email their owner on a stale open. **Not yet verified end-to-end (needs a real `prototo-56` share on the 57 Viewer + the switch at 57):** the Viewer stale screen copy + the stale-share email — do one manual check right after the flip.

> **⚠️ HISTORICAL as of 2026-07-04 — the App Store native-runtime viewer this section tracked SHIPPED** (v1.0 build 23, submitted + in review — see "Currently live" + the dated log at the bottom, 2026-06-22→07-04). Kept below as the engineering record of the fork.
>
> **Native runtime, condensed:** own the native host via `plugins/withPrototoNativeHost` (custom `AppDelegate` mounts our RN shell as root; Obj-C `ProtoNativeLoader` shim drives `loadApp`/`goHome`/`reload` through `EXDevLauncherController` + expo-updates `UpdatesDevLauncherInterface`). Launcher-free Release needs **3 `pnpm` patches** (`DC-13`, re-validate every SDK bump): drop `apple.debugOnly` on expo-dev-launcher + expo-dev-menu, and build expo-updates from source with `USE_DEV_CLIENT` + the dev-launcher branch forced in Release. Ship with `developmentClient:false` (APP_DEBUG off → the launcher react-delegate no-ops → our shell owns root). `goHome` = `updatesInterface.reset()` + a **fresh `ExpoReactNativeFactory` per mount** (recreateRootView reused the prototype's host). Proven on-device (builds 12/13) → App-Store-submitted (20→23).
>
> **Follow-ups:** SDK-bump re-validation of the 3 patches; keep the pinned sample published (EAS group `6d42487e`, runtime `prototo-56`); the Appetize web-stream is now a *fallback* for non-installed recipients (`SH-09` — native runs are unmetered).

- **App Store iOS viewer — design spec approved (2026-06-19), implementation plan next.** Brainstormed + speced the unified Expo-Go-style App Store app (`docs/superpowers/specs/2026-06-19-ios-viewer-app-store-design.md`). Decisions: one binary (graduate `apps/prototo-app`, not a new app) serving both designers (local-Metro dev) and recipients (run published share bundles natively on-device); recipient entry via **smart universal link + web-stream fallback** on `prototo.app/p/<token>`; **sign-in required** for recipients (reuses Supabase). Central risk = Apple **2.5.2** (native loader; Expo Go precedent) — hedged by the staged submission + the web-stream fallback. New risks logged: `AS-07`/`AS-08` (2.5.2 + UGC), `SH-09` (native runs are unmetered → pricing shift), `DC-10` (Sign in with Apple), `SH-10` (AASA). **Phase 1 implementation plan written:** `docs/superpowers/plans/2026-06-20-ios-viewer-phase-1-launch-ui-auth.md` — branded auth-gated launch UI in `apps/prototo-app` (Supabase RN client + AuthProvider + Sign in with Apple; email/Google deferred to Phase 1b since proto-components has no text input yet). 7 tasks, TDD on the pure Apple helpers, on-device validation for the RN UI + a Sheri-run Apple/Supabase dashboard config step. **Phase 1 code COMPLETE on branch `feat/ios-viewer-phase-1-auth`** (10 commits): deps + app.json (`usesAppleSignIn`, plugin) + `.env.example`; `lib/{apple-auth(+test),supabase,auth-context}`; `components/{SignInScreen,HomeScreen}`; auth-gated `app/{_layout,index}`. Two hardenings beyond the plan: nonce-bound Apple sign-in (replay guard) + simplified signOut. **Verified:** 6/6 apple-auth unit tests green; tsc clean for all new files (only the 3 pre-existing proto-components type errors remain). **Task 7 DONE — Phase 1 VERIFIED ON A PHYSICAL DEVICE (2026-06-20, "Sherizan 17" / iOS 26.5.1):** full 5-point auth loop works — sign-in screen → Sign in with Apple (native sheet) → Home greeting by name → session persists across kill/relaunch → sign out. Supabase Apple provider enabled (`com.sherizan.prototo` in Authorized Client IDs); Sign-in-with-Apple capability enabled on the App ID; `.env` set with the anon key. **Bring-up gotchas (carry forward for the production build):** (1) `usesAppleSignIn` adds the entitlement, but the **App ID must have the Sign-in-with-Apple capability enabled** or the device build fails signing (error 65 — simulator doesn't care, device does); (2) `EXPO_PUBLIC_*` vars are **inlined when Metro bundles JS**, not at native build — a missing/empty `.env` means an empty Supabase client; fix is a Metro restart, not a rebuild; (3) physical-device dev build needs Developer Mode on iOS + device online/trusted. **Phase 1 PR open: #31** (`feat/ios-viewer-phase-1-auth` → main). **Phase 2 design + plan DONE (2026-06-20):** branded in-app **QR scanner** for "Connect to your Mac" — design decided (no programmatic loadApp API in expo-dev-client; the only mechanism is the `prototo://expo-development-client/?url=…` deep link, which the `proto start` QR already encodes; app already bundles expo-camera + expo-linking). Plan: `docs/superpowers/plans/2026-06-20-ios-viewer-phase-2-connect-to-mac.md` — `lib/connect-url`(+TDD), `app/connect.tsx` scanner route (camera-permission states + CameraView), wire HomeScreen card; plus a reversible boot-behavior (`launchMode`) investigation deferred-able to Phase 5. Branches off the Phase 1 branch. **Phase 2 code COMPLETE on branch `feat/ios-viewer-phase-2-connect`** (6 commits, subagent-driven): `lib/connect-url`(+TDD), `app/connect.tsx` scanner (camera-permission states + CameraView), HomeScreen "Scan QR code" wiring, expo-linking stub. **Reviewed + hardened:** code-quality review caught two scanner bugs (per-frame `setError` churn → `errorShown` ref; permanent lockout if `Linking.openURL` fails → await + reset guard) — both fixed. **Security (`DC-11`, HIGH, mitigated):** the scanner is a code-execution sink (QR → dev client loads+runs JS from the URL); `parseConnectUrl` now re-extracts the inner `url=` and accepts **only loopback/RFC-1918/link-local hosts**, blocking the public-internet RCE vector by construction (tested). Deferred defense-in-depth: shared-secret pairing for same-LAN, `.local`/IPv6 hosts — see `RISKS.md DC-11`. **18/18 app tests green, tsc clean.** **Task 5 DONE — Phase 2 VERIFIED ON-DEVICE (2026-06-20):** Home → Scan QR → camera → scanning the `proto start` QR loads the live prototype. Phase 2 complete on `feat/ios-viewer-phase-2-connect` (branches off Phase 1; rebase onto main after #31 merges). (`launchMode` boot-into-shell experiment still optional/deferred to Phase 5.) **Phase 2 PR open: #32** (stacked on #31). **Phase 3 design + plan DONE (2026-06-20):** recipient native-run — `prototo.app/p/<token>` universal link → app (AASA already routes `/p/*`, app-side only) → Phase 1 sign-in gate → anonymous `GET /api/share/<token>` → validate deepLink (central-project pin, `DC-11` mindset) → `Linking.openURL` loads the EAS-Update bundle on-device. Decisions: **reactive** runtime gate (defer the proactive `runtimeVersion` field — impossible to mismatch on single `prototo-56` runtime today) + **no metering** (native runs are free, `SH-09`). Plan: `docs/superpowers/plans/2026-06-20-ios-viewer-phase-3-recipient-run.md` — `lib/share-lookup`(+TDD: validator + fetch), `app/p/[token].tsx` route; one on-device routing unknown to resolve (does expo-router match the https origin, or need a `useURL()` handler in `_layout`). Branches off Phase 2. **Phase 3 code COMPLETE on branch `feat/ios-viewer-phase-3-recipient-run`** (3 commits, subagent-driven): `lib/share-lookup`(+TDD: `isValidShareDeepLink` central-project pin + `fetchShare` never-throws), `app/p/[token].tsx` route (sign-in gate via Phase 1 `useAuth`/`SignInScreen` → resolve → validate → `Linking.openURL`). **Reviewed:** security path confirmed airtight (openURL unreachable without the `$`-anchored central-project validation); review fixes applied — double-open race (claim `opened` ref before await), stale error UI reset on re-entry, distinct `invalid` copy. **25/25 app tests green, tsc clean.** **Remaining (Sheri — plan Task 3):** on-device verify (signed-out `prototo.app/p/<token>` → app → Apple sign-in → prototype runs natively; signed-in → direct; expired token → friendly copy; not-installed → web stream) AND resolve the one routing unknown (does expo-router match the https origin to `/p/[token]`, or need a `useURL()` handler in `_layout`). **Phase 3b (share-link entry points) ADDED (2026-06-20, same branch):** surfaced during device testing — a scanned share QR (`https://prototo.app/p/<token>`) was rejected by the Metro-only scanner, and "Open a link" was a dead Phase-1 placeholder. Fix: new `lib/share-link.ts` (+TDD, 31 tests total) `parseShareLink` (extracts/validates the 12-char token from a `prototo.app/p/<token>` URL or bare token; rejects other hosts); **unified scanner** (`app/connect.tsx` now routes a share QR → `/p/<token>`, else Metro QR → openURL, keeping the `DC-11` LAN-host guard); **"Open a link"** reads the clipboard via `expo-clipboard` → `parseShareLink` → `/p/<token>` (no text-input component needed, per SH-09 decision). Three recipient entry points now: tap universal link, scan QR, copy+Open a link. **Phase 3 + 3b VERIFIED ON-DEVICE (2026-06-20):** all three recipient entry points reach the running prototype — tap universal link, scan share QR, copy+Open a link. **Routing unknown RESOLVED:** expo-router matches the `https://prototo.app/p/<token>` origin to the `/p/[token]` route on its own. **Phase 3 PR open: #33** (stacked on #32).

**Phase 4/5 (production build + App Store submission) — Part A DONE, Part B handed off (2026-06-20).** Plan/runbook: `docs/superpowers/plans/2026-06-20-ios-viewer-phase-4-5-app-store-submission.md`. **Crux:** the production build must keep the Expo dev-client runtime (it loads EAS Update + Metro) — so we ship the Expo Go model; **2.5.2 is the central review risk** (`AS-07`/`AS-08`), hedged by review-notes framing + Expo-Go precedent + TestFlight-first + the web-stream/EAS-Internal fallback (never blocked to zero). **Part A code DONE** on `feat/ios-viewer-phase-4-production` (`c21ffae`): `app.json` `launchMode` `launcher`→`most-recent` (cold launch → branded shell, not Expo's dev menu) + `version` `0.2.2`→`1.0.0`. 31/31 tests, tsc clean, valid JSON. **Part B (Sheri-run, runbook has exact commands + drafted metadata + 2.5.2 review notes):** (1) `eas build --profile production` — the never-built store-signed artifact; **set EAS env `EXPO_PUBLIC_SUPABASE_*` or sign-in silently fails**; (2) `eas submit` → internal TestFlight, validate the store binary runs the full set (cold-launch-into-shell, Apple sign-in, Connect, recipient-run); (3) mint a permanent sample share link for the reviewer; (4) App Store Connect metadata + privacy labels; (5) submit with phased release.

**Reminder — `EXPO_PUBLIC_*` env on EAS builds** (carry from Phase 1): the anon key is inlined at Metro bundle time; a production EAS build needs `EXPO_PUBLIC_SUPABASE_URL` + `EXPO_PUBLIC_SUPABASE_ANON_KEY` set as EAS env vars or the shipped Supabase client is empty and Apple sign-in fails.

**Stack:** #31 ← #32 ← #33 (recipient-run) all verified on the **dev-client TestFlight build**; that build is retained as the **designer / EAS-Internal channel** (keeps Connect-to-Mac).

**⚠️ PHASE 4/5 PIVOT (2026-06-20) — dev-client store build is a dead end; going standalone.** On TestFlight the production **dev-client** build cold-launches into **Expo's launcher** (off-brand + textbook 2.5.2 "enter a URL to load code"); `launchMode` can't fix it (a dev-client build's embedded experience *is* the launcher). Decision (with Sheri, full reasoning in `docs/superpowers/plans/2026-06-20-ios-viewer-standalone-pivot-spike.md`): the App Store app becomes a **standalone (non-dev-client) branded viewer** — full fidelity (Expo Go model, NOT the retired declarative renderer, NOT a Expo Go fork), 100% our UI + Supabase auth, loading shares via **`expo-updates` runtime override** instead of the `prototo://…` deep link. Connect-to-Mac stays in the dev-client/EAS-Internal build. **Spike abandoned before building (2026-06-20) — analysis killed it:** a standalone full-bundle-swap can't work for a multi-use viewer — once a prototype bundle loads, our JS stops running, so there's **no return-to-shell AND a 2nd tapped link can't be intercepted** (single JS runtime; the dev client only avoids this via its native launcher + dev-menu escape). `DC-12` resolved-by-avoidance.

**✅ SCOPE DECISION (2026-06-20) — focus local preview; recipients stay cloud-stream.** App Store app = the **designer local-preview tool** (Connect to your Mac / local Metro) — the cleanest 2.5.2 story (a dev tool previewing *your own* prototypes, Expo Go precedent), and the dev client's native job (dev menu = return). **Recipients remain on the live cloud stream** (full fidelity, zero 2.5.2). The native recipient-run (Phase 3) keeps working on the dev-client/EAS-Internal build but is no longer the App Store story. **This means the dev-client TestFlight build already validated *is* essentially the local-preview app.** Remaining for an App Store submission of it: decide how much of Expo's launcher to suppress/brand (`showMenuAtLaunch:false`, `skipOnboarding`, custom styling — acceptable-as-is for a Dev Tools app per Expo Go), then metadata + the dev-tool review notes (drafted in `…-phase-4-5-app-store-submission.md`) → submit as **Developer Tools**. **Banked:** Phases 1–3 (PRs #31/#32/#33) **merged to `main`** (`8e48e2f`).

**✅ LAUNCHER-UX SPIKE — PASS (2026-06-20).** Branch `feat/ios-viewer-launcher-ux` (`ff25498`): `expo-dev-client` config → `launchMode:most-recent` + `showMenuAtLaunch:false` + `skipOnboarding:true` + `toolsButton:false` (also `version 1.0.0` + production EAS-env pin). Built (`6e9c9566`) + on TestFlight: **the app now cold-launches into OUR Sign-in shell, not Expo's launcher.** Residual: a **sub-second Expo-launcher flash** before our shell mounts — inherent to `most-recent` (renders launcher → resolves most-recent → loads our embedded bundle); **no config fully removes it** (only the native custom-shell would, a large effort). **Decision (Sheri): accept the flash, proceed to submission** (sub-second, won't affect a Dev-Tools review). So the App Store app = the dev-client local-preview tool, our UX, Dev-Tools framing. (Branch merged to main `4066a18`.)

**🚩 BIG PIVOT (2026-06-21) — build our own NATIVE runtime (no Expo surfaces); ship dev-client is OFF the table.** Sheri's point: a reviewer seeing Expo's launcher + "sign in to Expo" auth = a 2.5.2 reject, not cosmetic — so the dev-client (even with cold-launch suppressed) can't go to the App Store; its in-session Expo menu/auth/go-home→launcher are unremovable in a managed dev-client build. Tried the cheap `defaultLaunchURL` probe to redirect "go home" → blocked by config-hygiene tangles (committed `ios/` entitlements + `app.json` both declare `applinks` → `eas update` duplicate error; `df9296f8` vs `8c8ddf7d` project mismatch — both logged as pre-req cleanup). **Native API audit is conclusive:** config-plugin/native-module CANNOT own the shell (`EXDevLauncherController.createRootViewController` hardcodes `DevLauncherViewController`; the ReactDelegateHandler wraps all RN roots — no hook). The ONLY path: **own the native host** (brownfield/integrated) — custom AppDelegate mounts OUR RN shell as root, drive the loader ourselves (`EXDevLauncherController.loadApp:` for Metro + EAS-Update bundles), own return via a native overlay-window "Home" + `loadLocalBundle` (not `navigateToLauncher`), kill Expo's shake menu (`setMotionGestureEnabled(false)`). Zero Expo surfaces → removes the 2.5.2 trigger. **Decision: do it right, flagship, take the time.** **Design spec: `docs/superpowers/specs/2026-06-21-native-prototo-runtime-design.md`** — build is **Phase-0 spike-first** (prove 3 crux unknowns on-device: own-the-root, drive-the-loader-into-our-VC, native-overlay-return) before the full build. New risk `DC-13` (drives private/alpha Expo internals; brownfield+dev-client unsupported; per-SDK-upgrade maintenance). Phased plan: `docs/superpowers/plans/2026-06-21-native-runtime-phase-0-spike.md`. Phases 1–3 (auth/Connect/recipient logic, on `main`) become the RN shell the host mounts; EAS-Internal + cloud-stream stay interim channels. Phase 1b (email/Google) still needs a proto-components text input.

**🛠️ TOOLING: XcodeBuildMCP wired (2026-06-21)** — `.mcp.json` adds `xcodebuild` (`npx xcodebuildmcp@latest mcp`). **Verified end-to-end:** I can build the sim, read captured runtime/os logs, and screenshot — so I drive the spike build→log→screenshot loop myself (sim, iPhone 17 Pro / iOS 26.5). Session defaults set (workspace `ios/Prototo.xcworkspace`, scheme `Prototo`, Release).

**🔬 PHASE-0 SPIKE PROGRESS (2026-06-21, branch `spike/native-runtime-phase-0`):**
- **Crux 1 (own the root) = PASS.** Release build cold-launches **straight into our Sign-in shell** — no Expo launcher, no account icon; rootVC = `ScreenOrientationViewController` (our normal root), not a DevLauncher VC. (The Expo launcher only appears in *Debug-without-Metro*.) So owning cold-launch is fine; the real fight is in-session.
- **KEY FINDING: `apps/prototo-app/ios/` is gitignored/generated** → the native shell **must be a config plugin** (`withAppDelegate`/`withDangerousMod`); direct `ios/` edits are wiped by `expo prebuild`. Resolves the `DC-14` committed-ios-vs-plugin question → **plugin**. (Spike edits `ios/` on disk + builds via MCP without prebuild; not git-committable — tracked in notes.)
- **Crux 2 (loadApp) — partially explored; key constraints found (2026-06-21):**
  - Calling `EXDevLauncherController` from Swift works via a **minimal bridging-header slice** (declare just `+sharedInstance`/`loadApp:onSuccess:onError:`/`loadLocalBundleOnSuccess:`/`navigateToLauncher`) — importing the full header drags `EXUpdatesInterface` into the PCH and fails; the slice avoids it (symbols resolve at link time). Useful technique for the plugin.
  - **BLOCKER → architecture-shaping:** in the current on-disk **Release** Pods, `EXDevLauncherController` is **not linked** (`Undefined symbol _OBJC_CLASS_$_EXDevLauncherController`). expo-dev-launcher linking is **build-config-dependent** — the production EAS build force-links it into Release via the `developmentClient` Podfile config; a plain xcodebuild Release on the current Pods doesn't. So `loadApp` (and the whole native host) can't be tested by ad-hoc `ios/` edits — it **requires the config-plugin + Podfile setup**. Debug links dev-launcher but its home IS the launcher (so it can't represent the Release "our shell as root" behavior), and Debug needs Metro (port 8081 was occupied by a `test3` playground).
  - **Conclusion:** crux 2/3 (loadApp-into-our-window, overlay return, kill menu) are **not cheap-spike-isolatable** — they live inside the real **config-plugin Release build** (inject our AppDelegate host + force-link dev-launcher in Release). The spike has delivered its value: tooling verified, crux 1 proven, and the build-config constraints (`ios/` gitignored → plugin; dev-launcher Release-linking → plugin/Podfile; bridging-slice technique) that shape that build. **Next real step = build the config plugin (spec Phase 1), then answer crux 2/3 there.** Spike branch deleted (throwaway; ios/ edits gitignored). **Phase-1 plan written: `docs/superpowers/plans/2026-06-21-native-runtime-phase-1-config-plugin.md`** — a `withPrototoNativeHost` config plugin (overwrites AppDelegate, adds overlay + bridging slice, force-links dev-launcher in Release), built/verified via the XcodeBuildMCP loop (prebuild → build_run_sim → logs + screenshot). 7 tasks; crux 2 (Task 3, loadApp-into-our-window) is the make-or-break NO-GO gate. Execution = executing-plans + Claude driving the MCP loop (not blind subagents); EAS production build + device steps are Sheri's.

**▶ PHASE-1 IN PROGRESS (2026-06-21, branch `feat/native-runtime-config-plugin`):**
- **Plugin built + applies (Task 0 ✓):** `plugins/withPrototoNativeHost/` copies our `AppDelegate.swift` + `PrototoDevLauncher-Bridging.h` (the minimal `EXDevLauncherController` slice), patches the bridging header, and adds `-ObjC` to the app target's Release `OTHER_LDFLAGS` via `withXcodeProject`. `expo prebuild` applies it cleanly. Wired in `app.json`.
- **pnpm fix:** added `@expo/config-plugins@56.0.8` as an app devDep — Expo's build-time "Configure project" phase re-evaluates the config (loading our plugin) and `require('@expo/config-plugins')` wasn't resolvable under pnpm.
- **Task 1 (force-link) blocker — PRECISELY IDENTIFIED:** in a **local prebuild Release** build, `expo-dev-launcher` **isn't linked into the app target at all** → `EXDevLauncherController` is an undefined symbol on arm64 even with `-ObjC` (which can only force-load *linked* libs). **This is NOT a production blocker** — the EAS `production` profile (`developmentClient: true`) force-links dev-launcher in Release (the TestFlight build proved it). It only blocks the *fast local Release MCP loop*. (`-ObjC` + bridging slice are correct and will work once the lib is linked.)
- **✅ CRUX 2 PROVEN (2026-06-21, the make-or-break) — `loadApp` mounts a real prototype in OUR window.** Path: build crux 2/3 in **Debug** (dev-launcher always linked), validate Release via EAS later. Solution that worked: a thin **Obj-C shim `ProtoNativeLoader`** (`.h`/`.m` in the plugin) that imports the real `EXDevLauncherController.h` *only in the .m* and exposes slim `+loadApp:`/`+goHome:` — this isolates the heavy header from the bridging PCH **and** avoids the cross-config "different definitions" conflict the inline bridging-slice hit in Debug. The shim `.m` is added to the target via `withXcodeProject.addSourceFile('Prototo/ProtoNativeLoader.m', …)` (project isn't a synchronized group). **On-device (Debug sim) result:** `loadApp(<EAS group URL>)` → `SUCCESS`, rootVC stayed `ScreenOrientationViewController` (NOT `DevLauncherViewController`), and the screenshot showed the real **test4** prototype rendering in our window — no Expo launcher. Committed.
- **Build-loop gotchas learned:** must `expo prebuild -p ios --clean` between plugin edits (plain prebuild reuses stale `ios/` files); `ONLY_ACTIVE_ARCH=YES` for sim (dev-launcher arm64-only locally); `@expo/config-plugins` must be a direct app devDep (pnpm) for the build-time config eval.
- **✅ CRUX 3 PROVEN (2026-06-21) — native overlay Home over the running prototype.** A small high-level `UIWindow` (`windowLevel = .alert+1`, built inline in AppDelegate so no extra target file) with our "⌂ Prototo" button **floats above the loaded prototype** — confirmed on-device by screenshot (the button sits over the running test4 prototype) and the "Home tapped" logs (touches reach it). Tapping → `ProtoNativeLoader.goHome()` → `loadLocalBundle`. Return needs `embeddedBundle: true` (now set in the `expo-dev-client` plugin config — precise error told us) + an embedded/Release build (Debug has no embedded bundle, so it errors there by design; returns properly in Release/EAS). Expo shake/touch menu disabled via Info.plist (`EXDevMenu*GestureEnabled=false`, set by the plugin). Committed.

**🎉 ALL THREE CRUXES PROVEN → native-runtime architecture is GO.** Own-the-root (1, Release), loadApp-prototype-into-our-window (2), native-overlay-Home (3) all validated on-device via the XcodeBuildMCP loop. The flagship "our own Expo Go, App-Store-clean" approach works. `DC-13` (private-internals risk) → de-risked; keep the shim thin + re-validate per SDK bump. **Remaining (engineering, not unknowns):**
- **(a) DONE (2026-06-21):** `ProtoNativeLoader` is now an **RCT module `PrototoRuntime`** (`loadPrototype`/`goHome`) — extracts the inner app URL from the `prototo://…?url=` deep link; keeps class methods for the overlay. Spike timer removed. JS `lib/native-runtime.ts` wraps it; `app/connect.tsx` (Metro) + `app/p/[token].tsx` (EAS share) now call `loadPrototype` instead of `Linking.openURL`. 31/31 JS tests green, tsc clean, builds + registers + overlay installs on the sim. Committed.
- **(b) DONE (2026-06-21):** overlay now **hidden on the shell, shown only over a running prototype** + branded `✕ Exit` pill (shadow, semibold). `ProtoNativeLoader` posts `ProtoPrototypeLoaded`/`ProtoReturnedHome`; AppDelegate toggles the overlay. **Crash fixed:** the toggle fires on the dev-launcher's background thread; mutating `UIWindow.isHidden` off-main crashed `EXC_BREAKPOINT` (UIWindow scene-attach barrier) — now dispatched to main. Verified on the sim (hidden on shell → `✕ Exit` floats over the test4 prototype, no crash). Committed.
- **UI cleanup DONE (2026-06-21):** consistent shell pass within proto-components/tokens — Sign-in (grouped brand/tagline + clearer copy), Home (greeting subtitle, card gap 12, divider before sign-out), recipient `/p` (title+body for loading/opening/error), and the native Exit pill clears the home indicator. 31/31 tests, tsc clean, builds. (Visual review of Home/Connect/recipient pending the on-device pass — only Sign-in is reachable without auth.)
- **⚠️ Behavioral finding:** `launchMode: "most-recent"` **re-opens the last-loaded prototype on cold launch** (not the shell). Recoverable via the Exit pill (it shows since a prototype is loaded). Decide for production whether cold launch should always be the shell (drop most-recent / clear recent on goHome) — UX follow-up.
- **(c)** validate the full loop incl. `goHome` return on a real **EAS production (Release)** build + device (Debug can't exercise the embedded-bundle return; live JS→native loadPrototype needs Metro/our-shell or the EAS build).
- **(d) DONE/mitigated (2026-06-21):** `DC-14` config hygiene — `associatedDomains` duplicate was transient (resolved config has one entry; `ios/` is generated, no committed entitlements); the `df9296f8`(build) vs `8c8ddf7d`(updates) split is accepted/by-design (app reads updates from the central share project; never `eas update`-d). No code change; revisit `updates.url`→`df9296f8` + cross-project `loadApp` during the device pass. See `RISKS.md DC-14`.
- **✅ EAS PRODUCTION BUILD SUCCEEDED (2026-06-21, build `74bf760c`)** via the `production` profile on `feat/native-runtime-config-plugin` — **the `withPrototoNativeHost` plugin works through the real EAS prebuild + Release build** (force-linked dev-launcher, shim, overlay, `embeddedBundle`, all in the production `.ipa`). **Submitted to TestFlight (build 11).** PR **#34** open (native runtime → main).

**❌ DEVICE TEST FOUND CRUX 1 IS NOT SOLVED (2026-06-21).** On a fresh device install, build 11 cold-launches into **Expo's dev-launcher** ("Finding Dev Servers" → launcher), NOT our shell. The loader works (scanning a QR loads the prototype), and our `AppDelegate` host IS applied — but the dev-launcher's `ExpoDevLauncherReactDelegateHandler` still **owns the window root**; our `startReactNative("main")` doesn't displace it. No config fixes it (`launchMode` is only most-recent|launcher; fresh install → launcher). **Earlier "crux 1 PASS" was a flawed sim observation** (onboarding pre-dismissed + `most-recent` auto-loaded a prototype masked the true fresh cold-launch; the `rootVC=ScreenOrientationViewController` log was ambiguous since it wraps either). So the App-Store-clean "no Expo launcher" goal is **unmet** — build 11 is NOT submittable. **Fix paths (genuinely hard):** (A) stop the dev-launcher owning the root (disable its react-delegate/subscriber, mount our shell, call `loadApp` on demand — fork-ish, `DC-13`, uncertain loadApp survives); (B) `defaultLaunchURL`→hosted shell (network-dependent/offline-broken); (C) step back to cloud-stream + EAS-Internal (already work, no 2.5.2 fight). **Validate any fix on a FRESH device/erased sim** (warm sim masks it).

**✅ FORK GATE 1 PASSED (2026-06-21) — chose (A); the debugOnly patch unlocks it.** `pnpm patch` removes `apple.debugOnly` from `expo-dev-launcher` + `expo-dev-menu` module configs → autolinking includes them in **Release**. On a **fresh-erased sim, true Release build**: `EXDevLauncherController` **links** (undefined-symbol gone) AND `ExpoDevLauncherReactDelegateHandler` no-ops (`return nil` when `!APP_DEBUG`) → **our Sign-in shell owns the root, NO Expo launcher** (screenshot-confirmed, with the polished copy). Patches committed (`patches/`, reproducible on EAS via `pnpm.patchedDependencies`). **REMAINING FORK WORK (gate 2) — loadApp mounting in Release (FULLY MAPPED, ready to implement):** the stock mount (`_initAppWithUrl` → `self.delegate.didStartWithSuccess`) is a no-op in Release (delegate nil; handler inert). And **do NOT call `EXDevLauncherController.start:`** — it sees our embedded `main.jsbundle` and calls `navigateToLauncher` (relaunches the launcher). Recipe:
  1. **At launch**, wire updates (Swift, `import EXUpdatesInterface`): `if let c = UpdatesControllerRegistry.sharedInstance.controller as? UpdatesDevLauncherInterface { EXDevLauncherController.sharedInstance().updatesInterface = c; c.updatesExternalInterfaceDelegate = EXDevLauncherController.sharedInstance() }` — **required** or EAS-update `loadApp` errors ("expo-updates not properly installed").
  2. **Load**: `controller.loadApp(url, onSuccess: { mount() }, onError:)` (delegate stays nil → its internal mount no-ops, harmless).
  3. **mount()**: `let rv = reactNativeFactory.recreateRootView(withBundleURL: controller.sourceUrl(), moduleName: "main", initialProps: nil, launchOptions: controller.getLaunchOptions()); controller.setRootView(rv, toRootViewController: window.rootViewController)`.
  4. **goHome()**: same mount with the embedded bundle URL (`Bundle.main.url(forResource:"main", withExtension:"jsbundle")`) — or `loadLocalBundleOnSuccess` then mount.
  Needs a new Swift integration file (EXUpdatesInterface Swift + EXDevLauncherController via bridging header — should import cleanly now that dev-launcher links in Release + factory ref from AppDelegate). Then re-test full loop on a FRESH sim, then EAS Release with `developmentClient:` **false** (NOT true — true = APP_DEBUG → launcher).

**GATE 2 — STARTED 2026-06-21; the integration code is written + COMPILES in Release, but it surfaced that the updates dev-launcher path is debug-gated at MULTIPLE layers (the fork is deeper than gate 1's one flag).** Implemented: shim `setUpdatesInterface`/`sourceUrl`/`launchOptions`; AppDelegate wires `UpdatesControllerRegistry.sharedInstance.controller` + mounts via `(factory as ExpoReactNativeFactoryProtocol).recreateRootView(...)` (recreateRootView is on the protocol, not RCTReactNativeFactory; needs `internal import ExpoModulesCore`). **On the fresh-sim Release run:** `updatesInterface: no updates controller available` → `loadApp` CRASHED. Root cause = **expo-updates only creates the `DevLauncherAppController` (the one conforming to `UpdatesDevLauncherInterface`) under debug gates**: `AppController.swift:217` runtime `if EXAppDefines.APP_DEBUG && !isNativeDebugging` **AND** compile-time `#if USE_DEV_CLIENT`, where `USE_DEV_CLIENT` is defined **only `[config=*Debug*]`** (EXUpdates xcconfig). In Release the dev-launcher branch is compiled out → registry controller is the normal/disabled one → no `UpdatesDevLauncherInterface` → loadApp can't fetch EAS updates.
  **So gate 2 needs 3+ MORE patches across expo-updates** (in addition to gate-1's dev-launcher/dev-menu `debugOnly`): (i) define `USE_DEV_CLIENT` in **Release** for EXUpdates (Podfile post_install / config-plugin build-setting patch — it's generated); (ii) patch `AppController.swift:217` to take the dev-launcher branch in Release; (iii) then re-test loadApp → mount. **Total fork now spans 3 Expo packages debug-gated at link + compile-flag + runtime layers** — materially more fragile/maintenance than gate 1 implied; re-broken every SDK bump. **✅ GATE 2 PROVEN (2026-06-21) — loadApp works in a launcher-free Release build.** Pushed through all the debug-gates with a 3rd `pnpm patch` (expo-updates): build from source (not the prebuilt xcframework), define `USE_DEV_CLIENT` in Release, and take the dev-launcher branch in `AppController.swift:217` (Release) → `UpdatesControllerRegistry` yields a `UpdatesDevLauncherInterface`. AppDelegate wires `updatesInterface` at launch + mounts via `(factory as ExpoReactNativeFactoryProtocol).recreateRootView(...)`. **On fresh-erased sim, true Release (APP_DEBUG false):** cold-launch our shell (no launcher) → `loadApp` **fetches + mounts the real test4 EAS prototype in our window** (screenshot-confirmed) → ✕ Exit overlay. Committed. **The whole App-Store-clean native runtime is proven end-to-end except one bug.**
- **🐛 REMAINING: goHome return-render (3 fixes tried, still open).** `goHome` → `loadLocalBundle` succeeds, we mount the **embedded** `Prototo.app/main.jsbundle` (logged; can't use `controller.sourceUrl()` for return — it prefers the last update's `launchAssetURL`), Exit overlay hides — BUT the screen still shows the stale prototype. **Tried & ruled out:** (1) fresh container VC + `window.rootViewController = vc` (not a view-swap issue); (2) `setValue(nil, forKey:"_reactHost")` ivar reset; (3) `setValue(nil, forKey:"reactHost")` setter reset (not the host-reset-key). **Diagnosis:** `recreateRootView(withBundleURL: embedded)` does NOT reload the embedded bundle on the return — it reuses the prototype's already-loaded JS runtime. `recreateRootView`'s impl is in the **prebuilt ExpoModulesCore xcframework** (no source to read). Asymmetry unexplained: `recreate(prototypeURL)` rendered the prototype, but `recreate(embeddedURL)` after it renders the prototype again. **Candidate next approaches:** (a) fully tear down + re-init the RN factory/Hermes runtime on return rather than `recreateRootView`; (b) route the return through a real `EXDevLauncherControllerDelegate` (mirror stock `didStartWithSuccess` exactly, incl. its VC parenting); (c) keep the shell host alive in a 2nd window/VC and switch windows instead of one-host swap; (d) source-build ExpoModulesCore to read/patch `recreateRootView`. Bounded but needs RN-host-lifecycle depth + fresh context.

- **🎉 ON-DEVICE TEST PASSED 2026-06-21 (build 12, real iPhone, iOS 26.5.1) — the native runtime works.** Confirmed: sign-in ✅, link opens prototype + renders correctly ✅, **✕ Exit → home ✅** (the gate-2 return, on real hardware), **no Expo surface on shake ✅**, no Expo launcher ✅. Two issues found + fixed: **(1) crash when opening a link** — `RCTInstance.handleBundleLoadingError → RCTFatal` aborts the app when a prototype's bundle fails to load (Release has no redbox). Fixed via `RCTSetFatalHandler` → recover to our shell + reset updates, plus a `loadApp` re-entrancy guard (overlapping loads half-load the RN instance). **(2) flaky QR first-scan** — same root (failed loads); the recovery + guard should make retries clean. Commit `368835a`. **Build 13 (with the fix, build number 13, commit `368835a`) FINISHED on EAS 2026-06-21.** Submit was flaky: attempt 1 died on a network/DNS blip (pre-upload); attempts 2–3 scheduled + then returned the generic "Something went wrong when submitting to Apple App Store Connect." Build 12 submitted fine with identical config, so this is almost certainly a **duplicate** (an attempt uploaded build 13's binary, later attempts reject as already-delivered). **ACTION (Sheri): check App Store Connect → TestFlight → iOS — build 13 is likely already processing.** If 13 is NOT there, the submission detail pages (`…/submissions/e920b9fb-…` / `…/c33d419a-…`) show Apple's exact reason. Re-test on 13: open several links/QRs incl. a flaky one → should recover to shell, not crash.
- **🚀 TRUE-RELEASE EAS BUILD SUCCEEDED 2026-06-21 (build `23d34007`, `developmentClient:false`).** The fork built through the real EAS pipeline — **expo-updates compiled from source on EAS** with all 3 `pnpm` patches + config plugin, producing the App-Store-shippable `.ipa`. **Submitted to TestFlight (build 12, processing).** This is the first build that should cold-launch to our shell (not the dev-launcher) on a real device. **On-device 5-point pass = final gate:** (1) fresh cold-launch → our Sign-in, no "Finding Dev Servers"; (2) Apple sign-in → Home; (3) open `prototo.app/p/<token>` → prototype full-fidelity in our window; (4) ✕ Exit → back to shell; (5) shake = nothing, no Expo surfaces. If all hold → flagship native runtime is done + submittable.
- **✅✅ GATE 2 COMPLETE 2026-06-21 — full native-runtime loop works App-Store-clean in Release.** The return-to-shell is FIXED. Final fix: `goHome` calls **`EXDevLauncherController.updatesInterface.reset()`** (nils the launched update → `launchAssetURL = nil`), so expo-updates stops globally serving the prototype bundle to new RN hosts; combined with the **fresh-`ExpoReactNativeFactory`-per-mount** (`startReactNative`, replacing `recreateRootView`), the return host falls back to our delegate's embedded shell bundle. **Verified on a fresh-erased sim, true Release (APP_DEBUG false), screenshot-confirmed each step:** cold-launch → our Sign-in shell (no Expo launcher) → `loadApp` mounts a real EAS prototype (test4) in our window → ✕ Exit / `goHome` → **back to our Sign-in shell.** 31/31 JS tests, tsc clean. Commit `f2ff45e`. **Gates 1 + 2 (load + return) all proven.** Next: full EAS production build with `developmentClient:` **false** → on-device end-to-end → then live JS wiring already calls `PrototoRuntime.loadPrototype`/`goHome` so the real sign-in→open→exit flow should work.
- **🧱 DEEPER ROOT CAUSE 2026-06-21 (attempts 7–9, host-teardown fix):** Replaced `recreateRootView` with a **fresh `ExpoReactNativeFactory` + `startReactNative` per mount** (rules out host reuse) and restored `loadLocalBundle` + flag reset in `goHome`. **Return STILL renders the prototype** — even a brand-new host loads the prototype bundle. **The bundle provider is expo-updates' `AppController` (the `DevLauncherAppController` in our forked Release mode), GLOBALLY** — it keeps handing every new RN host the loaded update's `launchAssetUrl`. Our delegate `bundleURL`/override is ignored (load matches by coincidence; return doesn't), and the dev-launcher's `loadLocalBundle` resets the *dev-launcher* controller's sourceUrl (flag=0 confirmed) but **not expo-updates' launched-update state**. So returning to the shell requires resetting **expo-updates' `AppController`** to "no update / embedded" (e.g. its `reset`/relaunch path or `UpdatesControllerRegistry` swap), not the dev-launcher controller. That's a separate expo-updates-internals task. **Net:** load path fully works in Release; return is blocked one layer deeper than gate 2, at expo-updates. ~14 builds this session; checkpointing. Branch commit `00c6fcb`. Current code = fresh-factory mount (best architecture) + controller reset; load ✓, return ✗.
- **✅ ROOT CAUSE FOUND 2026-06-21 (diagnostic build, attempt 6):** Logged the three values in `goHome` → **`flag=0`** (the `shouldPreferUpdatesInterfaceSourceUrl` KVC reset works) and the `sourceUrl` getter resolves correctly. Yet the return STILL renders the prototype. So it's **NOT the URL/flag** — it's **`recreateRootView` reusing the already-running prototype JS host on the *second* swap**: shell→prototype tears down cleanly (1st recreate after `startReactNative`), but prototype→shell does not, and `setValue(nil, forKey:"reactHost")` isn't enough to force a fresh host the 2nd time. **THE FIX is a different mechanism — full host teardown + re-init on return**, e.g.: create a fresh `ExpoReactNativeFactory` + `startReactNative` for the embedded shell (mirroring launch), OR find the correct host-invalidation call (`RCTHost invalidate`/destroy) before recreate, OR keep the shell host alive in a second window and switch windows instead of swapping one host. The flag-reset + fresh-VC code is committed and correct as far as it goes; only the host lifecycle on the 2nd swap remains. Branch commit `885aa44`.
- **Update 2026-06-21 (attempts 4–5):** Confirmed **`recreateRootView` resolves the bundle from `EXDevLauncherController.sourceUrl` (the getter), NOT the `withBundleURL` param** (proven by the load/return asymmetry: I pass the embedded URL on return and it's ignored). The getter is `if (_shouldPreferUpdatesInterfaceSourceUrl && _updatesInterface && launchAssetURL) return launchAssetURL; return _sourceUrl;`. So return must make this getter yield the embedded bundle. Tried clearing `_shouldPreferUpdatesInterfaceSourceUrl` via KVC in `goHome` before `loadLocalBundle` — first with the wrong key `"_shouldPrefer…"` (no-op), then the correct `"shouldPrefer…"` (no leading underscore). **Still** `goHome SUCCESS sourceUrl=…/.expo-internal/…` (the prototype), so either the flag isn't actually clearing OR `loadLocalBundle` isn't setting `_sourceUrl` to the embedded `Prototo.app/main.jsbundle` (it should, via `_initAppWithUrl`). **PRECISE NEXT STEP:** add temp logging in `goHome`/shim of the three values right before mount — the flag (read it back via `valueForKey`), `_sourceUrl` (`valueForKey:@"sourceUrl"` won't work — it's the getter; read the ivar), and `_updatesInterface.launchAssetURL` — to see which is wrong, then either fix the flag-clear or make the return mount the embedded bundle through a path `recreateRootView` actually honors. Likely 1–2 iterations once the values are visible. Branch commit `427a6cb`.
- **Patches (3, all `pnpm`, EAS-reproducible):** `expo-dev-launcher`/`expo-dev-menu` debugOnly (gate 1), `expo-updates` source+USE_DEV_CLIENT+AppController (gate 2). Fork spans 3 Expo packages at link/compile/runtime/packaging layers — re-validate every SDK bump (`DC-13`).
- **Next:** fix goHome render (proper root teardown / fresh host) → full loop on fresh sim → EAS production build with `developmentClient:` **false** → on-device. ~~Remaining = the on-device 5-point pass~~ — superseded by this finding: cold-launch shell · Apple sign-in · recipient-run **Exit→shell** (the embedded-bundle return, only testable on this Release build) · Connect QR → Metro · no Expo surfaces anywhere. That pass is the final gate before App Store submission (metadata + 2.5.2 review notes already drafted in `…-phase-4-5-app-store-submission.md`).
Branch `feat/native-runtime-config-plugin`: 5 commits (plugin, crux2 shim, crux3 overlay, JS module). The dev-client store build (`com.sherizan.prototo`) on TestFlight still serves designers/EAS-Internal until this lands.

## Known issues / quirks

- **`pnpm release` (desktop) can fail on Apple's timestamp service** (`codesign … The timestamp service is not available`, seen 2026-09-19 on the 1.0.14 cut). Transient — just re-run; the second run succeeded unchanged.
- **npm shows a new version before it can be fetched.** After `pnpm publish`, `npm view` reported proto-cli 0.8.1 while `npm pack @sherizan/proto-cli@0.8.1` still returned `notarget` for ~1–2 min (2026-09-19). Wait, then verify with `--prefer-online`.
- **Point-and-edit is label-only when:** the project's layout has several `return (` (backfill never wraps it), the overlay is missing/customised, `proto start` < 0.8.1, or the designer starts typing before the file:line answers (~0.5s poll as of the next proto-cli publish; 1.5s on 0.8.1). The agent still finds the element by its literal label.
- **Desktop preview taps can die while the video keeps streaming (1.0.12, Xcode 27).** serve-sim's in-process input session drops when another simulator on the Mac is shut down (repro 2026-09-18: `simctl shutdown` of an unrelated test sim → taps dead, stream fine; desktop relaunch restores). Desktop BACKLOG #59: always-available Restart preview + input heartbeat. **Hygiene for Claude sessions: never `simctl shutdown`/`boot` other simulators while Prototo Desktop is running.**
- **Desktop simulator taps dead on Xcode 27 (field report 2026-09-18).** Xcode 27 replaced Simulator.app with Device Hub; `serve-sim` 0.1.44's input path stopped delivering taps while the stream kept working. Fixed upstream in serve-sim 0.1.46 ("restore Xcode 27 Device Hub streaming and input", 2026-08-21); **RESOLVED: desktop 1.0.12 RELEASED 2026-09-18** (serve-sim 0.1.46; Sheri confirmed taps work on Xcode 27; updater feed + `/api/download` serve 1.0.12; source tagged `v1.0.12`).
- **Publish GC could delete the freshly uploaded bundle (field report 2026-09-18, FIXED on prototo.app).** The post-publish GC read `manifest.json` back from storage and deleted whatever it didn't reference; a stale read (storage CDN propagation) or a concurrent publish made publish N's GC delete publish N+1's launch bundle. Repeated publish/open cycles left a share with every image but no bundle → the Viewer's "Couldn't load this prototype". Fix: objects younger than 15 min are never GC'd (`lib/supabase/bundle-store.ts`). **RESOLVED for instagram `WYDTPPJJSSTY` 2026-09-18 12:52 (re-published → v7, new bundle present, share QR opens in the Viewer); any other share hit earlier heals the same way on its next publish.** (Note: a first re-publish attempt from the desktop left no trace on the server — no upload, no share update — cause unknown; the second went through. Watch for a desktop Publish that reports success without running.) Durable follow-up in BACKLOG: `POST /api/share` should receive the keep-set from the CLI.
- **A Mac-preview connect that fails showed nothing in the Viewer (≤ 1.1.0).** Scanning the desktop "Preview on iPhone" QR (or opening it via the Camera app) fired the native load with no screen behind it, so an unreachable Mac looked like "the scanner doesn't detect the QR" / "the app opens the home page and nothing happens". Fixed on `chore/sdk-57-upgrade` (`/open` screen + `+native-intent` + AppDelegate hands connect links to the JS shell, parking them during cold start) — **sim-verified 2026-09-18 on a Release build: warm link → "Connecting to your Mac…" → prototype; unreachable host → "Couldn't reach your Mac's preview… same Wi-Fi" + Try again after the native timeout; cold start → parked → flushed → loads.** Ships in the next Viewer build (1.1.1); the error copy now also names iOS's per-app **Local Network** permission (Settings → Prototo → Local Network) with an Open Settings button. **ROOT CAUSE FOUND 2026-09-18 night: Wi-Fi client isolation on Sheri's network.** Same SSID, phone `192.168.7.95` / Mac `192.168.5.195` (one /22), Local Network permission ON — yet the iPhone's Safari gets nothing from `http://192.168.5.195:8081/status` (the Mac's own Safari does), the Mac can't ping the phone or any other Wi-Fi peer, and no phone connection ever reached Metro during four port watches. The in-app scanner was proven fine (a public-host connect QR shows the on-screen rejection; a valid one is handed to the native connect, which then can't reach the Mac and, in 1.0.5, says nothing). **Verified workaround (Sheri, same night): iPhone Personal Hotspot with the Mac joined → Preview QR works end to end** (the desktop QR picks up the hotspot address; the Viewer accepts 172.16–31.x). Permanent fix is the router's AP/client-isolation setting. Software follow-up in BACKLOG (tunnel for Preview).
- **pnpm 11 exits 1 the first time it meets a dependency build script it hasn't recorded** (`ERR_PNPM_IGNORED_BUILDS`, e.g. `@shopify/react-native-skia` after an SDK move; it writes `allowBuilds` into `pnpm-workspace.yaml` and passes on the next run). `proto upgrade` retries `expo install --fix` once for this. Designers on pnpm 11 running `expo install` by hand would see it once.
- **`npx expo-doctor` flags the scaffold's `"proto": "proto"` script** as colliding with the `proto` bin (20/21 checks). Pre-existing, cosmetic; the script is what makes `pnpm proto start` work.
- **Xcode 27 has no `Simulator.app`** (replaced by Device Hub per Expo's SDK 58 notes). `ensure-prototo-app.ts:201` runs `open -a Simulator` silently, which now fails ("Unable to find application named 'Simulator'"); `simctl boot` + the `expo start --ios` deep link still launch the app, so `proto start` works, but nothing brings the simulator window to the front. Seen 2026-09-18 on Sheri's Mac (Xcode 27.0). **FIXED 2026-09-19 on `chore/sdk-57-upgrade`:** falls back to `open devices://device/open?id=<udid>` (DeviceHub, bundle `com.apple.dt.Devices`, at `Xcode.app/Contents/Applications/DeviceHub.app`). Ships with the next proto-cli publish; not yet eyeballed live.
- **`pnpm create proto@latest` has 120-min stale-cache** for `@latest` resolution. Workaround: `pnpm create proto@<exact-version>` to force fresh fetch. `npm create proto@latest` doesn't have this issue.
- **~~3 pre-existing typecheck errors in `packages/proto-components`~~ RESOLVED 2026-09-18** by refreshing its devDeps to the SDK 57 pins (they were stale at RN 0.83.6 / no `@expo/ui` types). Was: (`Card.tsx` can't find `expo-glass-effect` types; `Toggle.tsx` can't find `@expo/ui/swift-ui` types + has one implicit-any). Not caused by the motion-primitives work — present on `main` before this PR. Not blocking; the modules still compile and run.
- **iOS Simulator can stash old Prototo binaries across device instances.** If a designer's `proto start` boots a different simulator device than where the new Prototo was installed, they see a stale version. `ensure-prototo-app` SHA-checks via the GitHub Release manifest and auto-reinstalls when out of date, but only on `proto start` — the first launch of an unused simulator may need `rm -rf ~/.prototo/cache` + a fresh `proto start`.
- **Verifying a native module is in the dev client: check `Prototo.debug.dylib`, NOT `Frameworks/`.** Most Expo/RN modules are statically linked into `Prototo.app/Prototo.debug.dylib` (the ~53MB debug dylib), not shipped as separate `.framework`s — only some (ExpoImage, ExpoFileSystem, ExpoModulesCore, ExpoCamera, ExpoMaps, ExpoVideo, RNReanimated, RNScreens, RNSkia, RNSVG, async-storage, safe-area) get their own framework. The main `Prototo` executable is a ~120KB launcher shell with no module code. To confirm a module: `strings -a Prototo.app/Prototo.debug.dylib | grep -c '<ModuleName>'` (e.g. `ExpoAsset` / `AssetModule`). This was the trap during the 0.1.11 hotfix — the framework list falsely implied the fix failed.
- **`npm cache clean --force` prints an ownership warning** (`sudo chown -R 501:20 /Users/sherizan/.npm`). Pre-existing on Sheri's machine from a past `sudo npm`. Cosmetic.
- **A leftover local Xcode App-Clip build can shadow the official dev client (resolved 2026-06-14).** During the Phase E / E2 on-device tests, clip-containing builds were installed via local Xcode under the **same bundle id `com.sherizan.prototo`** (Phase E embedded the clip into `apps/prototo-app`) and `com.sherizan.prototo.viewer` (Phase E2). Those locally-installed binaries override the official one by bundle id, so the device shows the "App-Clip version" even though the official channels never shipped a clip build. **The official builds are clean:** install URL `prototo.app/install/ios/app` → device build `3b56455a` (commit `b5918f9f`, plain SDK-56 dev client, no clip); the clip-era device build `3d4e1770` ERRORED and never shipped; the only de-clipped finished build is the sim `cf91c268` (on Appetize). **Fix:** delete Prototo (and any "Prototo Viewer") from the device, reinstall from `prototo.app/install/ios`. Confirmed working.

## Next up

> **Everything deferred is now an issue on the single Prototo backlog** — GitHub Issues on `sherizan/prototo-shared` + the Prototo project board (`gh issue list -R sherizan/prototo-shared -l proto` · `gh project view 5 --owner sherizan`). This section keeps only in-flight or event-gated items.

- **The SDK 57 flip hour** — see In progress (waiting on Apple for Viewer 1.1.0 build 37). Then within a week: watch the stale-share email volume (lifecycle cron `shareStale` count) and the desktop auto-move in the wild; if a designer reports a half-moved project, `proto upgrade` again heals it. Gated on the flip: docs pass for 1.1.0 (#13), expo-sensors rollout (#8, rides the Viewer cut after 1.1.0 together with the 1.1.1 fixes on PR #60).
- **Point-and-edit: SHIPPED** (proto-cli 0.8.1 + desktop 1.0.14, 2026-09-19). Only create-proto 0.8.0 (template carries the overlay natively) is still held for the flip; existing projects heal via `proto start`'s backfill. Leftovers → #31.
- **From the evening field report (2026-09-18):** desktop Publish that reported success without publishing → #14 (watch); Preview-on-iPhone docs line → #12; tunnel mode + hotspot hint → #11; GC keep-set → #20.
- **Native-runtime maintenance:** re-validate the 3 `DC-13` patches on the next SDK bump (folded into the SDK 58 issue #24); keep the sample prototype (EAS group `6d42487e`) published or re-pin `apps/prototo-app/lib/sample.ts`.
- **CHANGELOG:** the designer-facing App-Store-launch entry → #36 (Sheri's call on copy/timing).

## How to maintain this file

**At the start of a Claude Code session in this repo:** read this file first to see what's current.

**At the end of a Claude Code session:**
1. If anything in "Currently live" changed (versions, install paths, new releases), update the table.
2. Move freshly-completed work from "In progress" → "Done recently" with date + commit refs.
3. **If a change is something a designer would feel, add a designer-facing line to `CHANGELOG.md`** (under "Unreleased" until it ships). That file — not this one — feeds the website's release notes.
4. If new bugs surfaced, add to "Known issues / quirks".
5. If new follow-ups came up that aren't being acted on, add them to `BACKLOG.md`.
6. If a cross-cutting risk surfaced, add it to `RISKS.md`.
7. Update the "Last updated" line in the header.
8. Commit this file alongside whatever else the session produced — the file IS the handoff note.

Keep "Done recently" to roughly the last 2–3 weeks. Older entries can be culled — `git log` is the long-term history.

## 2026-07-08 — Viewer home redesign SHIPPED as 1.0.3 (merged to main, EAS builds cut)
- One adaptive home: Recently viewed (+ designer name, Yours badge), Yours section, dashed empty state with the scan glyph inline, shimmer "Fetching prototypes" cards while shares load.
- Bottom nav is the NATIVE Liquid Glass tab bar (Sheri's call after seeing the custom pill live); Scan sits in the separated search-role slot (`app/home/scan.tsx`, camera gated by tab focus). Scanner extracted to `components/Scanner.tsx`: copy-free iOS-Camera-style reticle (soft-radius window, corner brackets), clipboard banner, white Cancel only on the pushed `/connect` variant.
- App-open clipboard prompt ("Open <name>?", solid card — GlassView cannot composite inside a transparent Modal) with remembered decline; suppressed on scanner routes. Detection covers string-typed links (our own Copy buttons); iOS asks "Allow Paste" once per source app.
- Root cause of every white launch: expo-splash-screen was never installed and the app.json `splash` key is ignored on SDK 56 — ALL prior builds incl. the live App Store one cold-start white. Fixed via the plugin (splash holds until first paint) + an AppDelegate storyboard overlay covering the exit-to-home runtime swap.
- Error screen rebalanced (headline title, full-width primary Try again, "Back to home"); root index shows a centered logo during session restore. Sample prototype deleted.
- Process: subagent-driven (8-task plan, per-task reviews caught 2 hook races + a nav race; final opus review READY TO MERGE). 78 tests + tsc green, sim-e2e PASS. 1.0.3 build 31 on TestFlight (pre-reticle); build 32 cut with the full set. Yours-section live check + on-device scan = Sheri's device pass.

## 2026-06-22 — Google + email sign-in (+ Sign-in UI polish)
- Added Google (OAuth web sheet via expo-web-browser + PKCE) and email OTP (signInWithOtp/verifyOtp) sign-in alongside Apple, built subagent-driven (per-task implementer+reviewer, final opus review; fixed a button double-submit). New: lib/auth-errors.ts (tested), proto-components Input + Lottie + Divider label + Button icon/style props.
- Sign-in UI: animated logo (logo-prototo.json @ 44px), short viewer copy, Google(+G icon)→Apple consistent light-gray buttons, "or" divider, buttons pinned to bottom, top safe-area edge fix. 39/39 tests, tsc clean. Commits 3fa9d6c..1837c4e on feat/native-runtime-config-plugin.
- **EAS production build 14 building (2026-06-22)** then TestFlight. NOTE: custom Apple button (not native control) — minor App-Store HIG risk, flagged to Sheri.
- **Supabase device prereqs (same project as web):** providers already set; STILL need (1) `prototo://auth-callback` in redirect allowlist. (2) DONE 2026-07-07: email template now exposes `{{ .Token }}` (the 6-digit code) for the native OTP flow — previously the template's `{{ .ConfirmationURL }}` link resolved to the Site URL (`https://prototo.app/?code=…`), a dead end nothing handled.

## 2026-06-23 — Dashboard: your-prototypes list + pinned sample
- Pinned sample prototype (lib/sample.ts → "Open sample" → loadPrototype; no backend, App-Review-safe) + "Your prototypes" list of the user's shares on HomeScreen.
- Backend (prototo-website, branch feat/api-shares, commit 9a40a44): GET /api/shares (Bearer Supabase auth) + ShareStore.listActiveByUser. DEPLOYED to main (commit 9a40a44); /api/shares live (401 confirmed); app degrades to empty-state without it.
- App (feat/native-runtime-config-plugin, ee09b73..779b364): lib/sample, lib/my-shares (fetchMyShares), lib/relative-time, HomeScreen rewrite. 52 tests, tsc clean, subagent-driven + final review clean.

## 2026-06-28 — 🚀 SUBMITTED TO APP STORE (build 20)
Build 20 submitted for App Store review (App Store Connect app 6772877748). Full v1: native launcher-free runtime, Apple/Google/email sign-in, dashboard (your-prototypes list + pinned sample), floating Refresh/Exit prototype menu. Review notes frame it as a dev tool (Expo Go precedent) for 2.5.2; built-in sample lets reviewers view a prototype without a shared link. Branch feat/native-runtime-config-plugin (PR #34) still UNMERGED. prototo-website main has the /api/shares backend (deployed).

## 2026-07-02 — App Store REJECTION (build 20) + fix (build 21)
- Apple rejected build 20 (reviewed on iPad Air, iPadOS 26.5): (1) 2.1(a) all buttons unresponsive on iPad; (2) 5.2.5 subtitle used "iOS".
- Finding: app is already iPhone-only (device family 1); iPad CANNOT be excluded (iPhone apps always run+review on iPad). Must fix, not dodge.
- 2.1(a) root cause: floating ⋯ menu's full-screen PassthroughWindow + menuOpen flag stuck true after iPad popover outside-tap dismissal → swallowed all touches. Fixed (commit 87d95f2): present menu from MAIN window, removed menuOpen; overlay now pure passthrough (can only intercept its own button). Verified on iPad Air 11" (M4) sim.
- 5.2.5: subtitle to be changed in ASC (remove "iOS" → "View native prototypes"). Manual ASC edit.
- Build 21 built + submitted. Fix on branch fix/ipad-floating-menu (commit 87d95f2), PR #35 open awaiting merge.

## 2026-07-04 — Resubmission build 23 (fixes rejection round 2)
Build 23 uploaded + SUBMITTED for review. Fixes all 3 issues from the 2026-07-02 rejection:
- 5.1.1(v) account deletion: DELETE /api/account live on prototo.app + in-app Profile → Delete flow.
- 2.5.4: removed unused audio UIBackgroundModes (expo-audio enableBackgroundPlayback:false).
- 2.1(a): pinned a new FUNCTIONAL sample prototype (EAS group 6d42487e, runtime prototo-56) replacing the dead-button test3.
Also shipped: dashboard redesign (native tabs Prototypes/Profile + Mine/Shared segmented), opened-history, dark-mode-leak fix, branded loader, Profile privacy/terms/docs links, native segmented control, button text-size match.
Branch fix/account-deletion-and-audio (PR #37, unmerged). prototo-website main has /api/account. User has the deletion screen recording. Submitted for review 2026-07-04 (build 23, deletion recording + reply attached).
