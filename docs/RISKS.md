# Risks Backlog

> Source of truth for open risks across the Proto + Prototo product line. Curated; not every "Open risks" item in every spec lands here — only the ones worth tracking *across* features.
>
> Each new spec under `docs/superpowers/specs/` must include an **Open risks** section. The risks that affect more than the immediate feature (cross-cutting, long-tail, or requiring follow-up work) get promoted here for visibility.
>
> Status: `open` (active concern), `mitigated` (controls in place; revisit if conditions change), `accepted` (we chose to live with this), `resolved` (no longer applies). Each entry links to its source spec.

## Format

`| ID | Area | Risk | Status | Source | Mitigation / next action |`

IDs are append-only, two-letter area prefix + sequence (`DC-01`, `SH-02`, etc.). Don't renumber.

| Area | Prefix |
|---|---|
| Dev client (`apps/prototo-app/`) | `DC` |
| Share infrastructure (`prototo.app`) | `SH` |
| CLI (`packages/proto-cli/`) | `CL` |
| Scaffold (`packages/create-proto/`) | `SC` |
| App Store / Apple Developer | `AS` |
| Cloud streaming (Appetize / EAS Update share path) | `CS` |
| Cross-cutting / infra | `XX` |

---

## Open

| ID | Area | Risk | Status | Source | Mitigation / next action |
|---|---|---|---|---|---|
| `AS-01` | App Store | App Store rejection of Prototo App. Apple has historically rejected JS-bundle-loading apps. | `superseded` | `2026-05-25-prototo-app-dev-client-design.md` §Open risks #1; `2026-06-10-app-clip-sharing-architecture-design.md` | Structural fix now in flight: the App Clip manifest architecture (spec 2026-06-10) renders declarative data, not downloaded code — removes the 2.5.2 trigger. Residual review risk is the UGC edge, tracked as `AS-05`. Plan B if review >2 weeks: TestFlight invites for early adopters. **SUPERSEDED 2026-06-12 — App Clip sharing retired; replaced by cloud streaming (Appetize).** v1 already ships via EAS Internal Distribution (not App Store), and the new share path streams video of a real prototype running on a cloud device — no app the recipient installs, no JS bundle in an Apple-reviewed binary. The App-Store-rejection vector for the share mechanism is closed. |
| `AS-02` | App Store | Apple Developer account scope. Currently personal; may need team upgrade for non-individual publishing. | `open` | (operational, not in spec) | Revisit if/when team members are added. |
| `DC-01` | Dev client | `MinimumOSVersion: 26.0` excludes designers on older iOS. | `accepted` | `2026-05-25-prototo-app-dev-client-design.md` §Open risks #2 | Intentional positioning bet ("Liquid Glass without Xcode"). Revisit if a meaningful audience is locked out. |
| `DC-02` | Dev client | Simulator Liquid Glass fidelity unknown — Apple's iOS 26 Simulator may not paint the real material even with correct entitlements. | `mitigated` | `2026-05-25-prototo-app-dev-client-design.md` §Open risks #3 | Validated on iPhone (real Liquid Glass renders); Simulator path works for layout/interaction. Re-test if Apple updates Simulator graphics stack. |
| `DC-03` | Dev client | `com.sherizan.prototo` bundle ID claim. Needs fresh registration in App Store Connect; old `com.sherizan.proto` claim abandoned but dormant. | `mitigated` | `2026-05-25-prototo-app-dev-client-design.md` §Open risks #5 | Registered 2026-05-25 (App Store Connect entry created with the new bundle ID). |
| `DC-04` | Dev client | EAS API uses `buildProfile`, not `profile`. A future maintainer touching `eas build:list --json` may hit the same gotcha. | `open` | `2026-05-25-prototo-app-dev-client-design.md` §Open risks #7 | Grep convention: always `buildProfile` in any new EAS-querying code. |
| `DC-05` | Dev client | `release-simulator.ts` leaks ~50–100 MB to `/tmp/prototo-release-*` after each run (workDir not cleaned). | `open` | Final code review of `feat/prototo-app-dev-client` | Add `fs.rmSync(workDir, { recursive: true, force: true })` at end of `main()`. ~5 min fix. |
| `DC-06` | Dev client | `prototoAppOutdated` message is defined + tested but never called at runtime. CLI-side silence-detection trigger not implemented. | `open` | `2026-05-25-prototo-app-dev-client-design.md` §Implementation footnotes | Either wire up a 5+ second silence detector after QR scan or remove the message + test. Decide before stable 1.0. |
| `SH-01` | Share infra | Rate-limit ceiling (10/hr/IP) is a guess. May bite early beta testers. | `open` | `2026-05-24-share-landing-and-token-router-design.md` §Open follow-ups #3 | Revisit after `proto share` ships and we see real usage patterns. |
| `SH-02` | Share infra | Token-format type sharing — `ShareCreateInput` will be duplicated between `prototo-website` and `proto-cli`. | `open` | `2026-05-24-share-landing-and-token-router-design.md` §Open follow-ups #1; restated in `2026-05-25-proto-share-design.md` | Defer until D ships. Extract `@sherizan/prototo-types` package once both sides need to evolve together. |
| `SH-03` | Share infra | Universal-link verification once F ships. Need to validate AASA file with Apple's tooling. | `superseded` | `2026-05-24-share-landing-and-token-router-design.md` §Open follow-ups #2 | Add to F's pre-submission checklist. **SUPERSEDED 2026-06-17 — "F" (the App Clip universal-link viewer) was retired in the 2026-06-12 cloud-streaming pivot; sharing is now a plain `prototo.app/p/<token>` web link, so there's no app-bound universal link / AASA verification on the share path.** |
| `SH-04` | Share infra | Master doc roadmap numbering mismatch — prototo-website calls share "Phase 4", proto repo master doc calls it "Phase 3". | `resolved` | `2026-05-24-share-landing-and-token-router-design.md` §Open follow-ups #4 | Reconcile before B is publicly demoed. **RESOLVED 2026-06-17 — share shipped publicly (cloud streaming); `docs/proto-master.md` reconciled (§8 marked SHIPPED, header reality banner added). Numbering is moot now that the feature is live.** |
| `SH-05` | Share infra | Cloudflare Quick Tunnel subdomain rotation. Designer Ctrl+C and restart produces a new tunnel URL; old share token points at a dead tunnel. | `superseded` | `2026-05-25-proto-share-design.md` §Open risks #1; `2026-06-10-app-clip-sharing-architecture-design.md` | Phase 3a tradeoff. Resolving path is now the App Clip manifest architecture (spec 2026-06-10): a static manifest is uploaded to CDN, not streamed from a live tunnel, so there is no tunnel URL to rotate. Tunnel kept only as a flagged fallback. **SUPERSEDED 2026-06-12 — App Clip sharing retired; replaced by cloud streaming (Appetize).** The live cloudflared tunnel is no longer on the share path: `proto share` now publishes via `eas update` to a central EAS project, so there is no designer-local tunnel URL to rotate. |
| `SH-06` | Share infra | Metro accessible publicly through the cloudflared tunnel. JS bundle = project source, no auth gate. | `superseded` | `2026-05-25-proto-share-design.md` §Open risks #3; `2026-06-10-app-clip-sharing-architecture-design.md` | Phase 3a tradeoff. The App Clip manifest architecture (spec 2026-06-10) ships a declarative manifest, never the project's JS source — closes this for the primary path. Tunnel kept only as a flagged fallback. **SUPERSEDED 2026-06-12 — App Clip sharing retired; replaced by cloud streaming (Appetize).** No public Metro/tunnel on the share path. The bundle is hosted on EAS Update and runs on a sandboxed cloud device; the recipient only receives video, never the JS source. |
| `SH-07` | Share infra | Cloudflare service outage on `trycloudflare.com` breaks `proto share`. No automatic transport fallback. | `superseded` | `2026-05-25-proto-share-design.md` §Open risks #4 | Failure message points designer at docs. Doubling transport options doubles testing surface. **SUPERSEDED 2026-06-17 — the cloudflared tunnel was dropped from the share path; `proto share` publishes via `eas update` (EAS Update hosting). No trycloudflare dependency.** |
| `CL-01` | CLI | `cloudflared` npm package quality / Cloudflare protocol churn. | `superseded` | `2026-05-25-proto-share-design.md` §Open risks #2 | `ensureCloudflared` checks system-installed first; falls back to npm-managed. Logged in translation layer when a fallback is taken. **SUPERSEDED 2026-06-17 — dead tunnel code removed (commit be11938); cloudflared is no longer on the share path.** |
| `CL-02` | CLI | Race between Metro readiness and tunnel start. First stakeholder request could hit a 502 before Metro is ready. | `superseded` | `2026-05-25-proto-share-design.md` §Open risks #7 | Wait for Metro readiness signal before printing the share URL. `expo-spawn` already exposes readiness via stdout; reuse that. **SUPERSEDED 2026-06-17 — no tunnel on the share path (`eas update` publish), so there's no Metro-vs-tunnel start race.** |
| `CL-03` | CLI | Zod added as runtime dep on proto-cli (~12KB gzipped). | `accepted` | `2026-05-25-proto-share-design.md` §Open risks #6 | Acceptable for v1. Replace with hand-rolled validator if package size becomes a concern. |
| `XX-01` | Cross-cutting | The `next` npm dist-tag retains old beta references after stable promotion (tombstones). | `resolved 2026-05-29` | Conversation 2026-05-25 | Removed `next: 0.3.0-beta.0` from both `create-proto` and `@sherizan/proto-cli` via `npm dist-tag rm`. Only `latest` remains. |
| `DC-07` | Dev client | expo-dev-client's bundle-load error UI (e.g., 502 from a dead tunnel) is owned by Expo and not customizable. Stakeholder sees Expo's default error screen instead of Prototo-branded copy. | `superseded` | `2026-05-25-prototo-viewer-mode-design.md` §Open risks #2 | Phase 3a tradeoff. B's share-landing page is the primary surface for stakeholders who can't connect. Phase 3b hosted-snapshot eliminates the live-tunnel failure mode. **SUPERSEDED 2026-06-12 — App Clip sharing retired; replaced by cloud streaming (Appetize).** The stakeholder no longer runs expo-dev-client locally — they open `prototo.app/p/<token>` embedding the Appetize stream, so Expo's bundle-load error screen is off the share path. Web-embed error states are owned by us. |
| `SH-08` | Share infra | Viewer-side stale-tunnel race: stakeholder taps a share link after the designer Ctrl+C's `proto share`. KV still has the (now-dead) tunnel URL, redirect succeeds, expo-dev-client gets a 502 from the bundle fetch. | `superseded` | `2026-05-25-prototo-viewer-mode-design.md` §Open risks #5 | Distinct from SH-05 (which is about designer-restart). Same Phase 3b hosted-snapshot fix. **SUPERSEDED 2026-06-12 — App Clip sharing retired; replaced by cloud streaming (Appetize).** No live designer-local tunnel in the loop: the share resolves to an EAS-Update-hosted bundle booted on a cloud device, so there is no dead-tunnel 502 race. Cold-start latency is the new failure mode, tracked as `CS-02`. |
| `AS-03` | App Store | Universal-link AASA file propagation lag — iOS caches the previous AASA for up to 24h after an App Store install. Universal-link path may not work for some users during that window. | `superseded` | `2026-05-25-prototo-viewer-mode-design.md` §Open risks #1 | Scheme path remains functional. Document the 24h window. Revisit if real users hit it after F lands. **SUPERSEDED 2026-06-17 — the App Clip / universal-link viewer was retired; the share recipient opens a plain `prototo.app/p/<token>` web page (no app-bound universal link, no AASA cache window).** |
| `AS-04` | App Store | Guideline 2.5.2 — Prototo App architecture (`expo-dev-client` + Metro bundle download via cloudflared tunnel) downloads & executes code at runtime. | `superseded` | `docs/prototo-app-store-plan.md`; `/Users/sherizan/.claude/plans/drifting-weaving-hammock.md` | Empirical probe paused. Audience pivot 2026-05-26: v1 ships via EAS Internal Distribution to designers + small teams, not App Store. The 2.5.2 risk is moot at this distribution channel. Re-engages when product validates and stakeholder reach (non-technical App Store users) becomes a priority — at which point the SDUI architecture from `docs/prototo-app-store-plan.md` §3 is the path. **Re-engaged 2026-06-10:** that SDUI path is now specced as the App Clip manifest architecture (`2026-06-10-app-clip-sharing-architecture-design.md`); Phase A (manifest contract) has landed. TestFlight `kVJN6n2A` external submission withdrawn 2026-05-26. **SUPERSEDED 2026-06-12 — App Clip sharing retired; replaced by cloud streaming (Appetize).** The SDUI/App-Clip path is abandoned (a delta analysis of real prototypes showed the manifest expresses ~0% of what users build). Cloud streaming sidesteps 2.5.2 entirely: nothing is reviewed by Apple on the share path — the prototype runs on a cloud device and the recipient receives video over the web. |
| `XX-02` | Cross-cutting | Native-module release cadence. Every future native dep (mapbox, video, ML Kit, audio, etc.) requires a Sheri-led EAS Internal cut + designer re-install on every device. No OTA escape hatch — code-push only patches JS. | `open` | `2026-05-27-motion-graphics-primitives-design.md` §Open risks #1 | Maintain a "what's in the box" manifest in `docs/proto-master.md` listing every bundled native module. Batch native-dep additions into monthly Prototo App releases when possible. |
| `XX-03` | Cross-cutting | Subpath mirror drift between workspace `packages/proto-components/src/` and template `packages/create-proto/template/components/proto/`. | `mitigated` | `2026-05-27-motion-graphics-primitives-design.md` §Open risks #3 | Already handled: `packages/create-proto/scripts/sync-template.ts` recursively copies `proto-components/src/` → `template/components/proto/` and runs as `prebuild` + `prepublishOnly`. Template path is `.gitignored`. Workspace source is the single source of truth. |
| `XX-04` | Cross-cutting | Error translation gap for ease / reanimated / lottie / skia. Red-screen stack traces from these libraries surface their native package names to designers. | `open` | `2026-05-27-motion-graphics-primitives-design.md` §Open risks #4 | §14 error-translation layer must learn the failure modes of these libraries. Pick up in the next error-translation sweep. |
| `DC-08` | Dev client | `react-native-ease` maturity. Young library (App and Flow, 2025–2026), not as battle-tested as reanimated. Edge-case props (e.g. `borderRadius` on iOS) may not animate as expected. | `open` | `2026-05-27-motion-graphics-primitives-design.md` §Open risks #2 | Template CLAUDE.md instructs Claude to fall back to `gestures` subpath if a `motion` transition misbehaves. Revisit after first real designer-usage feedback. |
| `DC-09` | Dev client | Dual Prototo App distribution channels — EAS Internal device IPA at `prototo.app/install/ios` and the `prototo-sim-sdk{N}-latest` GitHub Release tag (consumed by `packages/proto-cli/src/ensure-prototo-app.ts` for simulator installs) are independent. Updating only one ships designers a binary that mismatches their environment's native module set, with silent failures (Lottie/Canvas render empty). | `mitigated` | Conversation 2026-05-27 (motion-primitives rollout almost shipped simulator-only 0.1.3 while devices got 0.1.8) | Added `pnpm release:all` script in `apps/prototo-app/package.json` that chains `build:ios` + `build:ios:sim` + `release:simulator`. Use it for every Prototo App release. |
| `XX-05` | Cross-cutting | Native-rebuild detection accuracy depends on the EAS release flow writing `nativeModules` into the build manifest. Until a dev-client build ships a manifest carrying that field, `warnUnsupportedNativeModules` is a silent no-op — it never falsely flags, but it also can't warn. | `open` | `2026-06-09-native-detection-and-proto-add-design.md` §Open risks | `release-simulator.ts` now emits `nativeModules` (from `apps/prototo-app` deps) into the manifest; the safe failure mode (silent skip) holds until the next `release:all` cut ships it. Pairs with `XX-02`. |
| `AS-05` | App Store | App Clip UGC-edge rejection. Demo App Clips are recognized (Apple HIG, June 2025), but Prototo sits on the user-generated-content boundary of that category — Apple may still reject the manifest-renderer clip. | `superseded` | `2026-06-10-app-clip-sharing-architecture-design.md` §Open risks #1 | Framing-based: one invocation = one prototype, no public gallery, parent app shows the designer's own work (4.2.6 picker pattern). Explicit review notes. First submission may bounce; appeal path is real. Successor to the structural half of `AS-01`. **SUPERSEDED 2026-06-12 — App Clip sharing retired; replaced by cloud streaming (Appetize).** No App Clip is submitted to Apple, so the UGC-edge review risk no longer exists. |
| `AS-06` | App Store | App Clip bundle-size budget — **100MB** (link, iOS 17+) / 15MB (QR); Hermes alone ≈3.2MB. Risk was that React Native wouldn't fit, forcing a SwiftUI clip = a second renderer. | `superseded` | `2026-06-10-app-clip-sharing-architecture-design.md` §Open risks #3; `2026-06-11-clip-size-spike.md` | **Resolved by the D0 spike (2026-06-11):** `react-native-app-clip` builds on SDK 56 / RN 0.85 / New Arch, and a clip reusing the current `proto-components` measures **≈13.5MB** (device, unthinned) — fits 100MB with ~87MB headroom. **RN renderer committed; no SwiftUI rewrite.** Residual: 15MB QR is plausible (~2MB margin) but unconfirmed — needs a thinned-device archive if QR is ever required (QR is a stretch goal). **SUPERSEDED 2026-06-12 — App Clip sharing retired; replaced by cloud streaming (Appetize).** No App Clip binary is built or shipped, so the bundle-size budget is no longer a constraint. |
| `XX-06` | Cross-cutting | Manifest schema ceiling is permanent. Anything not expressible in the manifest can never be shared natively; local fidelity and shared fidelity are two distinct tiers forever, not a temporary gap to close. | `superseded` | `2026-06-10-app-clip-sharing-architecture-design.md` §Open risks #2 | Accepted upfront as a structural feature. Generation guidance (`CLAUDE.md` + template) tells Claude to keep shared-bound screens within the manifest subset. Grow the schema only from real prototypes that hit its edge. **SUPERSEDED 2026-06-12 — App Clip sharing retired; replaced by cloud streaming (Appetize).** The delta analysis (motion/reactbits/test2/waaave — 14/14 screens) showed the manifest expressed ~0% of real prototypes; the new requirement is 100% fidelity. Cloud streaming runs the real prototype, so there is no manifest subset and no two-tier ceiling — local fidelity *is* shared fidelity. |
| `CS-01` | Cloud streaming | Appetize cost at scale. Cloud streaming bills per device-minute; popular shares (many recipients, long sessions, or a link that loops) can run up usage with no natural cap. | `open` | Pivot 2026-06-12 (cloud-streaming architecture); Phase 0 spike GO (ombak) | Model per-session cost before public beta. Add per-token session caps / idle-timeout / concurrency limits and a usage dashboard. Consider free-tier session budgets per designer. Revisit pricing tier once real session-length data exists. **Model decided 2026-06-12** (`2026-06-12-share-economics-and-pricing-design.md`): the core tool stays free for solo; only the *sharing/org layer* monetizes — Free/Plus solo funnel (Appetize minutes = accepted marketing spend) + per-seat **Team** revenue that scales margin with org size; four free-tier levers (project cap + minute budget + per-session caps + time-boxed links); metered self-served from our own embed. |
| `CS-02` | Cloud streaming | Cold-start latency. Each share session boots the Prototo dev client on a fresh cloud device before the recipient sees anything; multi-second spin-up reads as "broken link" to a non-technical stakeholder. | `open` | Pivot 2026-06-12; successor failure mode to `SH-08` | Measure cold-start on Appetize (iOS 26). Show a branded loading state on `prototo.app/p/<token>` while the device boots. Investigate warm-pool / pre-warmed sessions if latency hurts first-impression. |
| `CS-03` | Cloud streaming | EAS Update runtime-version churn. `proto share` publishes via `eas update`; any dev-client *native* change (new bundled module, SDK bump) bumps the runtime version and invalidates updates published against the old one — old share links can stop resolving to a compatible binary. | `open` | Pivot 2026-06-12; pairs with `XX-02`, `XX-05` | Graceful degradation via runtime-version match (SDK 56 / `prototo-56`). Keep the central EAS project's runtime version stable across releases where possible; when it must move, surface a designer-friendly "this prototype needs re-sharing" message rather than a raw EAS error. Batch native changes (see `XX-02`). |
| `CS-04` | Cloud streaming | Cloud hardware is simulated. The cloud device has no real camera, GPS, microphone, or sensors — prototypes that read live hardware show simulated/empty input when streamed, even though they work locally on the designer's phone. | `accepted` | Pivot 2026-06-12 | Inherent to the streaming model. Document which capabilities are simulated in-stream. Where Appetize offers injectable fixtures (mock camera image, mock location), wire them so a shared prototype shows representative data rather than a blank feed. |
| `CS-05` | Cloud streaming | Unsigned OTA updates — supply-chain / RCE surface. The dev client loads JS over the air via EAS Update; an attacker who obtained the EAS publish token could push arbitrary code that the cloud device executes. | `open` | Pivot 2026-06-12; pre-launch hardening | **Severity reduced** because the update runs on a sandboxed cloud simulator and the recipient only ever receives video — there is no path to the recipient's device. Pre-launch hardening: enable EAS Update **code-signing** (signed manifests, client rejects unsigned) and tightly scope + protect the EAS publish token (least-privilege CI secret, rotation). Close before public launch. |
| `CS-06` | Cloud streaming | Active-device concurrency ceiling. Appetize limits by *concurrent* streams: Starter = **3 active devices** (Premium 16) — at most 3 stakeholders across *all* designers can watch at the same instant; a 4th is queued. A burst of simultaneous viewers (or growth) hits this before the monthly minute budget does. | `open` | `2026-06-12-share-economics-and-pricing-design.md` | Appetize's built-in queue absorbs short bursts behind a graceful wait UI on `/p/<token>`. Monitor concurrent-session peaks; step Starter→Premium (16 devices) when it binds — Team revenue covers it. Size Premium-vs-Enterprise with Appetize once real peak concurrency is known. |
| `CS-07` | Cloud streaming | Minute-attribution accuracy. We meter streaming per designer **client-side in our own embed** (`ProtoStream` watch-seconds) rather than via Appetize reporting; closed tabs, network drops, or clock skew could drift our per-owner tally from Appetize's billed total — under-counting lets cost leak past a tier's budget. | `open` | `2026-06-12-share-economics-and-pricing-design.md` | Reconcile our monthly tally against Appetize's account total; round in our favour; alert on divergence beyond a threshold. If drift is material, add a server-side source of truth (Appetize session-end webhook / reporting API). Validate during Phase 2b before any paid enforcement. |
| `AS-07` | App Store | Guideline 2.5.2 re-opens for the App Store Prototo viewer — it downloads + executes JS at runtime (EAS Update bundles for recipients, Metro for designers). Expo Go is precedent but framed as a pure developer tool; recipients here are not developers. The successor to the (superseded) `AS-01`/`AS-04` for the loader-app path. | `open` | `2026-06-19-ios-viewer-app-store-design.md` §Open risks | TestFlight first; submit the designer/local-Metro **developer-tool** framing first, then enable recipient-native; explicit App Review notes. The smart universal-link → web-stream **fallback** means a rejection of the recipient-native path still ships a working app (designer tool) and recipients still get the stream — never blocked to zero. **UPDATE (2026-07-04): submitted builds 20/21/23 — Apple has NOT flagged 2.5.2 across two review rounds (both rejections were on other guidelines: 2.1(a) / 5.2.5 / 5.1.1(v) / 2.5.4). The developer-tool framing + built-in functional sample is holding so far.** |
| `AS-08` | App Store | UGC-boundary review risk for the viewer — recipients run content authored by *other* accounts; App Review may treat this as user-generated content needing moderation/reporting controls. Echo of the retired `AS-05` (App Clip era), now relevant for the loader app. | `open` | `2026-06-19-ios-viewer-app-store-design.md` §Open risks | Sign-in required (identity); per-account abuse controls + share-token revocation. Decide whether a report/abuse affordance is needed for v1 before recipient-native submission. |
| `SH-09` | Share infra | Pricing model shift: native on-device runs incur **no Appetize cost**, so they should not burn the Free 30-min/mo streaming budget — removing the per-minute cost lever that the Free/Plus economics were built on. | `open` | `2026-06-19-ios-viewer-app-store-design.md` §Open risks; `2026-06-12-share-economics-and-pricing-design.md` | Keep the **project cap** (Free = 1 active shared project) as the Free lever; decide the revised pricing story before recipient-native ships. Don't silently change pricing — this is a deliberate product decision for Sheri. |
| `SH-10` | Share infra | AASA propagation / universal-link reliability. iOS caches `apple-app-site-association` (~24h); a misconfigured AASA silently breaks app-open for `prototo.app/p/<token>`. Echoes the retired `AS-03` (App Clip era), now relevant for universal links. | `open` | `2026-06-19-ios-viewer-app-store-design.md` §Open risks | Validate the deployed AASA before App Store submission; the web-stream fallback covers any link that fails to open the app, so a propagation lag degrades gracefully rather than dead-ends. |
| `DC-10` | Dev client | Sign in with Apple required. Adding Google/email social login to the App Store viewer forces **Sign in with Apple** per App Review guideline 4.8. | `open` | `2026-06-19-ios-viewer-app-store-design.md` §Open risks | Include Sign in with Apple in the auth phase from the start (alongside the existing Supabase Google + email magic-link). |
| `DC-11` | Dev client | QR-driven bundle load = code-execution sink. The "Connect to your Mac" scanner opens a `prototo://expo-development-client/?url=…` deep link, which makes the dev client **load + run JS** from the scanned target — a malicious QR could drive the device at an attacker's server (RCE). | `mitigated` | `2026-06-20-ios-viewer-phase-2-connect-to-mac.md`; commit security review 2026-06-20 | **Mitigated in `parseConnectUrl` (`apps/prototo-app/lib/connect-url.ts`, commit `80c21d7`):** re-extracts the inner `url=` even from the `prototo://…` branch and accepts **only** loopback / RFC-1918 private / link-local hosts — blocks the public-internet vector by construction (tested). **Residual / deferred (defense-in-depth):** (a) a same-LAN attacker QR on shared Wi-Fi is still in-range — mitigate later with a short-lived shared secret printed by `proto start` and verified in-app; (b) scheme is currently exp/http/https (could tighten to exp-only); (c) `.local` mDNS + IPv6 ULA (`fc00::/7`) hosts are not yet accepted (only IPv4 + `localhost`/`::1`). Revisit if `proto start` ever serves on those. |
| `DC-12` | Dev client / App Store | **Return-to-shell + anti-bricking** for a standalone App Store viewer. Loading a shared prototype by swapping the whole JS runtime via `expo-updates` override means our shell's JS stops running — no in-app way back, AND a 2nd tapped share link can't be intercepted (our router isn't running). The dev client avoids this via its native launcher + dev-menu escape; a standalone build has neither. | `resolved` | `2026-06-20-ios-viewer-standalone-pivot-spike.md` | **RESOLVED 2026-06-20 by avoidance — standalone viewer abandoned before building** (analysis showed full-bundle-swap can't return-to-shell or open a 2nd link in one JS runtime). Superseded by the native-runtime direction (`DC-13`): the **native overlay-window "Home"** owns return regardless of the loaded bundle. |
| `DC-13` | Native runtime / App Store | **Custom native host drives private/alpha Expo internals.** The flagship "own our native shell" build mounts our RN root via a custom AppDelegate and drives `EXDevLauncherController.loadApp:`/`loadLocalBundle` + dev-menu delegates directly — brownfield + dev-client is **unsupported (Expo lists dev-client ✗ for brownfield)**, so we depend on private APIs that can break each SDK upgrade. Also the riskiest single unknown: can `loadApp:` mount a bundle into OUR root VC without the hardcoded `DevLauncherViewController` wrapper? | `open` | `2026-06-21-native-prototo-runtime-design.md`; device test 2026-06-21 | **DEVICE TEST (build 11) — own-the-root NOT solved.** Loader (crux 2/3) works, but on a fresh install the **dev-launcher still owns the window root** (shows "Finding Dev Servers"/launcher); our AppDelegate `startReactNative("main")` doesn't displace `ExpoDevLauncherReactDelegateHandler`, and no config does (launchMode = most-recent\|launcher only). Earlier sim "PASS" was masked (onboarding pre-dismissed + most-recent auto-load). **App-Store-clean goal unmet; build 11 not submittable.** Fix paths: (A) disable the dev-launcher's react-delegate/subscriber so our shell mounts + call loadApp on demand (fork-ish, uncertain loadApp survives); (B) defaultLaunchURL→hosted shell (network-dependent); (C) step back to cloud-stream + EAS-Internal. **Validate any fix on a FRESH device/erased sim.** **DECISIVE FINDING (2026-06-21, time-boxed (A) attempt): `expo-dev-launcher` is debug-only by design — the wall is framework-level, not config.** `ExpoDevLauncherReactDelegateHandler.createReactRootView` does `if !EXAppDefines.APP_DEBUG { return nil }`, and the pod only links in debug. So: **`developmentClient:true`** (EAS Debug-config dev client) → dev-launcher links + loadApp works, **but APP_DEBUG=true → its launcher owns the root** (build 11). **True Release** (APP_DEBUG=false) → launcher no-ops, **our shell is root** ✅ **but dev-launcher isn't linked** → `EXDevLauncherController` undefined (loadApp unavailable). Can't get both by config. "No-launcher + loadApp" requires **reimplementing the dev-launcher loader for release** (force the pod into Release link via Podfile + a custom controller delegate to mount bundles) — deep/fragile, fights the framework. Decision: invest in that fork vs fall back to cloud-stream + EAS-Internal (C) vs get a 2.5.2 read first. **UPDATE (2026-06-21): chose the fork; FORK GATE 1 PASSED via a cleaner lever than expected** — `pnpm patch` removing `apple.debugOnly` from expo-dev-launcher + expo-dev-menu makes them link in true Release while the react-delegate no-ops (`!APP_DEBUG`) → our shell owns the root, no launcher (fresh-sim Release, screenshot-confirmed; patches committed). Gate 2 (loadApp mounting in Release) reverse-engineered to a known recipe (see STATUS). Residual fragility unchanged: overrides framework debug-gating + private APIs; re-validate every SDK bump. **PROVEN + SHIPPED (2026-06→07): the full launcher-free loop (own root · loadApp · Exit-to-shell) works on-device (builds 12/13) and v1.0 build 23 is submitted to the App Store. The fork holds; the live residual risk is per-SDK-bump maintenance of the 3 patches (expo-dev-launcher + expo-dev-menu `debugOnly`; expo-updates source/`USE_DEV_CLIENT`/`AppController`).** |
| `DC-14` | Config hygiene | **`associatedDomains` duplicate + project/updates mismatch.** Earlier `eas update` failed manifest validation (duplicate `applinks:prototo.app`); separately `extra.eas.projectId` (`df9296f8`) ≠ `updates.url` project (`8c8ddf7d`). | `mitigated` | surfaced 2026-06-20; reviewed 2026-06-21 | **(a) associatedDomains — RESOLVED:** `ios/` is gitignored/generated, so there was never a committed entitlements file; the duplicate was a transient (stale on-disk `ios/` + an app.json mutation during the failed probe, since reverted). The **resolved config has exactly one** `applinks:prototo.app` (`npx expo config --json`), and `prebuild --clean` keeps it that way. **(b) project/updates mismatch — ACCEPTED (by design for now):** the app's EAS *build* project is `df9296f8`; it deliberately reads updates from the central **share** project `8c8ddf7d` (where prototypes live) — recipient-run is proven with this. `eas update` only "rewrites" it because the two normally match, but the app is **never** `eas update`-d (shares are published by `proto-cli`; the app ships via `eas build`). **Open follow-up (device pass):** test whether `updates.url` should instead be `df9296f8` with shares loaded cross-project via `loadApp:` — do not change mid-validation since recipient-run is proven with `8c8ddf7d`. |

## Resolved / closed

| ID | Area | Risk | Resolution date | Notes |
|---|---|---|---|---|
| (none yet) | — | — | — | — |

---

## How this file is maintained

1. **Adding a risk:** when a new spec lands, copy any cross-cutting items from its "Open risks" section into this file with a fresh ID. Inline-only risks (specific to one file, no downstream impact) stay in the spec.
2. **Updating status:** when a risk's status changes (mitigated → resolved, open → accepted, etc.), update the row + add a note in the right column.
3. **Closing a risk:** move the row to the "Resolved / closed" table with a resolution date. Don't delete — closed risks are useful history.
4. **No renumbering:** IDs are append-only. If a risk is closed, its ID is retired; subsequent risks in the same area continue counting up.

This file is human-readable and never machine-parsed — table format is fine to mass-edit by hand when a sweep is needed.
