# Proto

Native prototype tool for designers. Prompt-driven, runs on a real device, zero engineering knowledge required.

```bash
npm create proto@latest my-app
```

See [`docs/proto-master.md`](docs/proto-master.md) for the full product spec.
